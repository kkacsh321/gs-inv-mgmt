# GoldenStackers Inventory Roadmap

This roadmap is focused on operational reliability first, then channel integrations, then scale and automation.

## AI-First Execution Standard
- Every net-new workflow must define an in-flow AI assist path (not Tools-only) where it improves speed/quality.
- AI must remain operator-controlled: suggestion first, explicit approval before write actions, and full audit trace.
- AI behavior must be runtime-configurable (provider/model/profile/fallback) without redeploy.
- AI outputs used for decisions should expose confidence/citations and preserve evidence in history tables/logs.

## Progress Snapshot (as of April 26, 2026)
- Latest accounting/profitability hardening:
  - Dashboard, Reports, marketplace reconciliation, QBO-style exports, and chat/report snapshots now use one net convention (`gross + shipping charged - fees - label spend`), separate profit before returns, and final estimated profit after returns.
  - Cost basis precedence is now explicit: lot-assignment unit/allocated landed cost first, then remaining lot-level landed cost allocation for blank assignment rows, then product landed acquisition cost, then `product_cost` fallback.
  - Sale COGS now uses time-aware FIFO across multiple product repurchases/lots and inventory value uses FIFO remaining lot cost when lot history exists.
  - Mixed-value lots can now use assignment-level allocated dollars or `product_lot_assignments.allocation_weight` to split one bulk lot price across different products by value/share.
  - Whole-lot fallback allocation now supports `purchase_lots.expected_total_quantity`, so partial check-ins do not force early assigned products to carry the full lot cost before all products/items are entered.
  - Accounting exceptions now flag multi-product blank-cost lots that are still using equal quantity fallback with no allocation weights or expected quantity.
  - Lot Assignment report/export rows now include resolved landed unit/total cost and a `cost_source` label for accounting review of allocation basis.
  - Accounting Review and close packet exports now include Lot Allocation Source Summary grouped by `cost_source`, assignment count, quantity, resolved cost, and cost share.
  - Close readiness now warns when lot assignments still rely on equal fallback or missing lot cost basis.
  - Admin now includes an Accounting Close Sign-Off Tracker for accounting model acceptance and monthly close review, with owner/date/evidence capture and Go-Live Evidence Pack export (`accounting_close_signoffs.csv`).
  - Ask GoldenStackers and Reports now include role-gated AI Accountant access (`ai_accountant_use`) with read-only cited accounting snapshots, close-readiness review, write/tax/legal guardrails, and audit events for Reports accountant reviews.
  - Migration `0059_ai_accountant_permission` backfills AI Accountant access for existing ops/admin role-permission rows.
  - Added `GS-V10-021 Tax Reporting + Guidance` scope for tax review workspace, tax packet exports, tax exceptions, saved tax profiles, tax sign-offs, and advisory AI tax/accounting guidance with tax-advisor validation boundaries.
  - Dashboard 30-day net/profit, Ask/AI accounting snapshots, label spend, and shipping delta now use the same sale-window actual-economics basis as Reports; linked normalized marketplace-fee and shipping-label finance entries are allocated once per order, unlinked finance rows are ignored for profit inputs, and unmatched shipping-label rows are flagged as `unmatched_shipping_label_finance_entry`.
  - Dashboard all-time `Net Sales`, Sales table rows, Sales Copilot context, and Reports COGS/margin close-readiness math now consume the same actual-economics rows, exposing actual fee/label/net sources and keeping raw sale-field net only as audit comparison.
  - Marketplace reconciliation rollups now use the same actual-economics rows for fee, shipping, label spend, net before returns, and net after returns.
  - Inventory-cycle rebuy/resell analytics now use the same actual-economics rows for fees, shipping charged, label spend, net sales, and estimated margin versus known acquisition cost.
  - Reports `Sales` export rows now include actual fee, shipping charged, label spend, net before COGS, and source fields alongside raw field net.
  - Reports `QuickBooks Sales Export` now includes `cogs_source` beside FIFO COGS, `profit_before_returns_estimate`, and legacy `gross_margin_estimate`, preserving cost-basis provenance in bookkeeping staging rows.
  - Reports `QuickBooks Refund/Adjustment Export` now includes returned COGS, restocked COGS reversal, COGS source, and estimated profit impact for return/refund staging rows.
  - Accounting Close Readiness now applies restocked return COGS reversal totals when calculating net after returns and COGS, keeping close-review return impact aligned with the adjustment export.
  - Estimated tax detail rows now use actual-economics allocated buyer shipping when available and expose raw-field/source evidence for review.
  - Reports now includes `Tax Exceptions / Advisor Review` rows for missing jurisdiction/rate/category evidence, facilitator-channel scope review, exempt-category review, and taxable shipping on exempt items.
  - Reports now provides a `Download Tax Review Packet ZIP` with manifest and tax review CSVs; Accounting Close Packet ZIP includes tax exception evidence.
  - Reports `Orders` export rows now preserve raw order fee/label fields while adding normalized actual fee, actual label spend, actual shipping delta, actual net before COGS, and source columns.
  - Orders workspace table rows and Orders Copilot context now include the same normalized actual fee/label/net/source evidence while retaining editable raw order fields.
  - Dashboard live metrics now expose 30-day COGS source provenance from FIFO sale cost maps, making estimated profit traceable to explicit assignment cost, lot allocation weights, expected-quantity fallback, equal fallback, product default cost, or missing basis.
  - Sale FIFO cost maps now also expose sale-total COGS and allocation-level evidence rows with product, lot, assignment, quantity, unit cost, total cost, and cost source; missing-cost and nonpositive-margin accounting exceptions include that evidence summary for close review, Reports/Accounting Close Packet exports include a `Sale FIFO COGS Evidence` table, COGS Source Checks tie that evidence back to COGS & Margin totals, and AI Accountant review context/citations include a bounded evidence sample.
  - Dashboard live metrics now classify 30-day profit basis with `sales_30d_profit_basis_status` and surface a warning when sold COGS used equal fallback, mixed, or missing basis, so under-defined partial lots are visible before operators trust estimated profit.
  - Dashboard live metrics and no-rollup fallback rendering now upper-bound 7-day/30-day sales and order windows by the dashboard snapshot time, preventing future-dated rows from affecting current profit, COGS, shipping, and order counts.
  - Dashboard rollup explain/baseline diagnostics now use the same snapshot end bound as production dashboard metrics, keeping accounting probe totals aligned with UI behavior.
  - Reports `COGS & Margin Detail` now carries sale-level FIFO and lot cost-source columns into report exports and close packets so margin review can trace the COGS basis without cross-referencing separate lot-assignment reports.
  - Reports now adds a `Sold COGS Source Summary` grouped by FIFO cost source, included in Accounting Review, AI Accountant context, and close packet exports.
  - Accounting Close Readiness now warns on sold COGS that used equal-quantity fallback or missing/unknown basis, making close status reflect the source actually used for sold-margin calculations.
  - Ask/AI Reports snapshots now include sold COGS source mix lines and citation metadata, keeping AI-visible margin explanations aligned with dashboard and Reports provenance.
  - Listing Wizard and Listings now recover from transient eBay Inventory `25001` Core Inventory Service errors with a one-time simplified payload retry, and graded-coin readiness/aspect defaults now enforce approved-grader evidence for numerical grades such as `PCGS MS69`.
  - Listing Wizard, Listings, and the shared eBay dependency preflight now validate selected listing condition values against eBay category condition policies, preventing saved defaults/presets from publishing category-invalid conditions that cause eBay `25021`.
  - Added a dedicated AI Accountant workspace with role-gated monitor rows, recommended cleanup actions, in-app message audit events, recent message history, and retry-safe Slack alert queuing through the notification outbox.
  - AI Accountant workspace now runs its own read-only LLM review over monitor rows, accounting exceptions, and dashboard profit-basis evidence, with grouped action summaries, scheduled/in-app monitor question status counts, downloadable evidence packets including captured Ask/Slack answer and answer-follow-up evidence plus answer status counts, visible answer-status counts/warnings in the Recent AI Accountant Answers panel, cited audit metadata, structured JSON rendering, deterministic monitor-review fallback when all LLM runtimes fail, accepted/edited/rejected outcome tracking, visible recent review outcome history, a latest-outcome follow-up banner for edited/rejected reviews, and one-click recommended automation setup plus runtime visibility for the core monitor timing/chat/Slack/LLM/web-research defaults.
  - Added an optional sync-runner AI Accountant scheduled monitor with runtime controls for schedule, lookback window, minimum alert severity, empty-run recording, and Slack notification-outbox queuing.
  - Daily Slack reports now include 24-hour estimated COGS plus a compact COGS source mix, and log the per-source COGS totals in the integration event.
  - Admin manual business status reports and previews now include estimated COGS plus the same COGS source mix, keeping operator-triggered Slack summaries aligned with dashboard, Reports, AI context, and scheduled Slack reporting.
  - Orders workspace raw-field fallback now uses one canonical helper for `subtotal + shipping charged - fees - label spend` and exposes `actual_net_source` for rollup-vs-field provenance.
  - Reports shipping-economics detail/summary now reuse the same sale actual-economics allocation as dashboard/profit rows, including gross-weighted multi-line order shipping charged and normalized label spend.
  - Purchase Lot `Lot P/L Snapshot (Estimated)` now aggregates product sales through the same actual-economics rows, so lot-level estimated net/profit uses normalized order fees and label spend.
  - Purchase Lot `Lot P/L Snapshot (Estimated)` now FIFO-consumes sales across all lots for repurchased products and attributes only the consumed sale quantity/net/COGS/profit back to the selected lot, with sold COGS source totals visible in the lot snapshot.
  - Legacy in-memory Reports inventory-cycle fallback now consumes repository actual-economics rows when available, keeping degraded report paths aligned with dashboard/Reports fee, shipping, label spend, and net calculations.
  - Legacy in-memory marketplace reconciliation fallback now consumes repository actual-economics rows when the primary rollup is unavailable, preserving normalized fee/label/net math in degraded report paths.
  - Reports `Sales Detail` dataframe/export now preserves raw field net while exposing actual fee, shipping charged, label spend, actual net before COGS, source fields, and actual-backed `net_sales` on fallback report paths.
  - Dashboard no-rollup fallback now uses repository actual-economics rows for 7d/30d net, shipping charged, label spend, fee, shipping delta, and estimated profit when live metrics are unavailable.
  - Standalone eBay fee reconciliation fallback now prefers normalized `order_finance_entries` marketplace-fee sums from `order.finance_entries` before notes-derived fee breakdowns or sale fee fields.
  - Daily Slack ops report 24h gross/net now uses repository actual-economics rows when available and otherwise falls back to the canonical net formula (`gross + shipping charged - fees - label spend`).
  - Reports accounting exception rows now use normalized actual fee evidence before raw `sales.fees` for missing-fee and margin checks.
  - Dashboard live metrics now use repository actual-economics rows for 7-day net sales as well as 30-day net/profit.
  - Admin manual business status reports now use repository actual-economics rows for window gross, fees, buyer shipping, label spend, and net when available.
  - Ask/AI accounting context builders now roll back failed actual-economics and FIFO cost-map lookups before falling back, preventing aborted SQL transactions from leaking into later report reads.
  - Lot-total allocation now avoids double-counting when one lot contains a mix of explicitly costed and blank-cost products.
  - Added regression coverage for lot-only product costs, mixed explicit/blank lot allocations, mixed-value allocation weights, allocation-basis exports/summaries, close-readiness allocation warnings, ambiguous equal-fallback lot exceptions, partial lot check-ins with expected quantity, multi-lot repurchase FIFO, dashboard inventory cost/profit, sale FIFO maps, Reports helper cost maps, and chat/report accounting snapshots.
- Next accounting/tax hardening focus:
  - Run production-sample monthly close review through Accounting Close Packet + Accounting Close Sign-Off Tracker and capture owner/date/evidence, readiness status, blockers, and period drift warning count.
  - Add deterministic drift checks comparing dashboard KPIs, Reports exports, QBO staging exports, Slack summaries, and AI context totals for the same close period.
    - Progress (April 28, 2026): first `Accounting Period Drift Checks` table compares close-readiness totals against QBO sales/refund staging exports and is included in Accounting Review plus the Accounting Close Packet.
    - Progress (April 28, 2026): drift checks now include Dashboard Live Metrics 30-day comparisons when the selected Reports window matches the dashboard 30-day window ending on `To Date`.
    - Progress (April 28, 2026): drift checks now include Slack-style daily/weekly business summary comparisons when the selected Reports window matches the business summary window ending on `To Date`.
    - Progress (April 28, 2026): drift checks now include Ask/AI accounting snapshot 30-day comparisons when the selected Reports window matches the AI/dashboard 30-day window ending on `To Date`.
    - Progress (May 13, 2026): Slack-style and Ask/AI drift checks now validate explicit `profit_before_returns` and `estimated_profit_after_returns` fields separately, replacing ambiguous estimated-margin wording in close-review evidence.
    - Progress (April 28, 2026): Reports Copilot and AI Accountant structured contexts now include Accounting Period Drift Checks, and AI Accountant audit citations include the drift-check evidence table for human review.
    - Progress (April 28, 2026): drift warnings now feed back into Accounting Close Readiness as a blocking `Period Drift Warnings` check.
  - Progress (April 28, 2026): Reports now exports latest Accounting Close Sign-Off Tracker rows in the Accounting Close Packet and passes them into AI Accountant context/citations, so production-sample review can verify owner/date/evidence and packet reference from the same evidence bundle.
  - Progress (April 29, 2026): Reports now adds an Accounting Close Sign-Off Review table to Accounting Review, the Accounting Close Packet, and AI Accountant context/citations, flagging stale approved sign-offs when recalculated readiness, blocker count, drift warning count, or packet/evidence references no longer match.
  - Progress (April 29, 2026): Reports now adds Accounting Close Formula Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, proving close arithmetic and blocking close-ready status if profit-before-return or return-impact formulas drift.
  - Progress (April 29, 2026): Reports now adds Accounting Sales Component Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying Sales Detail fee/shipping/label components to COGS & Margin close totals and blocking close-ready status on component drift.
  - Progress (April 29, 2026): Reports now adds Accounting Return Tie-Out Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying Returns refund totals, return COGS reversal, and QBO adjustment profit impact together before close sign-off.
  - Progress (April 29, 2026): Reports now adds Accounting Inventory Valuation Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying stocked inventory landed cost and Inventory Snapshot value to close-readiness inventory value before sign-off.
  - Progress (April 29, 2026): Reports now adds Accounting Fee Evidence Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying eBay Fee Reconciliation and Fee Source Priority evidence to Sales Detail and blocking close-ready status on fallback/mismatched fee evidence.
  - Progress (April 29, 2026): Reports now adds Accounting Shipping Evidence Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying Sales Detail shipping charged/label spend to Shipping Economics detail and summary evidence before close sign-off.
  - Progress (April 29, 2026): Reports now adds Accounting Reconciliation Tie-Out Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying marketplace reconciliation sales/return totals to Sales Detail, Returns, net-after-return formulas, and close flags.
  - Progress (April 29, 2026): Reports now adds Accounting COGS Source Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying Sold COGS Source Summary totals to COGS & Margin Detail and blocking close-ready status when sold COGS uses equal fallback or missing/unknown basis.
  - Progress (April 29, 2026): Reports now adds Accounting Lot Allocation Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying Lot Allocation Source Summary totals to Lot Assignment detail and blocking close-ready status when assignments use equal fallback or missing/unknown basis.
  - Progress (April 29, 2026): Reports now adds Accounting Exception Queue Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying exception queue total/P0/P1 counts to close readiness and keeping P0 exceptions visibly blocking.
  - Progress (April 29, 2026): Reports now adds Accounting Margin Anomaly Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, tying negative/nonpositive COGS & Margin rows to close readiness and `nonpositive_margin` exception evidence before close sign-off.
  - Progress (April 29, 2026): Reports now adds Accounting Close Consistency Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, validating that final close-readiness status, blocker/warning counts, blocker/warning text, and close-check fail/warn rows agree.
  - Progress (April 29, 2026): Reports now adds Accounting Close Packet Completeness Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, validating that required packet evidence artifacts are present and populated before sign-off evidence is trusted.
  - Progress (May 1, 2026): Accounting Close Packet Completeness Checks now require QuickBooks Refund/Adjustment export evidence when return/refund activity exists, while optional no-activity artifacts no longer warn solely because they are absent.
  - Progress (April 29, 2026): Reports now adds Accounting Close Packet Manifest Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, validating selected packet prefixes and manifest row counts against exported report dataframes.
  - Progress (April 29, 2026): Reports now adds Accounting Close Packet Hash Checks to Accounting Review, Reports Copilot context, AI Accountant context/citations, and Accounting Close Packet exports, adding SHA-256 CSV hashes to the packet manifest and validating hash evidence exists for selected packet artifacts.
  - Progress (April 29, 2026): Accounting Close Packet manifests now include `accounting_close_packet_evidence_hash_sha256`, a stable SHA-256 fingerprint derived from selected close CSV payloads, date range, and close summary for deterministic sign-off evidence references.
  - Progress (April 30, 2026): Admin Accounting Close Sign-Off Tracker now captures the Accounting Close Packet evidence hash, and Reports Accounting Close Sign-Off Review compares approved sign-off hashes against the recalculated packet evidence hash to flag stale packet evidence.
  - Progress (April 30, 2026): Reports now shows and exports an Accounting Close Packet Evidence Hash table so reviewers can copy the current packet evidence hash directly into the Admin Accounting Close Sign-Off Tracker.
  - Progress (May 3, 2026): Reports now includes an in-page Record Accounting Close Sign-Off workflow that writes `accounting_close_signoff` audit evidence with owner/date/status, packet ref, evidence link, recalculated readiness/blocker/exception/drift counts, and the current close-packet evidence hash.
  - Progress (April 30, 2026): Reports Accounting Close Sign-Off Review now warns when an approved close sign-off has packet evidence but no matching packet hash, keeping incomplete deterministic evidence visible before close approval is trusted.
  - Progress (April 30, 2026): Reports Accounting Close Sign-Off Review now compares approved sign-off exception counts against recalculated close total exceptions, flagging stale approvals when exception totals drift.
  - Progress (April 30, 2026): Reports Accounting Close Sign-Off Review now warns when approved close sign-offs are missing owner or sign-off date evidence, keeping monthly close approvals tied to an accountable reviewer.
  - Progress (April 30, 2026): Reports Accounting Close Sign-Off Review now validates approved sign-off dates are parseable, not before period end, and not future-dated.
  - Progress (April 30, 2026): Accounting Close Readiness now carries shipping charged, label spend, and shipping delta totals, and Accounting Period Drift Checks compare those components against Dashboard Live Metrics 30-day values.
  - Progress (May 1, 2026): Accounting Formula Checks now validate close-summary shipping delta from shipping charged minus label spend, keeping stale close shipping math as a close-ready blocker.
  - Progress (May 1, 2026): Accounting Formula Checks now validate close-summary net before COGS from gross sales plus shipping charged minus fees and label spend before close sign-off.
  - Progress (April 30, 2026): Accounting Close Readiness now carries fee totals from Fee Source Priority evidence, and Accounting Period Drift Checks compare close-period fee totals against Dashboard Live Metrics 30-day fee totals.
  - Progress (May 13, 2026): Accounting Period Drift Checks now validate Dashboard Live Metrics 30-day net, shipping delta, profit before returns, and estimated profit after returns formulas before dashboard totals are trusted in close review.
  - Progress (May 14, 2026): Accounting Close Readiness now emits explicit `profit_before_returns` and `estimated_profit_after_returns` summary fields alongside legacy `fifo_margin` and `net_after_returns_and_cogs`; drift checks prefer the clearer fields while keeping legacy fallback compatibility.
  - Progress (May 14, 2026): Reports close-readiness UI metrics, Reports Copilot context, and AI Accountant review context now prefer the explicit close-summary profit fields, keeping legacy key fallback only for older packet/evidence compatibility.
  - Progress (May 6, 2026): Dashboard estimated-profit cards now warn when 30-day COGS includes equal-fallback, mixed, or missing cost basis, pointing operators to expected lot quantity, allocation weights, or assignment costs before accepting profit for partially checked-in bulk lots.
  - Progress (May 6, 2026): Dashboard live metrics and no-rollup fallback rendering now exclude future-dated sales/orders from current 7-day/30-day windows, with regression coverage for gross/net/COGS/profit.
  - Progress (May 6, 2026): Dashboard rollup explain/baseline diagnostics now pass the same `end_dt` snapshot bound into dashboard-style sales/order SQL probes.
  - Progress (May 13, 2026): Accounting Period Drift Checks now validate QuickBooks Sales Export net and `profit_before_returns_estimate` formulas before QBO staging totals are trusted in close review; `gross_margin_estimate` remains populated only as a compatibility field.
  - Progress (May 1, 2026): Accounting Period Drift Checks now validate QuickBooks Refund/Adjustment Export estimated profit impact from refund components and COGS reversal before QBO return adjustment totals are trusted in close review.
  - Progress (May 13, 2026): Accounting Period Drift Checks now validate Slack-style and Ask/AI accounting snapshot `profit_before_returns` formulas from net minus COGS and tie out after-return estimated profit separately before summary/snapshot totals are trusted in close review.
  - Progress (May 14, 2026): Accounting Period Drift Checks now fall back to legacy Slack/Ask-AI `estimated_margin` evidence as before-return profit when `profit_before_returns` is absent, preventing older audit snapshots from producing false drift while still showing the observed source field.
  - Progress (May 18, 2026): Sale FIFO cost maps now return allocation-level COGS evidence, accounting exceptions cite the resolved source/evidence path for missing cost basis and nonpositive margin review, Reports/Accounting Close Packet exports include the sale-level FIFO evidence table, close checks validate evidence totals/counts against COGS & Margin, Reports Copilot/AI Accountant context/citations include the evidence rows for review, scheduled and manual AI Accountant reviews now carry bounded FIFO evidence rows plus summary/audit row counts, Recent AI Accountant Messages and Review Outcomes surface FIFO evidence row counts, and the AI Accountant page shows packet monitor/exception/FIFO/outcome/hash-index/artifact counts, integrity-error count, verified artifact count, manifest/expected-manifest rows, evidence hash, packet integrity verification, manifest verification, and action-summary task counts while manual workspace review metadata and the review hash index capture packet hash/row-count/artifact-hash/integrity/manifest evidence, integrity error counts, expected manifest rows, and action-summary task counts. The latest-review banner warns when an accepted review points at an unverified evidence packet, and the action summary adds a P0 evidence-packet integrity row when packet review is needed. The Evidence Packet exports `accounting_exception_queue.csv`, `sale_fifo_cogs_evidence.csv`, `ai_accountant_review_hash_index.csv`, and `evidence_summary.json` row counts/SHA-256 artifact hashes plus schema version, artifact count, artifact names, action-summary task counts, and manifest cross-checks.
  - Progress (April 30, 2026): Added an Order Map page with offline state/country centroid pins, date/marketplace/status filtering, mapped order/revenue metrics, aggregate destination detail tables, CSV export, shipped-order default filtering, an opt-in toggle for unshipped/paid orders, and marketplace/status counts per destination.
  - Promote saved tax profiles and tax sign-off records from planned scope into implementation so jurisdiction/channel assumptions, evidence notes, effective dates, and human-validation status are captured.
    - Progress (April 28, 2026): Admin now captures tax profiles and tax reporting sign-offs as audit-backed records with CSV export and Go-Live Evidence Pack artifacts (`tax_profiles.csv`, `tax_reporting_signoffs.csv`).
    - Progress (April 28, 2026): Reports can apply latest active Admin tax profiles to tax review assumptions and includes selected profile metadata in the Tax Review Packet manifest/evidence hash.
    - Progress (April 28, 2026): Reports includes tax reporting sign-off evidence in Tax Review Packet and Accounting Close Packet exports.
  - Extend AI Accountant/Tax guidance to cite tax packet/profile/sign-off evidence while preserving advisory-only boundaries and tax-advisor validation language.
    - Progress (April 28, 2026): Reports AI Accountant context and audit citations now include tax review assumptions, selected tax profile evidence, and tax reporting sign-off rows, with instructions to route filing/remittance/legal conclusions to human review.
- Latest eBay listing hardening:
  - Listing Wizard and Listings publish/revise now fetch eBay Taxonomy required item specifics by category and cache normalized aspect requirements in `ebay_category_aspects`.
  - Cached required specifics auto-hydrate when a category is selected and feed readiness/publish blockers when required item specifics are missing.
  - Publish calls now retry eBay `25604 Product not found` after probing the SKU inventory item, covering Inventory API eventual consistency after inventory upsert/offer creation.
  - Listing Wizard and Listings publish/revise now include an explicit `Main eBay Image` selector; the selected image is sent first in `imageUrls` so it becomes the eBay gallery image.
  - Direct-post dependency preflight and eBay Ops relist guardrails now detect immediate-payment policies and block live auction publish unless an `Auction Buy It Now` price is set, preventing eBay `25003` publish failures after offer creation.
  - eBay media upload is now eBay-hosted only for images, supports MP4 videos plus MOV/QuickTime conversion to MP4 via `ffmpeg`, uses eBay's required video upload content type, and records video upload diagnostics.
  - Listing Wizard and Listings publish/revise now verify video attachment at Inventory API checkpoints and, after live publish, via Trading API `GetItem` `Item.VideoDetails.VideoID`; if no supported video is selected/linked, publish continues image-only with an explicit warning in UI and metadata.
  - Listing Wizard Step 1 now keeps the Product dropdown bounded to recent products by default and adds product search by SKU/title/category/material/ID so older products can be selected without loading hundreds of rows into the default control.
  - `listing_wizard_recent_product_limit` is now a seeded/required runtime setting so each environment can tune the Step 1 dropdown cap through Admin runtime settings.
  - Migration `0067_listing_wizard_recent_product_limit` backfills the dropdown cap default for existing environments while preserving nonblank overrides.
  - Listing Wizard and Listings create flow now support lot/bundle listing metadata for selected products, including units per marketplace listing, available lot count, stock overcommit blocking, and expected-net COGS adjusted for units inside each lot.
  - Next eBay listing focus: run one live production listing with an MP4/MOV video, capture the new video diagnostics, and confirm the eBay app/mobile listing display after eBay review latency.
- v0.2 completed:
  - Inventory ledger (`inventory_movements`)
  - Source master data (`inventory_sources`)
  - Orders domain (`orders`, `order_items`) with sales linkage
  - Returns/refunds workflow (`returns`) with restock/non-restock handling
  - Shipping queue enhancements (carrier presets, shipment exports, exception actions)
  - Invoice/receipt generation page with multi-template branding and print/export support
  - Saved document template profiles in DB with environment-scoped defaults
  - Data quality validation layer (required fields, duplicate guards, tracking/amount checks)
  - Role-based access basics (viewer/ops/admin adapter, UI write guardrails, user-attributed audit events)
  - Admin page for managing users and role-permission mappings in-app
  - Password-required user creation + login/logout gating when password auth is enabled
  - Admin migration operations (upgrade + guarded downgrade)
  - Admin maintenance operations (seed, operational data reset)
  - Admin backup operations (SQL dump, S3 upload/list, guarded non-prod restore)
  - Source master data now supports optional `account_id` and `payment_method` tracking
  - Source master data now supports optional `source_url` links for vendor/dealer records
- v0.2 in progress:
  - No open v0.2 items
- v0.3 completed:
  - Sync queue foundation (`sync_runs`, `sync_events`, `sync_errors`) with initial Sync telemetry page
  - Added background sync runner service (`app.services.sync_runner`) with scheduled eBay pull/import execution and automatic run/event/error lifecycle logging
  - eBay pull stub now writes tracked sync runs/events/errors with queue state transitions
  - eBay pull import now upserts local `orders`/`order_items`/`sales` (SKU/listing mapping still iterative)
  - eBay pull import now enriches local sales with fulfillment tracking/provider/shipped/delivered updates from eBay shipping fulfillment APIs
  - eBay pull line-item mapping now handles ambiguous multi-item SKU/listing edge cases with title/price/status disambiguation before linking
  - Sync retries now supported with retry counters and parent-run linkage
  - Retry flow can now execute eBay pull/import directly from Sync page
  - Sync/eBay now show traceability metrics: `line_items_with_listing_link`, `line_items_unmapped_sku`, `auto_listings_created`
  - Added v0.3 scope item for media visibility and product-driven eBay listing creation
  - Product and Listing pages now include image/video preview galleries
  - Media upload flows now support in-browser camera photo capture plus video/file picker capture paths across Media Library, Products, and Listings
  - Added optional enhanced webcam capture mode via `streamlit-webrtc` (desktop frame capture + recorded webcam video import)
  - Media Library, Product/Listing create media blocks, and Product/Listing media managers now run outside forms for live enhanced capture controls
  - Media file access now includes single-file preview/download/open URL and bulk ZIP download for selected media rows
  - Products page now supports “Create eBay Listing From Product” with optional media attachment linkage
  - Listing workflows now enforce stricter publish/create validation for required fields and media when status is `active`
  - Listings page now supports direct eBay publish (fixed price/auction) and writes back eBay listing ID/URL
  - Added user/environment-scoped eBay publish presets for one-click policy/location/category defaults
  - eBay publish flow now supports pushing selected images to eBay EPS and attaching one uploaded MP4/MOV video with MOV conversion and diagnostics
  - eBay publish flow now supports explicit main-image selection by ordering the selected image first in eBay `imageUrls`
  - Added in-app eBay listing lifecycle actions: revise, end, and relist for eBay-linked listings
  - Added dedicated eBay Ops page with filtered tables and bulk end/relist/revise queue operations
  - eBay Ops now includes `eBay API Listings` tab for live offer/listing status visibility from eBay API
  - eBay Ops now supports bulk category/policy assignment helpers with optional immediate push to eBay offers
  - eBay Ops now includes `Policies & Locations` tab for refreshing merchant locations/policies from eBay and applying defaults to ops workflows
  - Added sandbox-aware seller-ops guardrails (`EBAY_ALLOW_SANDBOX_SELLER_OPS`) so unreliable sandbox policy/location behavior does not block day-to-day development
  - Shipping page now supports pushing tracking updates to eBay orders and records push telemetry in sync runs/events/errors
  - Sync page now includes an exception queue (unresolved-first) with resolve, retry-run, and retry+execute-now workflows for eBay order import failures
  - Added dedicated `eBay Push History` tables/views in Shipping and eBay Ops for run/event/error audit triage
  - Added eBay Push History quick actions: retry run, open in Sync, and resolve all unresolved errors for selected run
  - Reports page now includes accounting-ready exports (sales + refunds/adjustments with COGS inputs), marketplace reconciliation totals, and accounting validation/discrepancy flags
  - Reports now include FIFO + lot-weighted cost basis calculations and exportable margin analytics by SKU, marketplace, and month
  - Started GS-V03-008 with new Operations Home page: role-aware quick actions and centralized queue cards (needs listing/shipment, sync failures, accounting exceptions)
  - Added reusable saved filter presets across Products/Listings/Sales/Orders (session-scoped, user-specific)
  - Upgraded saved filters to DB-backed profiles (persistent across restarts/devices per environment/user/scope)
  - Added default-per-scope and team-shared saved filters for Products/Listings/Sales/Orders
  - Added Admin governance controls for shared filters: ownership transfer and admin delete workflows
  - Standardized row action UX (view/edit/sync/timeline) across Products/Listings/Sales/Orders and Operations Home queues
  - Added entity timeline views combining audit log + sync lineage for product/listing/order/sale drill-down
  - Added env-gated sync job controls so eBay pull/import and eBay tracking-push can be enabled/disabled safely per environment (worker + UI enforcement + status visibility)
  - Added Admin `Sync Jobs` tab to centrally view sync job enablement and generate env snippets for safe toggle changes
  - Hardened retry controls: disabled jobs now also block queued retry creation in Sync and eBay Push History quick actions
  - Added Sync queue guardrail to detect and bulk-finalize queued runs for disabled jobs, with auto-closure notes for auditability
  - Added sync job dispatcher scaffold (`execute_sync_job`) and job catalog with placeholder toggles for future QuickBooks/Shopify jobs
- v0.3 in progress:
  - No open v0.3 items
- v0.4 in progress:
  - Started GS-V04-001 Operations Home command-center consolidation
  - Added marketplace/provider filters directly on Operations Home
  - Added queue-router table with one-click navigation to execution pages
  - Fixed Sync Failures queue field mapping (`job_name`, `completed_at`) for reliable run triage visibility
  - Added inventory -> eBay -> shipping workflow stage table with direct page routing for daily operator flow
  - Added SLA watch metrics (oldest needs shipment, failed sync, needs listing, eBay draft age) for queue prioritization
  - Added dedicated eBay Listing Workflow queue tab so listing operations are separated from shipping/sync triage
  - Added saved command-center presets for Operations Home filters/queue-view mode with team-shared and role-scoped sharing support
  - Started GS-V04-002 by introducing a reusable table-toolbar UX (row count, active-filter summary, CSV/XLSX export) on Products, Listings, and eBay Ops tables
  - Added v0.4 side-panel detail/edit UX first slice for Products + Listings to speed daily inventory and eBay listing corrections without page switching
  - Extended side-panel detail/edit UX to Sales + Orders so core inventory->listing->sale->order correction workflows stay in-page
  - Completed GS-V04-002 keyboard-friendly quick actions with global sidebar command palette shortcuts (type command + Enter navigation across core pages)
  - Added first Comp Tool workflow (Tools tab) using inventory/title/image-hint queries to fetch eBay sold/completed comparables and summarize suggested pricing ranges
  - Enhanced Comp Tool with optional web-search fallback and optional AI/LLM synthesis for pricing guidance when eBay sold comps are sparse
  - Added Admin `AI Runtime` controls with DB-backed OpenAI/LocalAI profiles (base URL, model, endpoint type, key, timeout/tokens, default flag) so LLM routing can change live without environment-variable restarts
  - Added Admin `Env Config` tab to view configured `.env` keys, edit safe non-DB settings, and sync missing keys from `.env.example`
  - Added DB-backed `runtime_settings` with Admin management UI for live overrides (env fallback) and wired first runtime-overridable keys into Tools/eBay listing/ops defaults
  - Extended runtime overrides to spot-price provider/symbol/base-url settings and sync job toggles/worker limits (DB-first with env fallback)
  - Added live sync-toggle save action in Admin `Sync Jobs` and simplified `.env*` files by removing runtime-managed defaults
  - Started comp-capture hardening track for web fallback: deeper price extraction from destination pages (app-state JSON, metadata, and tiered quantity pricing) with better price confidence telemetry
  - Added web-fallback confidence scoring/labels with minimum-confidence filtering in Comp Tool to reduce noisy comp rows during pricing analysis
  - Expanded Comp Tool web filtering controls with numeric confidence threshold, parser-source filter, and domain include/exclude filters for faster operator-quality triage
  - Added domain-aware web extraction pass (JSON-LD, eBay/Amazon/common dealer templates, richer request headers) to reduce missed listed-price capture on fallback links
  - Expanded comp web parsing for cents-based JSON fields and dealer-tier/superscript price markup; page-detail fetch now also enriches low-confidence single-price rows
  - Added web parser telemetry in Comp Tool (domain and parser-source summaries) plus confidence weighting by extraction source to target underperforming domains faster
  - Added web comp extraction fallback via `r.jina.ai` reader proxy when direct page fetch misses pricing (helps JS-heavy or bot-protected pages)
  - Expanded web price parsing with keyword-led numeric capture (for layouts like `as low as 7.99` / `only 9.99` without explicit `$` token)
  - Expanded comp extraction coverage for ecommerce metadata/JSON keys (`og:price`, `twitter:data1`, `priceString`/`formattedPrice`) to improve non-standard listing page capture
  - Improved web comp listed-price selection from min-hint to representative median-hint, reducing low outlier skew from noisy snippets
  - Broadened destination page-detail fetch targeting beyond only zero/one-hint snippets to improve recovery when snippet prices are incomplete
  - Expanded domain-specific comp parsers for Etsy/Walmart and added broader class-based price-container fallback extraction
  - Added bullion-dealer focused parser expansion (APMEX/JM/SD/Monument/Provident/Bold/BGASC/etc.) and domain-aware confidence weighting to improve comp ranking reliability by source
  - Expanded dealer coverage again for comp extraction/weighting (BullionExchanges/HeroBullion/Silver.com/Kitco/USGoldBureau/LibertyCoin) to improve missed-price capture on key bullion domains
  - Added Admin-managed runtime dealer-domain configuration (`comp_dealer_domains_csv`) so comp parser/weighting target domains can be updated live without deploys
  - Added dedicated Admin `Comp Config` tab for dealer-domain management and normalized domain validation/cleaning on save
  - Added AI-assisted `Coin Grader` and `Coin Identifier` tools (camera/upload image support, runtime-configured LLM, and optional web-hint fetch for identification follow-up)
  - Added persistent `coin_ai_runs` history (grader/identifier) with optional product/listing links and recent-run views in Tools for auditable AI-assisted coin workflows
  - Added obverse/reverse multi-image support for coin grading/identification prompts to improve AI confidence and reduce one-angle misclassification
  - Added separate multimodal model configuration in Admin AI Runtime profiles, so image/camera tools can run a vision model independently from text/comps model
  - Added full edit UX for existing AI runtime profiles (all fields editable from selected profile with proper state refresh on profile switch)
  - Added inventory AI fields (`ai_graded`, `ai_grading_description`, `ai_description`) and tool-driven actions to create/update products from Coin Grader/Identifier/Comp outputs
  - Added tool input media persistence so grader/identifier/comp hint images can be stored as media assets for future listing/inventory reuse
  - Added LocalAI multimodal guardrails and clearer errors (vision-capability detection + endpoint guidance), plus identifier JSON truncation recovery/retry handling for more reliable structured results
  - Updated Coin Grader prompt contract to include professional-grading recommendation (`YES/NO/CONDITIONAL`) with cost-threshold rationale
  - Implemented async/cached web fallback page-detail fetch pipeline for comps (parallel fetch + LRU cache), improving coverage without serial UI latency spikes
  - Started GS-V04-003 with reusable eBay listing readiness checks and a Listings-page readiness queue (`ready/blocked`, score, blockers/warnings) using default publish preset policy/location/category context
  - Added first unified listing orchestration queue in Listings (`ready/blocked/published/error`) to centralize execution status for eBay listing workflows
  - Added listing review gate: new listings are forced to `draft` with pending review, Listings page supports approve/reject/pending review actions, and active status transitions are blocked unless approved
  - Added bulk listing review controls in Listings readiness queue (approve/reject/pending with notes) and readiness now explicitly blocks non-approved listings for publish flow
  - Added Listings reviewer dashboard metrics (pending count, oldest pending age, approvals in 24h/7d, reviewer status breakdown) for review throughput visibility
  - Added Admin-configurable two-person listing review policy (`listing_review_two_person_required` + channel list) and repository enforcement so active status can require reviewer/publisher separation on selected marketplaces
  - Added listing review-history persistence and side-panel timeline visibility so approve/reject/pending decisions are traceable per listing over time
  - Added review audit exports in Reports (`Listing Review Activity`, `Listing Review Summary`) with reviewer/date/channel visibility for compliance and coaching
  - Added Listings bulk publish batch planner (dry-run validation + publishability reasoning + batch-ID tagging) to stage reviewed-ready eBay listings for grouped execution
  - Added execute-batch publish from Listings queue with per-listing success/error results and persisted batch execution metadata for partial-failure triage
  - Added Listings bulk publish execution history view (filterable/exportable) so completed batch runs are visible without leaving listing operations
  - Expanded bulk publish execution history to persist/surface failed rows with status/message for direct operational triage
  - Added quick retry action from bulk publish history failed rows (retry batch ID + immediate re-execution path)
  - Started GS-V04-014 Coin Reference Database: added DB-backed `coin_reference_catalog` model + migration for in-app numismatic reference records
  - Added Coin Database tools UI for searchable coin specs/value bands with create/edit and CSV export (free-source friendly, no paid dependency required)
  - Added dev seed sample coin-reference records (Morgan/Peace/Standing Liberty) to accelerate local testing
  - Added Coin Database CSV import/upsert workflow (dry-run/apply, key-based matching, and row-level error reporting) for free/public dataset ingestion
  - Completed paid-source adapter contract guardrails for coin reference data (`none`/`greysheet`), including runtime feature toggles, license acknowledgment gate, and production-allow switch
  - Added dedicated Admin `Comp Config` editor for paid-source adapter settings with validation status and safe-reset action (no generic runtime-table editing required)
  - Started GS-V04-015 intake/listing UX flow: added explicit `products.coin_reference_id` linkage and UI support to tie inventory products to coin catalog records
  - Products create flow now supports coin-reference-assisted prefill (title/category/metal/weight/description) for faster and cleaner inventory intake
  - Listings create flow now supports auto-title from linked product/coin reference plus optional coin context + AI description/grading injection into listing details
  - Product side-panel now includes one-click Smart eBay Draft creation (coin-reference + AI-aware details, markup-based draft pricing, optional auto-attach of unassigned product media)
  - Added dedicated Coin Intake Wizard page (`pages/20_Coin_Intake_Wizard.py`) to run Reference -> AI assist -> Product -> optional draft eBay listing in one guided workflow
  - Hardened intake wizard AI prefill reliability:
    - identifier/grader/comp now force-apply into form keys safely before widget initialization in both Coin Intake and Inventory Intake wizards
    - identifier mapping now tolerates alternate JSON key shapes (`title`/`item_title`/`name`, `metal_type`/`composition`, `description`/`details`/`summary`) and falls back to normalized text for description when structured keys are sparse
    - added targeted regression tests (`tests/test_intake_wizard_prefill_helpers.py`) and browser verification spec (`tests/e2e/intake_prefill.spec.ts`)
  - Added Products/Listings -> Comp Tool integration: side-panel actions now open Tools with prefilled comp context (inventory/manual modes, query seeds, AI/listing notes)
  - Added channel capability matrix UI (eBay/Shopify/Whatnot/Facebook/Craigslist) to make current vs planned automation scope explicit
  - Comp Tool now accepts evidence screenshots and runs multimodal AI screenshot review against parsed comp rows for better human-auditable pricing context
  - Sync page now includes run lineage table (root/source run, retry chain depth, parent linkage, terminal lineage status) for faster retry-chain triage
  - Sync retry controls now enforce per-job `max_retries` policy from Runtime Settings and expose policy keys directly in Sync UI (`sync_job_<job_name>_max_retries`)
  - Added per-job retry policy model in sync catalog/runtime (`max_retries`, `retry_backoff_seconds`, `retryable_statuses`, `terminal_statuses`) and enforced these rules in Sync retry actions
  - Added sync dispatcher job metadata (`supports_execute_now`, `supports_retry_execute_now`, required args) and replaced hardcoded inline-retry job checks with metadata-driven compatibility logic
  - Added unified entity timeline controls (source/action/status/date-range filters) across row-action timelines
  - Added audit “change summary” diff cards (before/after field table) inside timeline detail for faster root-cause review
  - Added timeline jump action from sync-event rows directly to focused Sync run detail
  - Added timeline jump action from audit rows directly to Search & Edit for faster correction workflows
  - Added product-side repurchase/restock workflow for existing SKUs: each repurchase now records inventory movement + optional lot assignment + audited weighted cost/quantity update
  - Expanded product-side repurchase workflow to capture per-unit landed-cost components (tax/shipping/handling/product-cost), optional invoice/receipt attachment, and persisted purchase-document linkage directly from Products page
  - Added Purchase Lots `Lot P/L Snapshot (Estimated)` view that rolls linked assignments and proportional sales attribution into per-lot estimated gross/net/COGS/profit diagnostics
  - Added product lifecycle snapshot panel (acquired/sold/on-hand metrics + lot assignment + movement history) to track repeated buy/sell cycles over time
  - Started GS-V04-009 cycle reporting with new Reports export/view: `Inventory Cycles (Rebuy/Resell)` segments per-SKU zero-to-zero cycles and rolls up qty-in/out, sales, known acquisition cost, and estimated cycle margin
  - Added GS-V04-009 weighted rebuy cost trend analysis in Reports (SKU-selectable line chart + event export) combining lot assignments with deduped acquisition stock-in events
  - GS-V04-009 inventory cycle analytics now prefer repository DB rollup `report_inventory_cycle_rows` in Reports extended mode, reducing in-memory hydration cost with legacy helper fallback retained
  - Added GS-V04-009 `Inventory Cycle Summary by SKU` rollup export/view to surface cycle counts, open/closed mix, qty/sales totals, known-cost margin totals, and average closed-cycle duration
  - Completed GS-V04-009 with Product-side batch operator workflow for repeated SKU cycles (`Batch Receive` + `Batch Sale`) using shared batch reference linkage and optional listing association
  - Started GS-V04-008 with DB-backed eBay listing template profiles and Listings create-form template apply controls (including placeholder-aware title/details templates for repetitive branded listing workflows)
  - Completed GS-V04-008 HTML publish guardrails: Listings now previews sanitized HTML and blocks eBay publish/revise when disallowed markup remains
  - Added GS-V04-008 template usage analytics in Listings (template usage counts + recent listings linked to tracked template markers)
  - Added `products.inventory_class` (`sellable` / `raw_material` / `supply`) for cleaner stock classification and conversion workflows
  - Added inventory conversion flows:
    - single conversion (`conversion_out` + `conversion_in`)
    - bulk multi-target conversion (one source SKU into multiple target SKUs in one audited transaction)
  - Documents now supports `Listing` as a first-class source for local/off-platform invoice generation before a sale record exists
  - Added one-click listing invoice posting path:
    - optional linked `Order + OrderItem` creation
    - linked `Sale` creation
    - `Post & Open Sales` redirect to created sale row
  - Added listing outcome branch in Documents (`Sold` vs `Not Sold / Remove Listing`) so no sale is posted when unsold
  - Added media deletion controls (S3 object + DB row) in Media Library, Products media manager, and Listings media manager
  - Added explicit tax-treatment audit payload for listing-invoice posting events (mode, taxable subtotal, rate, jurisdiction, shipping taxability, exempt categories, exemption basis) for legal/accounting traceability
  - Added immutable document artifact retention:
    - new `document_artifacts` store (checksum + storage reference + retained payload)
    - listing-invoice posting now saves printable HTML artifact at post-time and links artifact lineage in sale audit events
    - Documents page now shows retained artifacts with download access for legal/accounting retrieval
  - Added Lots regression test hardening for create-state defaults/normalization/validation (`tests/test_lots_helpers.py`) to reduce Streamlit state regressions around eBay purchase lot capture
  - Added Products regression test hardening for eBay purchase validation in create/edit paths (`tests/test_products_helpers.py`) and factored validation helpers to keep UI behavior consistent
  - Added Lots create-state priming helper coverage for flash/reset state transitions to prevent post-create widget-state exceptions
  - Added Search/Edit Lots helper coverage for attached-products aggregation/table rows (`tests/test_search_edit_helpers.py`) to reduce regressions in lot-product traceability views
  - Added Comp Tool diagnostics helper coverage for legacy eBay Finding triage (`local_cooldown` vs `remote_rate_limited`) and clear-cooldown action wiring (`tests/test_tools_helpers.py`); panel later removed from active Comp Tool UI after HTML/web-first migration.
  - Added explicit view-render regression coverage for:
    - Lots create flash/reset state feedback rendering (`tests/test_lots_helpers.py`)
    - Comp Tool eBay diagnostics panel rendering + cooldown clear action (`tests/test_tools_helpers.py`)
  - Added explicit Products eBay field editability policy coverage (`tests/test_products_helpers.py`) and Search/Edit lot update mutation helper coverage (`tests/test_search_edit_helpers.py`) to lock regression-prone edit flows
  - Hardened eBay Finding remote-rate-limit handling: severe quota responses now trigger a longer local cooldown window with clearer operator messaging (quota exhaustion vs standard cooldown) to reduce noisy retries during throttle windows
  - Hardened wizard comp paths for eBay cooldown/rate-limit:
    - Inventory Intake and Coin Intake comp actions now degrade gracefully on `EBAY_FINDING_RATE_LIMITED` and continue with AI-only/web fallback context
    - Listing Wizard quick-comp context now records cooldown state and continues AI generation without hard-fail behavior
  - Added intake wizard e2e regression coverage for `Purchased On eBay` behavior in both Inventory and Coin flows (field enablement state + required purchase-item-id submit guardrail), reducing recurrence of eBay purchase field regressions.
  - Extended Listings e2e publish-path coverage to assert explicit `eBay Post Mode` switching between `Publish Live Listing` and `Save Unpublished Offer (API Draft)` before preflight, reducing mode-state regressions.
  - Extended Listings publish e2e flow with category-assist fetch/apply assertions (`Load eBay Category Assist` -> taxonomy suggestions -> apply selected category -> verify category ID state), with environment-aware skip fallback when eBay taxonomy fetch is unavailable.
  - Hardened Listing Wizard AI generation path: multimodal failures now degrade to text fallback without page crashes, and UI now surfaces `AI Runtime Diagnostics` (attempted mode, provider/model, source/endpoint, fallback/error chain) for fast operator debugging
  - Listing Wizard now persists AI draft metadata into listing details (`ai_draft.suggestions/diagnostics/acceptance`) so accepted AI edits are auditable per draft record
  - Listing Wizard AI prompts/personality are now runtime-configurable in Admin (`listing_wizard_ai_system_message`, `listing_wizard_ai_seed_default`, `listing_wizard_ai_instruction_template`) with dedicated save/reset controls
  - Listing Wizard now defaults `AI Seed Prompt` from runtime setting and supports no-dup media reuse by linking existing product media rows onto listing drafts (plus optional new uploads)
  - Listing Wizard AI draft now supports optional quick pricing context enrichment (eBay sold comps + spot quotes) using runtime keys `listing_wizard_ai_include_quick_comp_context` and `listing_wizard_ai_quick_comp_limit`
  - Listing Wizard AI review now includes explicit suggested price band handling (`suggested_price_low`/`suggested_price_high`) with robust fallback derivation for single-price outputs
  - Listing Wizard AI price-band parser now safely handles non-numeric model outputs (currency symbols/range text) without `Decimal` conversion crashes
  - Listing Wizard Step 7 now includes a consolidated `AI + Readiness Risk Summary` (risk level + readiness score + key highlights), and accepted risk snapshot is persisted to draft metadata (`ai_draft.risk_summary`) for auditable review
  - Listing Wizard existing-media preview was simplified to Streamlit-safe checkbox-driven inline preview (no nested expander constraint violations)
  - Listing Wizard added one-click product key-spec details helper (SKU/category/metal/weight) to accelerate listing copy completion while keeping the flow minimal
  - Listing Wizard now supports explicit quantity input for fixed/store modes, while auction modes remain quantity-locked to 1 for policy-safe draft defaults
  - Listing Wizard preview now supports rendered HTML view (plus raw source mode) so template/styled listing details can be reviewed before draft creation
  - Listing Wizard now supports optional direct single-listing eBay post from Step 9 (create local draft + immediate eBay publish in one operator path), reducing dependency on Listings batch workflow for one-off postings
  - Listing Wizard now supports per-user eBay post profiles (save/load/default/delete) for policy/location/category/currency/condition defaults, keeping direct post flows fast and repeatable
  - Listings and Listing Wizard now include simple percent-based volume-pricing builders (`Buy 2/3/4+ save %`) with one-click JSON tier generation plus advanced JSON override path
  - Added first-pass eBay fee economics assist in Listings + Listing Wizard:
    - upfront estimated-fee controls (buyer-paid shipping + promoted-rate assumptions),
    - gross/fees/net/fee% preview cards for pricing decisions,
    - persisted `fee_estimate` snapshot in `ebay_publish` metadata for downstream reconciliation/reporting.
  - Added first fee-reconciliation reporting slice:
    - `Reports -> eBay Fee Reconciliation` KPI card,
    - `eBay Fee Estimate vs Actual` detail export,
    - `eBay Fee Reconciliation Summary` export (estimate coverage + variance).
  - Added Reports `Fee Calibration Assist`:
    - computes implied final-value-rate from recent eBay sales using stored estimate components + actual fees,
    - shows current vs suggested final-value-rate and delta,
    - supports one-click runtime update for `ebay_fee_estimate_final_value_rate_percent` (permission-gated).
  - Improved fee reconciliation actual-fee fidelity:
    - reconciliation now prefers imported order `fee_breakdown_json.total_marketplace_fee` when available,
    - added `Actual Fee Source Breakdown` report/export for operational auditability of fee data provenance.
  - Added shipping-economics profitability visibility:
    - Reports now include `Shipping Economics Detail` and `Shipping Economics Summary` exports (shipping charged vs shipping label spend, with per-sale/per-marketplace deltas).
  - Documents/eBay invoice hardening:
    - customer-facing eBay invoices/receipts now suppress internal eBay fee rows while preserving optional marketplace-tax rendering.
    - eBay sync-note text shown in Documents is now normalized to buyer/shipping/ship-to summary (pricing blobs stripped from display copy).
  - Added local listing-state sell-through reconciliation from eBay order import:
    - linked listings are auto-marked `sold` only when cumulative imported sold quantity reaches local listed quantity.
    - multi-quantity listings remain active until final quantity sell-through; no premature closeout on partial fills.
  - Expanded Dashboard live business metrics:
    - order volume 7d/30d, sales gross/net 30d, estimated profit 30d, shipping charged vs label spend delta 30d, and shipped/not-shipped counts.
  - Hardened direct wizard eBay publish writeback to local listing state: active + approved sync is now updated atomically with eBay IDs/URL when publish succeeds
  - Added collision-safe direct-publish behavior when eBay listingId is already linked to another local row (new local row remains draft instead of linking wrong ID)
  - Added post-publish `eBay Sync Integrity Check` panel in Listing Wizard showing expected vs stored eBay ID/URL/status/review for immediate operator verification
  - eBay Templates workspace now supports editing existing templates (including active/inactive state) and custom reusable HTML block save/delete workflows
  - Admin Runtime Coverage now treats Listing Wizard AI prompt keys as required high-signal runtime keys for go-live readiness visibility and one-click remediation
  - Added proactive eBay OAuth user-token auto-refresh in sync runner (runtime keys: `ebay_user_token_auto_refresh_enabled`, `ebay_user_token_auto_refresh_interval_hours`, `ebay_user_token_auto_refresh_min_ttl_minutes`) to reduce expired-token outages during production operations.
  - Fixed eBay token expiry metadata persistence: access-token expiry now records actual `now + expires_in` and persists last-refreshed timestamp (`ebay_user_access_token_refreshed_at`) for accurate health/due checks.
  - Added `ebay_oauth / auto_refresh` integration-event telemetry for sync-runner token refresh success/failure visibility.
  - Added eBay OAuth refresh-failure cooldown guardrails (`ebay_user_token_auto_refresh_failure_cooldown_minutes`) with persisted failure/error state to prevent repeated bad-refresh loops during token/scope misconfiguration.
  - Added optional Slack alert routing for sync-runner eBay OAuth auto-refresh failures (`slack_notify_ebay_oauth_refresh_failures`) to improve operational awareness when refresh begins failing.
  - Added Admin eBay token diagnostics panel (next due, expiry window, last refresh failure, cooldown status) and one-click `Clear Refresh Failure State` action for fast recovery after credential/scope fixes.
  - Added System Health compact `eBay OAuth Auto-Refresh` card + integration row (status/next-due/expiry/cooldown/last-error) for at-a-glance runtime health in operations.
  - Extended System Health runtime seed-key coverage to include all eBay OAuth auto-refresh/failure state keys and OAuth-failure Slack alert key, reducing false runtime-drift noise.
  - Added System Health `Run Rollup EXPLAIN Snapshot` workflow (planning/execution timing + plan-text capture + CSV export) for dashboard/report rollup query evidence and tuning.
  - Added rollup EXPLAIN snapshot integration-event persistence (`system_health / rollup_explain_baseline_snapshot`) and a `Recent Rollup EXPLAIN Snapshots (14d)` panel with slowest-rollup summary metrics and history export.
  - Added repository DB rollup `report_ebay_marketplace_fee_rows` and switched Reports fee-detail pipeline to prefer DB-backed normalized finance entries before legacy order-payload parsing fallback.
  - Added repository DB rollup `report_marketplace_reconciliation_rows` and switched Reports reconciliation-by-marketplace pipeline to prefer SQL rollups (sales/orders/returns aggregates) before legacy in-memory fallback.
  - Added repository DB rollup `report_sales_actual_econ_rows` and switched Reports order-actual economics allocation pipeline to prefer SQL/window allocation before legacy Python fallback.
  - Added repository DB rollups `report_listing_review_activity_rows` and `report_listing_format_outcome_rows` with Reports fast-path adoption for extended analytics review/format outcome sections.
  - Added repository DB rollup `report_rebuy_cost_trend_rows` with Reports fast-path adoption for Rebuy Cost Trend analytics (reduced full-object hydration in extended analytics mode).
  - Added repository DB rollup `report_inventory_movement_rows` with Reports fast-path adoption for date-bounded Inventory Movements reporting (full movement-object hydration now fallback-only).
  - Added repository DB rollups `report_orders_rows` and `report_order_items_rows` with Reports fast-path adoption for date-bounded Orders/Order Items reporting (full OrderItem hydration now fallback-only).
  - Added repository DB rollup `report_returns_rows` with Reports fast-path adoption for date-bounded Returns + refund-adjustment reporting (full return-object hydration now fallback-only).
  - Added repository DB rollup `report_lot_assignment_rows` with Reports fast-path adoption for date-bounded Lot Assignment reporting (full assignment hydration now fallback-only for that surface).
  - Added repository DB rollup `report_sale_unit_cost_maps` with Reports fast-path adoption for FIFO-per-sale and lot-weighted-per-product cost mapping before helper fallback.
  - Added repository DB rollup `report_sales_rows` with Reports fast-path adoption for date-bounded Sales reporting; full Sale hydration is now fallback-only when rollup or compatibility prerequisites are unavailable.
  - Reports now reuses `report_orders_rows` as the primary report-window order context source (fee/shipping/reconciliation joins), making full `Order` hydration fallback-only in normal rollup paths.
  - Added repository DB rollups `report_products_rows` and `report_listings_rows` with Reports fast-path adoption for date-bounded Products/Listings reporting windows; full model hydration is now fallback-only for compatibility/extended fallback paths.
  - Tightened Reports fallback hydration strategy: full `InventoryMovement` / `ProductLotAssignment` lists are now lazy-loaded only when specific fallback branches require them (movement table fallback, cycle/rebuy helper fallback, cost-map helper fallback).
  - Extended lazy fallback hydration to full `Sale` objects as well, so normal rollup paths no longer eagerly preload full sales history.
  - Extended lazy fallback hydration to full `Product` / `MarketplaceListing` objects as well, so rollup-first runs only hydrate these models when helper fallback branches need them.
  - Extended lazy fallback hydration to full `Order` / `OrderItem` / `ReturnRecord` objects as well, so rollup-first runs avoid remaining upfront list preloads for these entities.
  - Optimized Reports shipping-summary coverage calculation to use grouped coverage rollups + merge (eliminating repeated full-dataframe scans in per-row apply).
  - Optimized Reports margin percentage derivations (SKU/channel/period) to vectorized calculations, removing repeated row-wise `apply` calls on grouped margin frames.
  - Consolidated Reports eBay fee-reconciliation KPI/source-priority derivations into cached summary helpers for reuse across cards/sections.
  - Optimized Reports Copilot context assembly for top-N anomaly slices by switching to `nlargest`/`nsmallest` helper selection paths instead of repeated full-frame sort pipelines.
  - Added Reports per-run scalar summary cache for repeated KPI/context totals (shipping/Cogs/reconciliation/row counts) to reduce duplicated sum/len scans in UI and Copilot paths.
  - Refactored Reports rollup-loading control flow behind a shared helper for consistent `report_*_rows` try/fallback semantics and lower view maintenance overhead.
  - Added Streamlit cache-data memoization to deterministic Reports fee-source transform helpers to improve repeated rerun latency under unchanged filters.
  - Added Streamlit cache-data memoization to deterministic Reports tax-drilldown filter/option transforms for faster repeated drilldown toggles.
  - Added Streamlit cache-data memoization to deterministic Reports document-handoff source option builders (sale/order) for faster repeated handoff control toggles.
  - Added Streamlit cache-data memoization for Tax Drilldown KPI scalar rollups to avoid repeated per-toggle aggregate scans.
  - Added repository DB rollup `report_returns_rows` with Reports fast-path adoption for date-bounded Returns and QBO refund-adjustment reporting (full Return object hydration now fallback-only).
  - Added repository DB rollup `report_lot_assignment_rows` with Reports fast-path adoption for date-bounded Lot Assignment reporting (full assignment-object hydration now fallback-only for that table).
  - Hardened Listings publish-form state mutation path: category-assist apply and item-specific default/aspect actions now preserve key publish fields (category/pricing/offer values) to prevent rerun clobber between category ID and aspects.
- v0.5 scoped:
  - Added AI-copilot workstream scope for domain-specific assistants across inventory, listings/eBay, sales/orders, shipping, sync, reports, and admin operations
  - Added in-app data chat workstream scope for secure conversational access to DB-backed business data with RBAC, audit logging, and guardrails
  - Started comp-tool embedding slice: Products/Listings now hand off context directly into Comp Tool with prefilled source/query/product metadata
  - Started Ask GoldenStackers chat slice: added read-only in-app chat page with repository-backed intent routing, common prompt shortcuts, transcript export, and source citations
  - Hardened Ask GoldenStackers chat with scoped memory by environment/user plus role-based domain allowlists (runtime-overridable `chat_allowed_domains_<role>_csv`)
  - Added chat guardrails: prompt-length limit, row-scan caps, soft timeout warnings, and explicit write-intent denial for read-only mode
  - Added AI chat audit trail in `audit_logs` (`entity_type=ai_chat`) with intent, scope, citations, denial flag, and elapsed time metadata
  - Added System Health page (`pages/22_System_Health.py`) with runtime metrics, service checks, integration status, and manual live checks (eBay/spot)
  - Expanded System Health with worker/queue diagnostics (queued/running/failed runs, unresolved sync errors, stale-running detection, and recent run table)
  - Added fallback LLM scope: multi-provider/model failover strategy (primary + backup profiles/providers) for resilient AI operations
  - Added Google Workspace integration scope (Gmail/Calendar/Drive) for invoice/send workflows, calendar scheduling, and document storage handoffs
  - Added Slack integration scope for operational notifications, alert routing, and daily summaries
  - Added voice interaction scope (speech-to-text + text-to-speech) for hands-free AI chat/copilot workflows
  - Added "Goldy" scope for in-app AI tool actions with approvals, role checks, and audit trail
  - Started GS-V05-008 first pass: Ask GoldenStackers now supports optional mic transcription + optional spoken response playback with Admin-configurable voice runtime settings and safe capability fallbacks
  - Expanded GS-V05-008 voice runtime for LocalAI/OpenAI-compatible audio endpoint variants (`/audio/*` and `/v1/audio/*`) to improve provider compatibility without redeploys
  - Completed GS-V05-008 audit extension: AI chat audit payload now records voice-origin prompt metadata and TTS playback generation events
  - Added next v1.0 scope `GS-V10-018 App Performance Optimization` focused on slow-page load reduction (query efficiency, render decomposition, caching/lazy hydration, and baseline observability).
  - Started GS-V10-018 step 1: added System Health page/read baseline capture (manual runner + CSV export), repository probe collector, and integration-event snapshot logging for performance traceability.
  - Advanced GS-V10-018 step 2: System Health baseline now includes probe-budget checks, over-budget counters, and a slowest-probe summary for faster hotspot triage.
  - Advanced GS-V10-018 step 3: added cross-session baseline trend visibility via `Recent Page/Read Baseline Snapshots (14d)` in System Health with snapshot CSV export.
  - Started GS-V10-018 first read-path optimization: Listings side-panel document draft flow now uses listing-scoped repository reads instead of full sales/order/order-item table hydration.
  - Continued GS-V10-018 Listings optimization: removed duplicate product/listing fetch passes in bulk draft-create controls by reusing the page’s preloaded datasets.
  - Continued GS-V10-018 Reports optimization: consolidated base-data hydration to single-load/reuse across report slices (products/listings/sales/orders/order-items/returns/assignments/movements) instead of repeated repository fetches.
  - Continued GS-V10-018 Reports render optimization: added explicit `Load Extended Analytics` gate so heavy fee-reconciliation/cycle-trend computations are deferred until operator-requested.
  - Continued GS-V10-018 baseline accuracy hardening: System Health page/read baseline now treats eBay fee reconciliation as extended-only probe (`report_ebay_fee_reconciliation_rows_extended`) when heavy mode is enabled, matching Reports first-load behavior.
  - Continued GS-V10-018 Reports UI rendering optimization: report tables now default to bounded previews with explicit full-render opt-in, reducing heavy DataFrame rendering overhead on initial page load.
  - Continued GS-V10-018 Reports analytics optimization: shipping economics and tax drilldown dataset computation is now explicit opt-in (`Load Shipping + Tax Analytics (slower)`), reducing default report-load query/aggregation cost.
  - Continued GS-V10-018 System Health optimization: critical-alert evidence + calibration/alert-routing sign-off history queries are now deferred behind explicit `Load Critical Alert / Sign-Off History` toggle to reduce default tab load time.
  - Continued GS-V10-018 Admin optimization: Go-Live evidence ZIP generation is now explicit on-demand (`Prepare Go-Live Evidence Pack`) and cached for download, avoiding repeated bundle rebuild cost on normal reruns.
  - Continued GS-V10-018 Admin workflow-state optimization: event-history retrieval is now opt-in (`Load Workflow Events`) to avoid multi-query event hydration on default governance load.
  - Continued GS-V10-018 Admin governance export optimization: handoff/feedback/parity/photo-comp export reads are now opt-in (`Load Governance Event Exports`) to avoid default audit-log hydration cost.
  - Continued GS-V10-018 Search/Edit optimization: added per-run lazy dataset loader reuse across tabs (`products`, `listings`, `sales`, `orders`, `order_items`, `lots`, `sources`, `lot assignments`) to avoid duplicate repository reads in a single rerun.
  - Continued GS-V10-018 Search/Edit render optimization: `Audit Log` query/render is now deferred behind explicit `Load Audit History (slower)` toggle to reduce default first-load cost.
  - Continued GS-V10-018 Orders render optimization: full order-items table hydration/render is now deferred behind explicit `Load Order Items Table (slower)` toggle.
  - Continued GS-V10-018 Orders stability/perf hardening: removed direct session-state mutation for keyed multiselect filters and switched to normalized widget defaults to prevent rerun warnings and filter churn.
  - Continued GS-V10-018 Intake Wizard optimization: existing-media attach selectors now support deferred option loading (opt-in) to avoid default hydration of large media lists on first load.
  - Continued GS-V10-018 Coin Intake optimization: coin-reference catalog hydration (up to 5k rows) is now explicit opt-in (`Load Coin Reference Catalog (slower)`) for faster default render.
  - Continued GS-V10-018 Intake submit-path optimization: consolidated existing-media attach lookups to a single media-map hydration per submit flow to remove duplicate `list_media_assets()` scans.
  - Continued GS-V10-018 Intake render optimization: AI diagnostics panels are now explicit opt-in in both intake wizards, reducing default rerun/render overhead from raw payload and structured-debug blocks.
  - Continued GS-V10-018 Purchase-document preview optimization: detailed extract rendering (AI summary, normalized key table, full JSON) is now opt-in behind `Load Detailed Purchase Extract View (slower)`.
  - Continued GS-V10-018 Reports render optimization: bounded preview/full-render controls now also apply to earlier analytics panels (fee source breakdowns/trends, shipping economics summary, tax drilldown table), not just the final export-table block.
  - Continued GS-V10-018 Admin System-Health optimization: navigation telemetry and workspace-feedback analytics are now explicit opt-in loads (`Load Navigation Telemetry (slower)`, `Load Workspace Feedback Insights (slower)`) to avoid default heavy audit-log hydration.
  - Continued GS-V10-018 Admin Integrations optimization: large integration-event history sections are now explicit opt-in loads (`Load Shipping Validation Events`, `Load Shipping Adapter Events`, `Load Automation Engine Events`, `Load Slack Delivery Events`) so default Integrations tab render avoids heavy audit-log hydration.
  - Continued GS-V10-018 Admin Integrations optimization: Slack/Google retry-queue table hydration is now explicit opt-in (`Load Slack Queue Jobs`, `Load Google Queue Jobs`) so default Integrations tab render skips large queue-table reads unless requested.
  - Continued GS-V10-018 Admin Integrations query-efficiency optimization: added per-rerun integration-event query cache reuse across validation/shipping-adapter/automation/Slack sections to eliminate duplicate `integration_event` reads when multiple event panels are loaded in one rerun.
  - Continued GS-V10-018 Admin Integrations optimization: validation and automation hardening sign-off history tables are now explicit opt-in (`Load Integrations Sign-Off History`) to avoid default audit-log reads on tab load.
  - Continued GS-V10-018 Admin eBay verification optimization: recent `ebay_verify` feedback history is now explicit opt-in (`Load Recent Verification Feedback (slower)`) so default eBay verification tab load skips audit-history scans.
  - Continued GS-V10-018 Admin governance optimization: Documents-Handoff preset/clear audit summaries are now explicit opt-in (`Load Preset Audit Summary (slower)`, `Load Clear Audit Summary (slower)`) so default governance view avoids additional audit-log scans.
  - Continued GS-V10-018 Admin governance-export optimization: recent governance snapshot history is now explicit opt-in (`Load Governance Snapshot History (slower)`) so default governance export load skips snapshot-history queries.
  - Continued GS-V10-018 observability coverage: System Health page/read baseline now includes optional Admin Integrations probes (`Include integration probes`) for Slack/Google queue reads and integration-event history timing, with runtime-tunable probe budgets (`perf_budget_<probe_name>_ms`).
  - Continued GS-V10-018 baseline efficiency hardening: consolidated duplicate 14-day integration-event baseline reads into one shared probe (`integration_event_rows_shared_14d`) so baseline runs reflect real query cost without duplicate sampling overhead.
  - Continued GS-V10-018 Search/Edit render optimization: added page-level preview-first table controls (`Render Full Tables`, `Preview Row Limit`) and applied bounded table rendering across Products/Listings/Sales/Lots/Media/Audit tables to reduce first-load UI payload cost.
  - Continued GS-V10-018 Listings render optimization: added page-level preview-first controls (`Render Full Listings Tables`, `Listings Preview Row Limit`) and applied bounded rendering to the largest listings operational tables (main listing table, eBay readiness queue, orchestration queue, publish plan/result, bulk publish history).
  - Continued GS-V10-018 Listings hydration optimization: bulk publish execution history metadata scan is now explicit opt-in (`Load Bulk Publish History (slower)`), preventing default full metadata-history traversal on initial load.
  - Continued GS-V10-018 Listings read-path optimization: eBay readiness queue generation now avoids per-row product DB fetches by reusing preloaded product maps, precomputes runtime/preset defaults once per rerun, and uses keyed readiness maps for pending-age/batch-plan lookups (removes repeated O(n) scans from large readiness sets).
  - Continued GS-V10-018 Listings readiness-panel optimization: recent blocker follow-up task history scan (`workspace_followup` audit events) is now explicit opt-in (`Load Recent Blocker Follow-up Tasks (slower)`) to avoid default heavy audit-log hydration.
  - Continued GS-V10-018 Listings metadata optimization: added per-rerun `marketplace_details` JSON parse cache and shared bulk-publish default resolver so tag/retry/history flows avoid duplicate JSON decode and repeated runtime-setting fetches.
  - Continued GS-V10-018 Listings audit-read optimization: added per-rerun shared audit-log cache (`_get_audit_logs` / `_audit_logs_for_entity`) so photo-comp lineage tagging and blocker-followup history reuse one audit fetch instead of issuing duplicate `list_audit_logs` reads.
  - Continued GS-V10-018 Listings product-read optimization: batch publish/retry and side-panel/publish-selected flows now reuse the page’s preloaded `product_by_id` map instead of issuing direct per-listing `repo.db.get(Product, ...)` lookups.
  - Continued GS-V10-018 Listings side-panel optimization: document-draft related-source queries are now explicit opt-in (`Load Related Sales/Orders (slower)`), deferring `list_sales_for_listing`/`list_order_items_for_listing`/`list_orders_by_ids` from default panel load.
  - Continued GS-V10-018 Listings template-analytics optimization: template usage scan/aggregation is now explicit opt-in (`Load Template Usage (slower)`) so default Listings load skips listing-wide template-tracking passes.
  - Continued GS-V10-018 Listings capability-matrix optimization: channel capability matrix build/render is now explicit opt-in (`Load Channel Capability Matrix (slower)`) to reduce default render payload.
  - Continued GS-V10-018 Listings origin-tag optimization: photo-comp draft origin labeling is now explicit opt-in (`Load Photo-Comp Origin Tags (slower)`), deferring default 5k audit-log scan from base Listings load.
  - Continued GS-V10-018 Listings coin-context optimization: linked coin-reference catalog hydration is now explicit opt-in (`Load Coin Reference Catalog (slower)`), avoiding default 5k coin-reference row load on Listings render.
  - Continued GS-V10-018 Listings media-hydration optimization: main listing table media counts are now explicit opt-in (`Load Listing Media Counts (slower)`), deferring default per-listing media relationship hydration.
  - Continued GS-V10-018 Listings selected-media optimization: selected listing media rows now reuse one per-rerun cache across media-manager hydration, publish workspace image/video selectors, and batch publish image gathering; count-only side-panel views keep scalar count reads unless media rows are already cached, and publish byte reads use the shared cached media loader.
  - Continued GS-V10-018 private S3 preview hardening: shared media galleries/file actions and Listing Wizard selected-media previews now render private S3 assets through backend-loaded bytes instead of raw S3 browser URLs, with direct URL fallback limited to non-S3 public-looking URLs.
  - Continued GS-V10-018 Listings publish-workspace cache deferral: cached eBay category item-specific reads no longer run just because the workspace opened or category state changed; they run when item-specific controls or preflight/publish validation need them, and dependency preflight now includes selected condition consistently.
  - Continued GS-V10-018 Listing Wizard category-cache deferral: cached eBay category item-specific reads no longer run just because wizard category state changed; they run when item-specific controls or direct eBay preflight/post validation need them.
  - Continued GS-V10-018 Listing Wizard item-specific preview deferral: quick suggested item-specific defaults preview is now opt-in, avoiding duplicate default-merge/table rendering on ordinary title/details edits.
  - Continued GS-V10-018 Listings preset-read optimization: repeated eBay publish preset lookups now reuse a per-rerun cache (`_list_ebay_publish_presets_cached`) across create/readiness/manage sections, removing duplicate preset queries in one render cycle.
  - Continued GS-V10-018 Listings external-ID collision optimization: external listing owner checks now reuse preloaded in-memory listings and compute collision once per publish/update path, removing repeated `list_listings()` scans during eBay update/relist flows.
  - Continued GS-V10-018 Listings external-ID lookup optimization: added precomputed `(marketplace, external_listing_id) -> listing_id` owner map reuse in publish/relist flows for O(1) collision checks, avoiding repeated linear scans during update-path decisioning.
  - Continued GS-V10-018 Listings allocation optimization: external-ID owner helper now reuses provided listings sequence directly (no defensive list copy) when preloaded rows are available, reducing avoidable memory churn on publish paths.
  - Continued GS-V10-018 Listings bulk-create optimization: bulk draft-create path now reuses the existing page-level `product_by_id` index instead of rebuilding a second `{id -> product}` map from `products`.
  - Continued GS-V10-018 Listings bulk-filter optimization: candidate product filtering now precomputes selected category set once per rerun instead of rebuilding set conversion on every product iteration.
  - Continued GS-V10-018 Listings filter-state hardening: pending saved-filter apply now sanitizes marketplace/status values against live options before writing widget session-state, preventing `default value not part of options` crashes on status multiselects.
  - Continued GS-V10-018 Orders filter-state hardening: removed keyed multiselect `default=` usage in favor of pre-sanitized session-state assignment to eliminate Streamlit default-vs-session-state warnings.
  - Continued GS-V10-018 Reports bounded-render optimization: introduced shared `_bounded_dataframe(...)` helper and routed report table rendering (including Rebuy Cost Trend SKU details and report export table loop) through preview-bounded logic for lighter default payloads.
  - Continued GS-V10-018 performance guardrail coverage: added helper-level tests for Reports bounded rendering behavior (`tests/test_reports_helpers.py`) to lock preview/full-table semantics.
  - Continued GS-V10-018 Media payload optimization: Media Library now uses preview-first table rendering (`Render full media table` toggle + configurable preview row limit) so large media catalogs no longer force full default table payloads.
  - Continued GS-V10-018 Listings guardrail coverage: added helper tests for photo-comp draft lineage ID extraction and map-based external listing owner lookup behavior (`tests/test_listings_helpers.py`) to protect optimized audit/owner lookup paths.
  - Continued GS-V10-018 System Health guardrail coverage: extracted page/read baseline row normalization + summary helpers and added unit coverage (`tests/test_system_health_helpers.py`) to lock budget/over-budget metric semantics.
  - Continued GS-V10-018 operational evidence runbook hardening: documented explicit performance baseline capture/export workflow in go-live and QA plans (rollup baseline, page/read baseline default + heavy probes, focused helper guardrail suite).
  - GS-V10-018 closeout recorded (April 18, 2026): first-wave performance remediation completed with baseline capture instrumentation, bounded/deferred rendering controls across heavy workflows, and targeted helper guardrail coverage for optimized paths.
  - Continued GS-V10-018 Admin parity-checker optimization: parity audit/task evidence scan is now explicit opt-in (`Load Parity Audit Evidence (slower)`), deferring the 5k-row audit-log pull and follow-up snapshot scan from default Admin parity tab load.
  - Continued GS-V10-018 Admin parity export optimization: Parity Governance ZIP generation is now on-demand (`Prepare Parity Governance Bundle`) and session-cached for download, avoiding repeated bundle rebuild cost on reruns.
  - Continued GS-V10-018 Admin parity-history optimization: parity snapshots, release-decision history, and follow-up task table queries are now explicit opt-in (`Load Parity History + Follow-up Tables (slower)`) so default parity view skips additional audit-history hydration.
  - Continued GS-V10-018 Admin Go-Live evidence optimization: heavy diagnostics scans (integration-event alert/validation windows, queue snapshot, recent evidence-capture history) are now explicit opt-in (`Load Go-Live Diagnostics Tables (slower)`), reducing default Admin go-live rerun query load.
  - Continued GS-V10-018 Admin Go-Live history-depth optimization: sign-off/history tables now support default recent-window mode with explicit full-depth opt-in (`Load Full Go-Live Sign-Off History (slowest)`), reducing large audit-history pulls on default load while retaining deep-history access.
  - Continued GS-V10-018 Admin Go-Live query-efficiency optimization: consolidated duplicate integration-event SQL reads (7d, 24h, 30d windows) into one shared 30-day query with in-memory window filtering for alert evidence and signal summaries.
  - Continued GS-V10-018 Admin Go-Live checklist optimization: DR checklist table hydration now decodes audit payload once per row (instead of repeated `_audit_changes(...)` calls), reducing CPU overhead on large checklist history sets.
  - Continued GS-V10-018 Admin parity history optimization: release-decision table hydration now decodes audit payload once per row, removing repeated `_audit_changes(...)` calls in parity decision rendering.
  - Continued GS-V10-018 Admin parity decision optimization: parity status cards and latest-approved scan now reuse a predecoded decision payload map (`decision_payload_by_id`) instead of re-decoding payloads across latest/summary/table paths.
  - Continued GS-V10-018 Admin parity follow-up optimization: follow-up task rows now reuse cached create-event payloads (`created_payload_by_key`) instead of re-decoding each create row during follow-up table hydration.
  - Continued GS-V10-018 Admin parity precheck optimization: follow-up open/overdue counters now also reuse cached create-event payloads in the readiness pre-score path, removing a second decode pass during parity metric calculation.
  - Continued GS-V10-018 Admin governance telemetry optimization: workspace-handoff event table mapping now decodes audit payload once per row (instead of repeated `_audit_changes(...)` calls) for lower CPU overhead in handoff telemetry views.
  - Continued GS-V10-018 Admin navigation telemetry optimization: handoff-event table now reuses payloads cached during primary telemetry iteration (`nav_payload_by_id`) instead of running a second decode pass over the same audit rows.
  - Stability hardening: fixed a Listings model-type annotation regression that caused shared views import-chain failures (`NameError`/`ImportError`) and validated runtime container importability across all `app.components.views.*` modules.
  - Added next v1.0 scope `GS-V10-019 Slack AI Ops Bot` to let approved operators submit images/commands in Slack for inventory intake/comps and trigger constrained in-app operations with approvals + audit trail.
  - Enhanced AI Runtime Admin UX with endpoint-backed `/models` querying plus manual override fields for text and multimodal model selection
  - Started GS-V05-005 AI Admin controls: added per-domain AI feature toggles in Admin (`chat`, `comp_tool`, `coin_grader`, `coin_identifier`) with runtime enforcement in Ask GoldenStackers and Tools actions
  - Expanded GS-V05-005 role policy layer: added dedicated AI permissions (`ai_chat_use`, `ai_comp_use`, `ai_coin_grade`, `ai_coin_identify`) and enforced them in Ask/Comp/Grader/Identifier execution paths
  - Added prompt template versioning/rollback controls in Admin AI Runtime by reading runtime-setting audit history and allowing key-level rollback to selected before/after values
  - Completed GS-V05-005 usage telemetry dashboard in Admin AI Runtime (chat latency, denial/failure counts, top intents/users, voice-provider usage, coin AI tool usage, configured provider/model profile view)
  - Started GS-V05-001 provider failover: Comp Tool and coin multimodal actions now attempt fallback across active AI runtime profiles (primary + backups), with runtime controls for enable/max profiles and per-run profile/fallback-attempt visibility
  - Added GS-V05-001 response citation payloads for Tools AI outputs (comp summary/review, coin grader, coin identifier) with provider/model/fallback lineage and persisted citation metadata in stored coin AI run records
  - Added GS-V05-001 shared AI orchestration layer (`app/services/ai_orchestration.py`) and migrated Tools comp/grader/identifier execution paths to use it for consistent fallback + citation behavior
  - Added GS-V05-003 optional Ask GoldenStackers AI refinement mode (`chat_ai_refine_enabled`) that routes final chat answers through shared orchestration with fallback lineage citation while keeping chat read-only
  - Added Admin AI Runtime controls for Ask refinement templates/toggle (`chat_ai_refine_enabled`, `chat_ai_refine_system_message`, `chat_ai_refine_instruction`) plus versioning support keys
  - Added Ask chat UX refinement controls: per-session refine toggle and explicit `AI refined` badge on assistant responses when refinement path is used
  - Extended AI chat audit metadata to capture refinement lineage (provider/model/endpoint/fallback attempts) for telemetry and governance reporting
  - Expanded Admin AI telemetry to show refinement adoption and refinement provider/model/endpoint usage breakdown
  - Added runtime-policy sensitive-data masking layer for Ask GoldenStackers responses (`chat_mask_sensitive_enabled` + email/phone/tracking toggles), applied before display and audit logging
  - Started GS-V05-002 embedded domain copilots with Products-page Product Copilot assistant (AI suggestions for title/category/description/metal/weight applied directly to create-form defaults)
  - Extended GS-V05-002 with Listings/eBay Copilot in Listings create flow (AI draft title/details/price suggestions plus readiness/risk preview and publish checklist)
  - Extended GS-V05-002 with Sales/Orders side-panel copilots for exception triage, mismatch explanations, and refund/return guidance from selected records
  - Extended GS-V05-002 with Shipping Copilot queue triage (prioritized queue plan, exception-resolution guidance, and recommended bulk actions from live shipping queues)
  - Extended GS-V05-002 with Sync Copilot (failure clustering by job/error, retry recommendations, and lineage-aware root-cause next actions)
  - Completed GS-V05-002 with Reports Copilot (margin anomaly narrative, reconciliation findings, recommended exports, and action plan from report aggregates)
  - Started GS-V05-001 domain context builder extraction: added reusable chat context builder service for inventory/listings/sales/shipping/sync/orders snapshots and routed Ask GoldenStackers intent handlers through it
  - Completed GS-V05-001 domain context coverage: Ask GoldenStackers now includes role-gated `reports` and `admin` snapshots via shared context builders and removed duplicated snapshot logic from chat view code
  - Started GS-V05-007 foundation: added Admin `Integrations` tab with environment-scoped Google runtime settings (OAuth credentials/scopes/default sender/Drive root)
  - Extended GS-V05-007 with first Gmail workflow: Documents page can now send invoice/receipt HTML via Gmail API using Admin-managed runtime credentials, with integration audit events for success/failure
  - Extended GS-V05-007 with Calendar workflow: Documents page can now create follow-up reminder events in Google Calendar with runtime-default calendar/timezone and audited outcomes
  - Extended GS-V05-007 with Drive workflow: Documents page can now upload generated document artifacts (HTML/CSV/XLSX) to Google Drive with runtime folder defaults and returned Drive links
  - Completed GS-V05-007 reliability layer: failed Google Gmail/Calendar/Drive actions now auto-enqueue into DB-backed retry jobs with exponential backoff, plus Admin `Google Retry Queue` controls for due-run execution and manual failed-job retry
  - Started GS-V05-010 foundation: added environment-scoped Slack runtime settings in Admin (token/channel/toggle/schedule) for upcoming delivery service and alert routing
  - Extended GS-V05-010 with first delivery layer: added Slack notifier service, Admin test-send action, sync run failed/partial alerts, and terminal Google retry-queue failure alerts
  - Extended GS-V05-010 observability: added System Health Slack connectivity checks/status plus Admin recent Slack delivery-event history for faster ops triage
  - Extended GS-V05-010 delivery resilience: Slack alerts now use routed dispatch with optional channel overrides by event/severity/env and queue fallback/backoff with Admin Slack queue controls
  - Completed GS-V05-010 polish: added runtime-editable Slack alert templates and one-click environment channel preset seeding in Admin Integrations
  - Added Admin config-coverage dashboards for `.env` + runtime settings to track missing/empty/default/overridden keys and feature-flag-like toggles
  - Expanded config-health governance: one-click required + bulk default repairs, drift detection for untracked env/runtime keys, top-level Admin health summary + JSON snapshot export, and shared config-health policy service reused by Admin/System Health
  - Moved System Health access under Admin tab set (instead of dedicated sidebar page) to reduce sidebar length and centralize operational controls
  - Added auth session persistence hardening scope: replace deprecated `streamlit_cookies_manager` (`@st.cache`) dependency with a non-deprecated cookie/session adapter while preserving remember-token behavior and OAuth callback compatibility

## Production Go-Live Re-Evaluation (April 2026)
Target: get legally and operationally ready for production selling with this system as the system of record.

Priority outcomes:
- legal/compliance controls explicitly mapped to product behavior
- reliable transaction posting (listing/order/item/sale/invoice consistency)
- auditable tax + shipping + landed-cost records
- evidence-backed release gates for Dev -> Prod promotion

Critical readiness tracks:
- Commerce legality + policy:
  - marketplace policy conformance (eBay listing/payment/returns/shipping requirements)
  - local/off-platform sale documentation standards (invoice/receipt retention)
  - terms/privacy/record-retention controls for customer and transaction data
- Tax + accounting correctness:
  - jurisdiction-specific tax handling defaults and overrides (including bullion/coin exemptions where applicable)
  - explicit auditability for tax treatment decisions per document/sale
  - consistent order/item/sale/invoice linkage for reconciliation
- Operational safety:
  - production-only guardrails for destructive actions (bulk listing end/remove/media delete)
  - stronger role separation for review/publish/financial posting
  - backup/restore and incident rollback drills on current production-like data
- Go-live evidence:
  - signed owner/date/evidence links for each checklist section
  - exportable evidence pack attached to release candidate

## v0.2 (Operations Foundation)
Target: stabilize daily inventory + fulfillment workflows

Scope:
- Inventory ledger (immutable quantity movements)
- Source master data (dealers/vendors/acquisition channels)
- Orders domain (order header + line items)
- Returns/refunds workflow
- Shipping queue improvements (bulk carrier/status operations, shipment exports)
- Data quality rules (required fields, duplicate guards, tracking format checks)
- Role-based access basics (admin/ops/viewer) with user-level audit attribution
- Invoice/receipt document generation with branding templates and print-ready output

Deliverables:
- New tables: `inventory_movements`, `orders`, `order_items`, `returns`
- UI pages: `Sources`, `Orders`, `Returns`, improved `Shipping`
- Document template engine and print/export flows for invoices/receipts
- Validation layer for creates/updates
- Access guardrails by role

Acceptance Criteria:
- Every quantity change is traceable to a movement event
- Purchase lots can reference standardized source records
- Orders can represent multi-item sales
- Returns update inventory + financial fields consistently
- Shipping exceptions are visible and actionable in UI
- Audit log includes real user identity on updates
- Invoices/receipts can be generated from orders/sales using branded templates and printed/exported

## v0.3 (Channel Sync + Accounting Readiness)
Target: remove manual re-entry between marketplaces and bookkeeping

Scope:
- eBay sync jobs (listings/orders/tracking pull; status push where possible)
- Sync queue with retries and failure dashboard
- QuickBooks-ready exports (sales, fees, shipping, refunds, COGS inputs)
- Cost basis engine (FIFO and lot-specific methods)
- Margin analytics per SKU / channel / period
- Media visibility in Products/Listings and single-action “create eBay listing from product” workflow
- eBay seller operations console (publish/revise/end/relist + operational dashboards)
- Environment-aware eBay operational guardrails for sandbox vs production execution

Deliverables:
- Background worker + scheduled sync jobs
- Sync log tables: `sync_runs`, `sync_events`, `sync_errors`
- Reconciliation reports for channel totals vs internal totals
- Cost basis + profitability report views
- Product/listing media gallery components and eBay listing composer from product records
- eBay publish/revision operational controls and preset-driven listing workflows
- eBay live listing status dashboards and operational control center workflows
- Sandbox-safe UX behavior and explicit override path for seller operations during testing

Acceptance Criteria:
- eBay order/listing sync completes on schedule with retry behavior
- Sync failures are visible with actionable error messages
- Bookkeeping exports can be produced without manual spreadsheet shaping
- Gross/net margin reports match sampled transactions
- Users can preview product/listing media in-app and generate an eBay listing draft from product + media selections
- Listing create/publish actions block invalid active listings (missing media or missing external listing reference URL/ID)
- eBay operators can monitor live offer/listing status from eBay API directly in app and run core lifecycle actions without switching tools
- Sandbox mode surfaces clear operational limits while keeping non-seller validation workflows available

## v0.4 (UX Consolidation + Central Operations Hub)
Target: make GoldenStackers the single daily workspace for inventory, marketplace, fulfillment, and bookkeeping operations.
Execution tracking: `V0_4_IMPLEMENTATION_CHECKLIST.md`

Scope:
- UI cleanup and workflow consolidation (fewer clicks, clearer navigation, role-aware dashboards)
- Unified command center pages (Today queue, Exceptions, Approvals, Reconciliation)
- Global search + edit experience across products/listings/orders/sales/media with consistent inline actions
- Cross-channel listing orchestration (publish templates, channel readiness checks, bulk publish)
- Integration abstraction for marketplaces/accounting/shipping (provider adapters, queue dispatch, shared retry model)
- Operational traceability hardening (entity timeline views, audit diffs, sync lineage across systems)
- eBay listing template system (saved listing-body templates, branded HTML blocks, policy/category presets, repeat-listing acceleration)
- Repeatable inventory lifecycle model for re-buys/re-sells of the same SKU across multiple lots and pricing periods

Deliverables:
- Consolidated IA/navigation and shared component patterns for forms/tables/filters/actions
- “Operations Home” dashboard with SLA widgets (pending publish, unshipped, exceptions, failed syncs, margin anomalies)
- Global search/edit surface with saved views and role-based shortcuts
- Channel orchestration layer for eBay-first publish/sync flows, ready for Shopify/Whatnot/Facebook expansion
- End-to-end activity timeline per SKU/order/listing (local events + external sync events)
- eBay listing template profiles (including reusable branded HTML description sections) with one-click application during create/publish
- SKU lifecycle ledger views showing repeated acquisition/sale cycles with lot-level and movement-level traceability

Acceptance Criteria:
- Core daily workflows (receive, list, sell, ship, reconcile) can be completed without switching external admin tools for normal cases
- Teams can triage all exceptions from one queue-driven UI
- Data edits are fast, auditable, and consistent across domains
- New marketplace/accounting connectors can be added without redesigning app workflows
- Repetitive eBay listings can be created from saved templates (including branded HTML body content) with minimal manual re-entry
- Same SKU can be repurchased and sold repeatedly with clear period-over-period quantity/cost traceability

## v0.5 (AI Operations Copilot + Data Chat)
Target: embed AI-assisted workflows across every major service domain and provide a secure chat interface for operational insights from app data.
Execution tracking: `V0_5_IMPLEMENTATION_CHECKLIST.md`

Scope:
- Domain copilots for Inventory, Coin Catalog, Products, Listings/eBay Ops, Sales/Orders, Shipping, Sync, and Reports
- Action-oriented AI assistance (draft suggestions, anomaly detection, policy checks, workflow shortcuts) with human approval gates
- In-app “Ask GoldenStackers” chat interface with role-aware access to business data
- Fallback LLM routing across providers/models (OpenAI/LocalAI/etc.) with health-based failover controls
- DB-aware retrieval/query orchestration (read-focused by default; write actions gated and explicit)
- AI safety controls (prompt/version tracking, citations/row references, confidence labels, hard deny for restricted data)
- Admin governance for AI runtime profiles, tool permissions, and usage controls by environment/role
- Google Workspace integration layer for Gmail/Calendar/Drive operational workflows (invoice send, follow-up scheduling, document handoff)
- Voice interaction layer (speech-to-text input + text-to-speech output) for chat and copilots
- "Goldy" action framework to expose approved app tools for AI-assisted operational execution

Deliverables:
- Shared AI orchestration service layer with domain tool adapters and context builders
- Per-domain copilot panels embedded in existing pages (not separate disconnected tools)
- Unified chat page with conversation history, source citations, and exportable transcripts
- Query safety layer for DB chat (RBAC filtering, table allowlists, row-limit defaults, timeout/cost controls)
- AI interaction audit model (who asked what, data scopes touched, actions proposed/executed)
- Voice UI controls for chat/copilot surfaces (mic capture/transcription + optional spoken responses)
- Goldy tool registry and execution ledger for approved actions (inventory/listing/sync/report primitives)

Acceptance Criteria:
- Operators can complete common workflows faster using AI suggestions without bypassing required review/approval controls
- Chat answers include traceable source context (table/row/time window references) and respect role permissions
- Sensitive fields remain protected in chat and copilot responses according to role/environment policies
- AI-generated write actions are never auto-executed without explicit user confirmation and audit trace
- AI runtime/provider can be switched by admins without app redeploy
- Voice interactions follow the same RBAC, guardrails, and audit requirements as typed prompts
- Goldy action execution requires explicit approval, logs outcome/status, and never bypasses existing app permissions

## v0.6 (UX Consolidation + Workflow Unification)
Target: reduce page sprawl and make high-frequency operations flow through fewer, clearer, role-aware surfaces.
Execution tracking: `V0_6_IMPLEMENTATION_CHECKLIST.md`
Detailed planning artifact: `UX_CONSOLIDATION_PLAN.md`

Scope:
- Merge overlapping pages into domain workspaces (start with eBay + eBay Ops)
- Standardize workspace layout (queues, filters, actions, detail panel, timeline, audit)
- Consolidate service operations pages using the same interaction model (Listings/Sync/Shipping/Media/Orders)
- Build a role-aware navigation model that groups pages by workflow stage (Intake, Listing, Fulfillment, Reconcile, Admin)
- Add cross-workspace saved views and pinned operator presets
- Preserve backward compatibility with existing routes while introducing unified pages
- Add a generalized `Inventory Intake Wizard` for non-coin intake (bullion, collectibles, antiques, normal goods) with optional AI assist and listing handoff
- Add image-first intake and general photo-based comps scope (non-coin): start from camera/uploaded image and generate reusable comp/intake suggestions for any inventory type
- Add agentic workflow scope (`Goldy`) with single-agent and multi-agent modes for comps, listings, inventory, integrations, and finance support tasks
- Expand branding templates across invoices/receipts and marketplace listings with Golden Stackers LLC identity defaults and reusable channel-specific HTML sections
- Add tax-aware invoicing scope with jurisdiction/rule profiles (including Colorado bullion/coin exemption handling), with clear operator review controls
- Add eBay Store operations scope (store category mappings, storefront metadata, and store-policy defaults) plus explicit listing-format workflows (Auction vs Buy It Now, duration, pricing rules, and review gates)
- Add multi-environment delivery scope (versioned Docker release pipeline, Dev->Prod promotion gates, Kubernetes runtime parity checks, and ArgoCD GitOps handoff in separate infra repo)

Deliverables:
- Unified `eBay Workspace` replacing split `eBay` and `eBay Ops` daily operations flows
- Shared workspace shell component for table + filters + quick actions + side-panel details
- Migration plan for other services into consolidated workspaces (Shipping Ops, Sync Ops, Inventory Ops, Documents/Reports)
- Legacy-page redirect/deprecation notices with rollout toggles
- UX instrumentation for click-depth/time-to-complete baseline vs post-consolidation
- `Inventory Intake Wizard` MVP page + workflow contract (source/lot/media/costing/AI assist -> product/listing-ready output)
- eBay Store + listing-format control surface in eBay Workspace (store profile selection, auction/fixed-price templates, validation, and publish-preflight checks)
- GitHub Actions release workflow that builds/tests and publishes versioned Docker images (semver tag + commit SHA tags)
- Promotion contract for contained Dev and Prod Kubernetes environments (image tag pinning, required migration run, and rollback expectations)
- ArgoCD integration contract for separate infra repo (app manifests reference published image tags and promote by GitOps PR/merge)

Acceptance Criteria:
- Daily eBay tasks (verify auth, monitor live listings, revise/end/relist, push tracking, inspect sync history) run from one workspace
- Operators can complete top 10 repetitive workflows with fewer page switches and clearer state
- Consolidated workspaces keep audit traceability and permissions unchanged
- Navigation complexity is reduced without hiding critical controls
- Intake flow supports both coin-specific and generic inventory onboarding without forcing operators through coin-only fields
- Operators can create/review/publish eBay listings with explicit format intent (`auction` or `fixed-price`/Buy It Now) and store-default policy/category context from one workspace flow
- Build artifact provenance is clear across environments (each running image is traceable to a git commit/version)
- Dev promotion does not require manual image rebuilds, only tag/manifest updates
- Prod promotion is controlled via GitOps workflow in ArgoCD-managed infra repo with auditable change history
- Progress:
  - Completed GS-V06-008 first pass in Ask GoldenStackers with a new `Goldy Orchestration (Preview)` panel (single/multi mode + agent-role selector), role-safe domain scoping, read-only write-intent guardrails, and recent orchestration trace visibility
  - Added `ai_chat` orchestration trace enrichment (`event_type=goldy_orchestration`, mode/role/plan/status metadata) and repository-side trace query helper for operator/audit review
  - Expanded workspace feedback capture coverage beyond Operations/eBay/Sync/Shipping/Intake/Listings into Products, Sales, Orders, Reports, Documents, and Tools for fuller operator UX signal collection
  - Added explicit workflow task-completion telemetry capture (`workspace_task_completion`) in Operations Home, eBay Workspace, Listings, Sync, and Shipping to improve rollout baseline confidence for time-to-complete signals
  - Hardened Admin parity/readiness checks with workflow task-completion evidence thresholds (`ux_parity_min_task_completion_events`) and task-gap readiness weighting (`ux_readiness_weight_task_gap`)
  - Started GS-V06-001 with a new unified `eBay Workspace` page (`pages/22_eBay_Workspace.py`) combining integration + ops flows in one surface
  - Added rollout flag scaffold `ux_workspace_ebay_enabled` (runtime + env fallback) and legacy-page redirect behavior for `eBay` and `eBay Ops`
  - Updated ops navigation shortcuts/links to target the unified workspace path
  - Added shared eBay workspace controls (token + Ops status/linked/search defaults) with apply/reset behavior to keep Integration and Operations tabs aligned
  - Expanded shared eBay workspace controls with listed-date window defaults and wired Local Ops filtering/export state to the same date context for cross-tab consistency
  - Added eBay Workspace account-context profile management (alias-based save/load/delete with optional token persistence) to support faster multi-account operational switching
  - Added eBay Integration + eBay Ops in-tab profile quick switch action (`Apply Profile to Integration + Ops`) so account context can be swapped without reopening workspace controls
  - Added a shared “Current Active Account Context” banner across eBay Workspace/Integration/Ops to show active profile and filter/date context before running operations
  - Added an eBay Workspace pre-flight runbook checklist with Ops warning when not complete to reduce high-impact action mistakes
  - Added optional runtime runbook enforcement (`ebay_require_runbook_for_bulk_ops`) to disable bulk eBay Ops controls until checklist completion
  - Added Admin `UX Navigation Controls` checkbox for eBay runbook enforcement so teams can toggle guard behavior per environment quickly
  - Added eBay Workspace quick-link panel (Listings, Sync, Shipping, Admin) for faster operational routing from a single surface
  - Added context-preserving quick-link handoff from eBay Workspace into Listings/Sync/Shipping (preloaded eBay filters/provider focus) to reduce re-filtering friction
  - Added explicit handoff-context banners with one-click reset on Listings/Sync/Shipping so preloaded eBay filters are visible and reversible
  - Added navigation telemetry for workspace handoffs (`workspace_handoff_applied`/`workspace_handoff_cleared`) to quantify context-routing usefulness and reset frequency
  - Added Admin System Health handoff telemetry card (applied/cleared counts and clear-rate by destination) for direct IA/workflow tuning
  - Added `Recent Handoff Events` detail view in Admin telemetry for audit-friendly handoff troubleshooting (actor/action/target/payload summary)
  - Added handoff-event filtering/export controls in Admin telemetry (action/target/actor filters + CSV) for weekly UX tuning reviews
  - Added handoff KPI strip (`Top Actor`, `Top Target`, `Most Cleared Target`) for faster operational signal scanning
  - Added one-click `Export Handoff Governance Bundle (ZIP)` in Admin telemetry (KPI snapshot + target aggregate + filtered/full handoff event exports)
  - Added one-click `Export Workspace Feedback Governance Bundle (ZIP)` in Admin (feedback events, workspace/sentiment aggregates, notes, summary KPIs)
  - Added one-click `Export Parity Governance Bundle (ZIP)` in Admin (readiness summary, workflow parity table, permission/audit gaps, recent snapshots, release decisions, filtered follow-ups)
  - Added dedicated Admin `Governance Exports` tab to centralize one-click governance artifact exports (handoff, workspace feedback, parity/follow-up, photo-comp) plus combined all-in-one bundle
  - Added manual `Governance Snapshot Runner` in Admin Governance Exports (audit-stamped `governance_export/snapshot` events + recent snapshot history export) for recurring governance review cadence
  - Added sync-runner scheduled governance snapshot automation scaffold (runtime-gated `governance_snapshot_*` settings + interval-based snapshot recording in worker loop)
  - Added Admin `Sync Job Controls` inputs for governance snapshot scheduling (enable toggle, interval hours, lookback days, max rows per scope)
  - Added Admin `Sync Job Controls` one-click `Run Governance Snapshot Now` action for immediate snapshot capture/testing without waiting for worker interval
  - Added Admin `Sync Job Controls` scheduler status readout (last worker snapshot, next due, and overdue indicator) for governance snapshot operational visibility
  - Added Governance Exports snapshot-history filters (`source` + scheduled/manual mode) and filtered CSV export to speed governance triage by origin (`sync_runner`, `admin_sync_jobs`, `admin_governance_exports`)
  - Expanded Sync Jobs scheduler status to show both latest worker snapshot and latest manual snapshot side-by-side (plus recency delta) for faster scheduler-vs-operator verification
  - Added Sync Jobs scheduler source breakdown table (last 7d / last 30d counts by snapshot source) for quick governance-cadence monitoring
  - Added Sync Jobs governance snapshot cadence health indicator (GREEN/YELLOW/RED) based on expected-vs-actual worker snapshot frequency for configured interval
  - Added RED-cadence follow-up task creation in Sync Jobs (with owner/priority/due controls and duplicate-open-task guard) to route scheduler drift into tracked blocker workflow
  - Added Operations Home `Governance Cadence Follow-ups` visibility (KPI, queue-router row, dedicated queue tab, and Admin quick-open action) so cadence blockers surface outside Admin
  - Added direct resolve action for `governance_snapshot_cadence` follow-ups in Operations Home queue (with resolution note) to close cadence blockers without leaving command center
  - Started image-first inventory intake: Inventory Intake Wizard now supports optional starter photo/upload analysis to prefill title/category/item type/description/AI description and comp-search seed
  - Added first non-coin photo-comp starter in Tools: image-hint mode now supports camera/upload + AI-generated comp query seed for general-item comparable searches
  - Added dedicated Photo-Comp one-click workflow controls in Tools (auto image query seed on run, optional always-on web fallback overlay, optional auto AI summary) for general non-coin comp execution
  - Added Photo-Comp quality diagnostics and retry profiles in Tools (coverage/missing-price metrics + one-click retry presets for structured web, broad web, and broadened eBay reruns)
  - Extended Photo-Comp retry presets with dealer-domain focus and missing-domain targeted reruns (derived from prior web rows missing prices)
  - Added DB-backed saved Photo-Comp retry presets (mine/shared/default) so repeat comp-capture strategies can be reused across sessions/devices
  - Added default Photo-Comp retry preset auto-load (once per user/env session) with explicit session clear control
  - Added Photo-Comp retry telemetry trail and in-panel recent-run table (strategy/preset + coverage outcomes) to make comp-capture tuning data-driven
  - Added Admin `Comp Config` photo-comp telemetry dashboard (strategy filters + no-result/coverage KPIs + CSV export) for centralized comp tuning
  - Added Admin one-click `Promote Best To Default` action from photo-comp telemetry to create/update default retry presets from best-performing runs
  - Added promotion guardrails for `Promote Best To Default` (minimum run threshold + confidence hint from sample size/no-result rate) to reduce premature default changes
  - Added Admin strategy trend visuals for photo-comp telemetry (coverage % and no-result rate % by strategy over time)
  - Added Admin top missing-price domain leaderboard (missing/priced counts + miss rate + top-10 chart) to target comp parser improvements
  - Added Admin one-click dealer-domain recommendation apply flow (threshold-based add/remove from telemetry) to keep `comp_dealer_domains_csv` tuned without redeploys
  - Added dealer-domain dry-run change preview (add/remove/all) and recommendation-apply audit trail for safer runtime domain tuning
  - Added Admin recommendation-apply history with CSV export for dealer-domain governance traceability
  - Added Admin undo flow for recommendation applies (restore prior domain set from snapshot + explicit undo audit event)
  - Added undo telemetry and failed-attempt audit reasons for dealer-domain recommendation governance (with actor breakdown + CSV export)
  - Added one-click `Export Photo-Comp Governance Bundle (ZIP)` in Admin Comp Config (strategy performance, domain leaderboard, recommendation apply history, undo telemetry, recommendation summary)
  - Added direct handoff action from Comp Tool image-hint mode into Inventory Intake Wizard to prefill intake seed/title/description and jump into intake flow
  - Added direct handoff action from Comp Tool image-hint mode to create a draft-ready Product record (AI-seeded fields + optional hint-media attach) for faster review before listing
  - Extended Photo-Comp draft flow to optionally create multi-marketplace draft listings in one action, with review-first default behavior (`draft` + `pending`)
  - Added Listings bulk draft creation workflow from selected products (marketplaces + price/qty strategy + existing-pair skip) for faster intake-to-review throughput
  - Added Listings origin filter for `photo_comp_draft` lineage (derived from audit trail) to isolate AI-assisted intake listings during review
  - Added one-click + team-save `Photo-Comp Review Queue` preset actions in Listings to standardize review filtering across operators
  - Added Admin-controlled auto-apply option for Listings Photo-Comp Review Queue preset (`ux_listings_auto_photo_comp_review_preset`) for review-focused sessions
  - Added Operations Home `Photo-Comp Pending Review` queue card/tab with direct jump into Listings prefiltered review state
  - Extended Listings handoff banner to recognize Operations Home queue handoffs and clear/reset origin-aware filter context in one click
  - Added first-pass Documents tax engine controls (jurisdiction, auto/manual/no-tax modes, category exemption rules, shipping-taxable toggle, tax row rendering, and tax-adjusted total option)
  - Seeded tax runtime defaults (`invoicing_tax_jurisdiction`, `invoicing_tax_rate_percent_default`, `invoicing_tax_shipping_taxable_default`, `invoicing_tax_exempt_categories_csv`, `marketplace_facilitator_channels_csv`) for environment-level configuration
  - Updated Golden local tax default profile to `7.50%` (CO `2.90` + Jefferson County `0.50` + Golden `3.00` + CD `0.10` + RTD `1.00`) across Documents/Reports runtime fallbacks and baseline tooling inputs
  - Added helper-level guardrails for facilitator-channel tax-scope defaults in Reports (`_default_tax_marketplace_scope` tests), ensuring local liability scope excludes configured facilitator channels by default while preserving fallback behavior when all channels are facilitators.
  - Added first-pass Documents tax presets (`Golden Local Retail`, `Marketplace Shipped`, `Bullion/Coin Exempt`) plus one-click save of current tax controls to runtime defaults
  - Added line-item taxability overrides in Documents auto-tax mode (per-line taxable toggles) to support mixed taxable/exempt invoice scenarios
  - Added first-pass Reports tax analytics (`Tax Summary`, `Tax by Marketplace`, `Tax Detail`) with jurisdiction/rate/marketplace scope controls and exempt-category-aware estimates
  - Added Reports tax preset quick-apply actions (`Golden Local Retail`, `Marketplace Shipped`, `Bullion Exempt Focus`) for repeatable finance review workflows
  - Added Reports Tax Drilldown view (marketplace + taxable/exempt segment filters) with focused CSV/XLSX exports for monthly reconciliation
  - Added Tax Drilldown handoff into Documents to create invoice drafts from selected sale rows with tax context preloaded
  - Added shared Reports `Document Draft Handoff` panel supporting both `Sale` and `Order` source prefill into Documents (invoice/receipt)
  - Added direct `Open in Documents` quick actions in Sales and Orders side panels (invoice/receipt prefill from selected row)
  - Added eBay Workspace `Document Draft Quick Handoff` for recent eBay orders/sales (one-click invoice/receipt prefill into Documents)
  - Added Listings side-panel `Open in Documents` action with auto-discovered related Sale/Order sources from listing links/order items
  - Added Search/Edit `Open in Documents` quick actions on Listings and Sales tabs (including related-source resolution for listing-selected records)
  - Added Documents `Recent Document Handoffs` panel with one-click reopen/clear to quickly restore recent Sale/Order draft contexts
  - Upgraded `Recent Document Handoffs` to DB-backed per-user/per-environment persistence (cross-restart/device) with session merge + one-click clear
  - Added Admin `Runtime Settings` panel for persisted Documents handoff history (team/user filters, open-in-documents action, and per-user clear)
  - Added role guardrails for Admin handoff history panel (`admin` can manage all users; non-admin can only view/reopen/clear own history)
  - Added explicit audit events for handoff-history clear actions (`documents_handoff_history/clear_history`) from Documents and Admin paths
  - Added Admin clear-audit summary card/table for Documents handoff history governance (by actor/target + CSV export)
  - Added date-range filters (`From Date` / `To Date`) to Documents handoff clear-audit summary for weekly/monthly governance slices
  - Added date presets (`Last 7d`, `Last 30d`, `This Month`, `Custom`) for Admin Documents handoff clear-audit summary
  - Added one-click `Export Governance Bundle (ZIP)` from Admin clear-audit summary (raw events + actor/target aggregate CSVs)
  - Added governance clear-reason control: admin must enter reason when clearing another user; reason is captured in audit payload/export
  - Standardized clear reasons with taxonomy (`reason_code`) plus optional note (`reason_note`), including required note for `other`
  - Added clear-audit reason-code analytics (table + bar chart) and included reason-code aggregate CSV in governance bundle export
  - Added clear-audit filters for `reason_code` and `scope` so governance metrics/tables/exports can be narrowed pre-download
  - Added user-scoped saved governance view presets (save/load/delete) for clear-audit date/filter/lookback settings
  - Added shared governance presets (`My Presets` vs `Shared Presets`) with admin-only shared save/delete and shared load for all users
  - Added `Set as Team Default` shared governance preset and one-time auto-load of team default when entering clear-audit panel
  - Added audit events for governance preset lifecycle (`save`, `load`, `delete`, `set_team_default`, `clear_team_default`)
  - Added Admin governance-preset audit summary panel (event/action/actor metrics, recent events table, CSV export)
  - Added date-range filters to governance-preset audit summary for period-scoped preset-governance review
  - Added quick date presets (`Last 7d`, `Last 30d`, `This Month`, `Custom`) to governance-preset audit summary
  - Added `Governance Review Mode` toggle so clear-audit and governance-preset-audit share one date preset/range during recurring review runs
  - Added runtime-backed persistence for Governance Review Mode (`documents_handoff_governance_review_mode`) with explicit save action in Admin Runtime Settings
  - Added saved governance date-window presets (`My Presets` / `Shared Presets`) for Governance Review Mode plus admin window team default controls
  - Added audit telemetry for governance date-window lifecycle actions (`window_save/load/delete/set_default/clear_default`) in the Admin governance audit stream
  - Added v0.6 scope planning item for eBay Store operations + listing-format workflows (Auction/Buy It Now) in unified eBay Workspace roadmap track
  - Started GS-V06-010 with runtime-backed eBay Store profile controls in eBay Workspace (save/load/delete/default) including policy/category/format defaults and active-context visibility
  - Added workspace apply option to persist selected store context into eBay runtime defaults (`ebay_*` keys) for cross-page listing/publish consistency
  - Added format-aware readiness/validation for eBay listing workflows (`FIXED_PRICE` vs `AUCTION`) with format/duration surfaced in readiness queue and stricter auction/BIN guardrails
  - Added fixed-price Best Offer support in Listings publish/revise payloads (`bestOfferTerms`) with runtime + store-profile defaults and context-banner visibility
  - Added auction default controls (`start/reserve/buy-now`) to eBay store profiles/runtime keys and enforced reserve/start validation in readiness + publish/revise flows
  - Expanded readiness queue with auction pricing context columns for format-specific review (`auction_start_price`, `auction_reserve_price`, `auction_buy_now_price`)
  - Added readiness format triage controls in Listings (`all/fixed/auction`) with quick shortcuts for `Auction Blocked` and `Fixed Ready`
  - Added one-click format template actions in Listings publish flow (`Fixed Price Standard` / `Auction Standard`) preloading store/runtime policy + format defaults
  - Added format quick-hint visibility in Listings table (`format_type`, `format_hint`) to flag fixed/auction issues earlier in the workflow
  - Added Listings `Format Issue Only` filter to quickly isolate rows with format setup issues (`format_hint`) for correction workflows
  - Added one-click Listings `Format Fix Queue` preset controls (apply + team-save) for standardized format-correction triage
  - Added Operations Home `Format Fix Needed` queue workflow (metric card, queue tab/router row, and one-click handoff to Listings with format-fix preset state)
  - Added eBay Workspace quick-link `Open Format Fix Queue` to route straight into Listings with format-fix triage filters and handoff audit context
  - Added eBay Workspace tab-level `Format Fix Needed` KPI/actions in both Integration and Operations tabs for pre-navigation triage awareness
  - Added blocker-priority summaries for eBay listing readiness in Listings and eBay Workspace (top blocker/warning reason counts)
  - Added Listings readiness reason filters (`Blocker Reason` and `Warning Reason`) for targeted remediation queues
  - Added Listings top-blocker quick-action filters with automatic format targeting (auction/fixed) for one-click triage
  - Added Listings blocker-driven follow-up task creation (audit-backed `workspace_followup` create events) to convert repeated readiness blockers into tracked remediation tasks
  - Added Listings in-context recent blocker follow-up task panel (status/history snapshot + CSV export) to reduce Admin page context-switching during triage
  - Added Listings in-context follow-up resolve action (`workspace_followup` resolve) to close blocker tasks directly from readiness workflow
  - Added Operations Home blocker-followup command-center visibility: open-task KPI/button + `Listings Blocker Follow-ups` queue view with direct handoff into Listings remediation workflow
  - Added blocker follow-up SLA tagging (`due_in_days`, `sla_status`) with urgency-first ordering in Listings and Operations Home blocker queues
  - Added blocker follow-up SLA KPI summaries (`Open`, `Due Soon`, `Overdue`) in Listings and Operations Home queue views
  - Added Operations Home blocker-followup queue filters (`status`/`owner`/`priority`/`SLA`) for focused command-center triage
  - Added Listings blocker-followup filters (`status`/`owner`/`priority`/`SLA`) with filtered KPI/export/resolve behavior for workflow symmetry with Operations Home
  - Added Listings blocker-followup quick filter presets (`Overdue Critical`, `My Open`, `High Priority Open`) for one-click triage context switching
  - Added Operations Home blocker-followup quick queue presets (`Overdue Critical`, `My Open`, `High Priority Open`) to mirror Listings triage acceleration patterns
  - Added DB-backed saved/shared blocker-followup presets in Listings and Operations Home for persistent team triage configurations
  - Added default blocker-followup preset support (set/clear default + auto-load) for both Listings and Operations Home triage queues
  - Enhanced Admin Saved Filters governance with scope filters and blocker-only focus mode for managing blocker preset ownership/deletion at scale
  - Added Admin Saved Filters one-click scope presets (`All`/`Blocker Presets`/`Listings`/`Operations Home`) to speed governance triage
  - Added Admin Saved Filters ownership presets (`My Owned`/`Shared Owned By Me`/`Shared Not Owned By Me`) for faster ownership transfer/delete workflows
  - Added Admin Saved Filters one-click reset control for governance filters (scope + blocker-only + ownership) to quickly return to baseline view
  - Added Admin Saved Filters governance filter-state summary metrics (active scope count, owner mode, blocker focus, visible rows)
  - Added Admin Saved Filters CSV export for the filtered governance table to support audit handoff/reporting
  - Added Admin Saved Filters filtered breakdown summaries (`By Owner`, `By Scope`) for faster governance triage patterns
  - Added Admin Saved Filters default-only toggle and visible-default metrics to simplify default-preset governance checks
  - Added v0.6 scope item GS-V06-011 for multi-environment release and GitOps delivery readiness (GitHub Actions versioned images, Dev/Prod Kubernetes promotion contract, ArgoCD infra-repo handoff)
  - Started GS-V06-011 delivery implementation by updating GitHub workflows for strict semver release tags, main-branch prerelease image tags, multi-arch Docker publish, compile smoke-checks, and optional ArgoCD infra-repo PR handoff via configurable repo variables
  - Added copy-ready Kubernetes manifest template packs for `dev` and `prod` (`k8s/templates/*`) including namespace/config/secret-template/PVC/deployments/service/ingress/migration-job/HPA/PDB/network-policy
  - Added ArgoCD bootstrap templates (`k8s/templates/argocd/*`) for Dev/Prod Applications, optional root app-of-apps, and AppProject scaffold
  - Added deployment runbook (`DEPLOYMENT_RUNBOOK.md`) documenting release artifact contract, Dev/Prod promotion gates, ArgoCD sync flow, smoke checks, and rollback procedure
  - Added operator sign-off artifact `GO_LIVE_CHECKLIST.md` for pre-Dev/Prod promotion readiness tracking (owner/evidence/date fields)
  - Added Search/Edit Lots workflow with lot lookup and edit controls (lot/source/vendor/cost/tax/shipping/handling/eBay refs/notes)
  - Added lot-to-product visibility in Search/Edit Lots (`attached_products_count`, SKU summary, and per-lot assignment detail table)
  - Fixed Streamlit state reset behavior in Lots create flow (post-create flash + safe rerun reset) to avoid widget mutation exceptions
  - Fixed Products `Purchased On eBay` field UX so eBay item/link inputs remain editable and submit-time validation enforces required item ID
  - Hardened Comp Tool eBay reliability with:
    - per-run call caps
    - rolling 10-minute call caps
    - local cooldown after eBay Finding rate-limit responses
    - web-only graceful fallback when eBay is unavailable/capped
    - explicit diagnostics for `local_cooldown` vs `remote_rate_limited` errors
    - one-click local cooldown clear action and last-error payload visibility in the UI
  - Hardened Kubernetes Dev/Prod template NetworkPolicies to scoped egress ports only (DNS/HTTP/HTTPS/Postgres/NTP) and constrained policy targeting to GoldenStackers app pods
  - Completed GS-V06-011 by adding build metadata traceability support (`APP_BUILD_VERSION`, `APP_BUILD_SHA`, runtime keys `app_build_version`/`app_build_sha`) surfaced in Admin System Health
  - Extended GS-V06-011 automation by adding optional Argo configmap metadata update in release workflow (`ARGO_CONFIGMAP_PATH`) so deployed manifests automatically carry `APP_BUILD_VERSION` and `APP_BUILD_SHA`
  - Refined GS-V06-011 release automation with environment-specific Argo promotion variables (`ARGO_DEV_*`, `ARGO_PROD_*`) so `main` can drive Dev manifest PRs and semver tags can drive Prod manifest PRs independently
  - Added deployment preflight workflow (`deploy_config_check.yaml`) to validate Docker secrets and Argo promotion variables before running live environment promotions
  - Added eBay Workspace `Active Format Defaults Summary` (format/policy/defaults table + missing-policy indicator) for preflight verification before listing actions
  - Completed GS-V06-010 reporting slice by adding Reports export/view `Listing Format Intent vs Publish Outcome` (intent format/duration, publish attempt/success/error counts, published id/timestamp, last publish error)
  - Completed GS-V06-009 template expansion by adding reusable branded HTML blocks in Listings and one-click Golden Stackers starter marketplace templates (eBay/Craigslist/Facebook/Whatnot)
  - Updated document business default to `Golden Stackers LLC` with website `https://goldenstackers.com`
  - Added `Comp Tool: Photo Mode` handoff actions in Products/Listings side panels to open Tools in image-hint flow with product/listing context prefilled
  - Added first-pass eBay Ops command rail with centralized day-to-day actions (end/relist/queue/open-sync/retry/resolve-errors)
  - Added eBay listing side-panel detail tabs (detail + sync lineage + timeline) to reduce context switching during listing triage
  - Began GS-V06-002 workspace shell extraction by moving command-rail UI into reusable component and applying it in eBay Ops
  - Standardized queue status semantics (`needs_action`, `in_progress`, `blocked`, `done`) with shared helpers and applied them across Operations Home and eBay workspace/ops queue tables
  - Completed GS-V06-002 by standardizing shared empty/error/loading workspace states across Operations Home, Sync, Shipping, and eBay Ops
  - Completed GS-V06-003 planning by defining Inventory/Fulfillment/Sync/Revenue workspace scopes and publishing migration dependency sequence in `UX_CONSOLIDATION_PLAN.md`
  - Started GS-V06-004 role-aware IA with sidebar pinned workflow pages and role-default landing behavior (`admin -> eBay Workspace`, `ops -> Operations Home`, `viewer -> Dashboard`)
  - Completed GS-V06-004 workflow-stage navigation grouping with role-aware unified sidebar sections (`Intake`, `Listing`, `Fulfillment`, `Reconcile`, `Admin/Support`)
  - Added GS-V06-004 rollout + telemetry controls: runtime navigation mode toggle (`ux_navigation_mode` unified/legacy), navigation telemetry audit events (`page_view`/`page_switch`), and Admin System Health nav analytics (top pages, switch paths, bounce counts)
  - Expanded workspace rollout flags beyond eBay (`ux_workspace_inventory_enabled`, `ux_workspace_fulfillment_enabled`, `ux_workspace_sync_enabled`, `ux_workspace_revenue_enabled`) and wired them to unified sidebar visibility + Admin controls
  - Added GS-V06-005 workflow baseline metrics in Admin (median switch latency, bounce rate, session click-depth, and workflow handoff transition medians)
  - Added dedicated Admin `UX Navigation Controls` card (nav mode + telemetry/default-landing toggles) and telemetry-window reset/filter support for cleaner baseline measurement runs
  - Started GS-V06-005 operator feedback capture with reusable workspace feedback control (thumbs + note) on Operations Home and eBay Workspace, persisted to audit events
  - Added GS-V06-005 Admin parity checker in System Health to validate legacy-vs-workspace permission coverage and recent audit evidence before cutover
  - Added parity snapshot controls (CSV export + audit-stamped `workspace_parity/snapshot` event) so each cutover decision can be documented with a timestamped artifact
  - Added `Recent Parity Snapshots` review table in Admin System Health with snapshot inspect + workflow detail drill-down for faster release-readiness comparisons
  - Added parity `Compare Two Snapshots` diff view (permission/audit delta metrics + workflow change table) to track rollout readiness improvements over time
  - Added release decision capture on parity snapshots (`approved`/`rejected` + notes) with recent decision history for auditable go/no-go records
  - Added release-readiness status layer in parity tooling (latest decision/approved snapshot metrics, rejected/stale/missing-decision warnings)
  - Added aggregate rollout readiness scoring (`0-100` with GREEN/YELLOW/RED) from parity gaps, decision status, and overdue follow-up workload
  - Added runtime-configurable readiness scoring weights (`ux_readiness_*`) and Admin controls so strictness can be tuned by environment/team policy
  - Added one-click readiness policy presets (`conservative`, `balanced`, `aggressive`) for rapid environment-level scoring strategy changes
  - Added parity follow-up task workflow for rollout blockers (create/resolve + open/resolved queue) so rejected gaps can be tracked and closed in-app
  - Added follow-up queue operations controls (status/owner/priority filters + CSV export) to support weekly rollout blocker reviews
  - Added follow-up SLA/urgency triage (overdue/due-soon flags, urgency-first sorting, and queue metrics) for rollout blocker management
  - Added optional Slack alerting for rollout governance events (parity decision notifications and overdue follow-up alerts with templates/channels + audit dedupe)
  - Removed duplicate legacy eBay pages from sidebar; consolidated navigation now points to `eBay Workspace` only
  - Started row-action standardization in command surfaces by applying shared action pattern to Operations Home sync-failure queue
  - Started GS-V06-006 with new `Inventory Intake Wizard` page (generalized intake for bullion/collectibles/antiques/normal goods, optional lot creation, media upload, and draft listing handoff)
  - Added wizard audit/timeline integration and post-create action rail (open Products/Listings/Search + inline entity timeline review)
  - Added in-wizard AI suggestion generator for title/category/description defaults using configured AI runtime profiles
  - Expanded GS-V06-005 workspace feedback coverage beyond initial pages by adding the shared feedback control to Sync, Shipping, Inventory Intake Wizard, and Coin Intake Wizard
  - Added GS-V06-005 baseline-metrics artifact controls in Admin (CSV export + audit-stamped `navigation_baseline/snapshot` event) to preserve measurable UX baselines per rollout window
  - Added GS-V06-005 Admin `Workspace Feedback Insights` panel (lookback filters, workspace/sentiment breakdown, recent notes, CSV export) to turn operator feedback into triage-ready backlog signals
  - Added GS-V06-005 feedback-to-action bridge: selected feedback notes can now create tracked rollout follow-up tasks directly in Admin System Health
  - Advanced GS-V06-001 parity by moving eBay listing filters into one shared eBay Ops filter bar and reusing the same status/linked/date/search state for both `Local Ops` and `eBay API Listings` views
  - Advanced GS-V06-001 side-panel parity by reusing a shared listing side-panel renderer across both `Local Ops` and `eBay API Listings` (Detail + Sync Lineage + Timeline)
  - Advanced GS-V06-001 command-rail unification by adding `Unqueue Selected` and `Run Revise Queue` rail actions, then removing duplicate legacy bulk-action button/forms in eBay Ops
  - Completed GS-V06-001 eBay Workspace Unification milestone (shared context/filtering, unified command rail, unified listing side-panel, and legacy eBay sidebar-page consolidation)
  - Advanced GS-V06-010 template workflows with eBay Workspace quick actions for format intent (`Open Listings (Fixed Template)` / `Open Listings (Auction Template)`) that preseed listing publish defaults before handoff
  - Advanced GS-V06-010 create-flow integration by persisting active workspace-store format/policy/category defaults into draft listing `ebay_publish` metadata during listing creation
  - Advanced GS-V06-010 create UX with explicit `eBay Draft Publish Defaults` controls in Listings create flow and persisted those user-selected values into draft `ebay_publish` metadata
  - Tightened GS-V06-010 create-flow validation for eBay draft defaults (fixed BIN > 0, auction duration/start/reserve/buy-now constraints) and normalized auction draft local `listing_price` to auction start when needed
  - Added Listings `eBay Store Profile Context` apply controls for saved workspace profiles and completed GS-V06-010 end-to-end
  - Completed GS-V06-006 by adding buffered image-first intake starter behavior (analyze photo -> prefill defaults -> optionally persist starter image into intake media upload at submit time)
  - Advanced GS-V06-007 handoff depth by adding direct Comp Tool image-query seed handoff into Listings draft-create defaults with one-click navigation
  - Completed GS-V06-009 tax + branding expansion scope (Documents tax controls/presets/line-level overrides, tax reports + drilldown + handoffs, branded listing templates, and compliance-oriented exports)
  - Expanded GS-V06-005 feedback coverage by adding in-page Workspace Feedback capture on Listings

## v1.0 (Scale + Automation)
Target: production-grade resilience and multi-channel growth
Execution tracking: `V1_0_IMPLEMENTATION_CHECKLIST.md`

Scope:
- Additional channels (deferred until account readiness; current focus remains eBay-first, then Whatnot/Craigslist/Facebook when enabled)
- QA hardening and coverage expansion (P0) to protect inventory/listing/shipping/admin workflows before broader rollout
- DB-backed workflow draft/state persistence (P0) to reduce Streamlit session-state fragility in multi-step operator flows
- Label buying integration (eBay shipping and/or Pirate Ship API)
- Event-driven automation (auto-create shipment, auto-close listing, auto-status transitions)
- Observability and SRE baseline (error tracking, metrics, alerts)
- Backup/restore automation and runbooks

Deliverables:
- Integration adapters by channel/provider
- Coverage baselines + quality gates in CI with staged threshold ratchets
- Expanded unit/integration suite for core business logic (`repository`, `services`, `auth`, `validation`)
- Expanded Playwright suite for critical operator flows (auth, intake, listing review/publish, shipping, sync, admin sign-off)
- Workflow draft persistence layer (`workflow_drafts` / `workflow_events`) with repository/service API (`load/save/clear/resume`)
- Session-state reduction map per page (Listings, Listing Wizard, eBay Workspace, intake wizards) separating `db-persisted` vs `ui-transient`
- Autosave + resume-draft UX for long-running listing/intake/eBay setup workflows
- Unified fulfillment API in app for label + tracking lifecycle
- Monitoring dashboards + alert thresholds
- Disaster recovery documentation + tested restore process

Acceptance Criteria:
- Multi-channel order ingestion works with no manual copy-paste
- QA gates block regressions:
  - mandatory unit + e2e smoke in CI on every PR
  - enforced coverage thresholds configured and ratcheted upward by milestone
- Core business logic reaches near-complete coverage target before full production cutover:
  - target `>=95%` on scoped core modules (`repository`, `services`, `auth`, `validation`, `security`)
  - target `>=70%` global Python line coverage as interim operational standard, then continue ratcheting
- Labels and tracking can be created/updated from app workflows
- Critical multi-step workflows survive reruns/restarts without data loss:
  - no business-critical dependence on volatile `st.session_state` alone
  - listing/intake/eBay setup drafts are recoverable by user/environment
  - widget-key mutation errors are eliminated from core operator paths
- SLA/health metrics and alerts are in place
- Backup restore drill passes on schedule
- Progress:
  - Started GS-V10-001 Shopify ingestion foundation with executable sync-job scaffold (`shopify_orders_pull`) that creates normal sync runs/events and completes safely as a no-op pull while adapter mapping is built
  - Added Sync `Execute Job Now` controls for dispatcher-supported jobs so Shopify scaffold (and future adapters) can be triggered directly from UI with immediate run telemetry
  - Deferred Shopify-first implementation until account onboarding; Shopify remains disabled-by-default while v1.0 execution shifts to eBay/inventory/fulfillment priorities
  - Started GS-V10-002 label-buying integration scaffold: Shipping now includes `Label Purchase Queue` controls that create/process shipping queue jobs (`purchase_label`) for eBay Shipping/Pirate Ship style workflows with dry-run-safe execution and audit trace
  - Extended GS-V10-002 label artifacts and visibility: sale records now track `shipping_label_id/url/cost/currency/purchased_at` and Shipping queue/export/read-only views expose those fields for faster fulfillment audit
  - Added GS-V10-002 runtime guardrails in Admin + execution path: shipping queue enablement, label purchase enablement, live-provider-call toggle, and provider-specific toggles are now runtime-configurable per environment
  - Added GS-V10-002 provider adapter foundation: shipping queue now dispatches live label purchase through provider adapters, with Pirate Ship `mock/api` mode and runtime-configured base URL/API key controls
  - Hardened GS-V10-002 Pirate Ship adapter runtime contract: configurable endpoint path/auth scheme/timeout plus tolerant nested-response mapping for label/tracking/cost fields
  - Added GS-V10-002 Admin adapter test action (`Test Pirate Ship Adapter`) so runtime configuration can be validated with audit traces before queue execution
  - Started GS-V10-003 event-driven automation foundation with DB-backed `integration_automation_rules` and Admin CRUD preview (conditions/effects JSON + audit trail)
  - Extended GS-V10-003 execution foundation: queued integration jobs now run automation-rule evaluation pre-execution with dry-run mode, approval gating, and integration-event telemetry for matched/applied/blocked outcomes
  - Added GS-V10-003 explicit approval records (`integration_automation_approvals`) and Admin approval controls; requires-approval rules now need active non-expired approvals (optionally job-scoped) before execution
  - Added GS-V10-003 automation failure triage panel in Admin with quick actions to approve gated rules, retry queue jobs, and disable matched rules
  - Added GS-V10-003 rule impact preview controls in Admin Automation Rules to estimate candidate/matched queue jobs (with sample rows) before saving/enabling rule changes
  - Added GS-V10-003 triage replay simulation (read-only) so operators can re-evaluate a historical queue job against current rule set + approval state and compare drift from original event outcome
  - Added GS-V10-003 simulation-to-action bridge: replay panel now supports one-click approval creation for currently gated rules (job-scoped, TTL-controlled) to reduce triage handoff steps
  - Added GS-V10-003 replay drift detection and logging: triage now shows historical-vs-replay metric deltas (`matched/applied/gated/blocked`) and supports one-click drift event audit records
  - Extended GS-V10-003 with `Automation Hardening Sign-Off (Dev/Prod)` in Admin Integrations (guardrails/approval-policy/runbook checks + owner/date/status/evidence)
  - Extended GS-V10-003 go-live artifact packaging with `integration_automation_hardening_signoffs.csv` and hardening sign-off status fields in summary/capture payloads
  - Closed GS-V10-003 implementation scope for now (remaining execution evidence/sign-off remains a go-live operations activity)
  - Started GS-V10-004 observability baseline with structured queue execute-exception capture (`*_execute_exception` integration events with exception type + traceback preview) and generalized terminal queue failure Slack alert hook (`slack_notify_integration_queue_failures`)
  - Extended GS-V10-004 System Health with `Error Signals (24h)` (queue execute exceptions, terminal queue failures, warning counts, and recent integration warning/error samples)
  - Extended GS-V10-004 with runtime-configurable error-signal thresholds and remediation links (`health_*_24h`, `runbook_*_url`) surfaced directly in System Health error details
  - Extended GS-V10-004 with optional critical-state auto-alerting from System Health (cooldown-guarded Slack alert + integration event evidence) and direct runbook quick-link actions for remediation
  - Extended GS-V10-004 with manual critical-alert validation action in System Health (`Send Critical Health Alert Now`) to verify Slack channel/template/audit behavior pre-go-live
  - Extended GS-V10-004 with `Critical Alert Evidence (Recent)` view in System Health for recent manual/auto health alerts and Slack dispatch outcomes (sent/queued/error)
  - Extended GS-V10-004 with CSV export for `Critical Alert Evidence` to streamline go-live evidence packaging and audit handoff
  - Extended GS-V10-004 with Admin `Go-Live Evidence Pack (ZIP)` export containing governance, system-health alert evidence, queue snapshot, config-missing summaries, and checklist snapshot in one artifact
  - Extended GS-V10-004 evidence packaging with checklist readiness scoring in Admin (done/in-progress/not-started/completion%) and exported `go_live_checklist_status.csv`
  - Extended GS-V10-004 evidence controls with audit-stamped `Record Evidence Capture Event` action and recent capture history in Admin Governance Exports
  - Extended GS-V10-004 with composite `Go-Live Readiness Score` (green/yellow/red) shown in Admin and included in `go_live_summary.csv` plus evidence-capture audit payloads
  - Extended GS-V10-004 readiness controls with runtime-configurable scoring weights/thresholds in Admin so go-live strictness can be tuned per environment
  - Extended GS-V10-004 with System Health `Calibration Sign-Off` capture (Dev/Prod status + owner/date/evidence) and CSV export
  - Extended GS-V10-004 go-live artifact packaging with `system_health_calibration_signoffs.csv` and calibration sign-off status fields in summary/capture payloads
  - Extended GS-V10-004 with System Health `Alert Routing Acceptance Sign-Off` capture (Dev/Prod status + owner/date/evidence + ownership/escalation/runbook confirmations) and CSV export
  - Extended GS-V10-004 go-live artifact packaging with `system_health_alert_routing_signoffs.csv` and alert-routing sign-off status fields in summary/capture payloads
  - Closed GS-V10-004 implementation scope for now (remaining calibration/alert-routing execution evidence remains a go-live operations activity)
  - Extended GS-V10-002 with guided `Live Provider Validation Run` flow in Admin Integrations (safety checks + queue execution + standardized audit evidence + CSV export + recent validation history)
  - Extended GS-V10-004 Go-Live Evidence Pack with `shipping_provider_validation_30d.csv` and provider-validation run counts for release audit completeness
  - Extended GS-V10-002 with Admin `Validation Sign-Off (Dev/Prod)` tracking (owner/date/status/evidence link), exportable CSV, and go-live artifact inclusion (`shipping_provider_validation_signoffs.csv`)
  - Closed GS-V10-002 implementation scope for now (remaining live validation execution/sign-off remains an operational go-live checklist activity)
  - Started GS-V10-005 backup/restore automation with runtime-backed backup policy controls in Admin (`backup_policy_*`, drill interval, RTO target, owner)
  - Extended GS-V10-005 with Admin `Restore Drill Evidence` workflow (audit event capture + SLA age warning + recent history table + CSV export)
  - Extended GS-V10-005 go-live evidence packaging with `backup_restore_drills.csv` and restore-drill summary fields in go-live metadata/capture payloads
  - Extended GS-V10-005 with Admin `DR Checklist + SLA Reporting` (environment-targeted checklist snapshots, per-env SLA rollup, and CSV exports)
  - Extended GS-V10-005 go-live package with `backup_dr_checklist_snapshots.csv` and DR checklist metrics in summary/capture payloads
  - Closed GS-V10-005 implementation scope (backup policy controls, restore-drill evidence, DR checklist/SLA reporting, and go-live artifact packaging are complete in-app)
  - Extended go-live operations tooling with Admin `Go-Live Section Sign-Off Tracker` (per-checklist-item owner/date/evidence/status records + `go_live_section_signoffs.csv` in evidence pack)
  - Extended go-live legal readiness tooling with Admin `Commerce Legal Sign-Off Tracker` (policy-level owner/date/evidence/status for tax treatment, retention, marketplace policy, privacy, financial role controls, and legal/accounting reviewer sign-off) and evidence-pack export (`commerce_legal_signoffs.csv`)
  - Added legal sign-off coverage matrix and one-click `Seed Missing Legal Sign-Off Items` action (`prod` / `dev` / `dev+prod`) to accelerate go-live checklist population
  - Added legal sign-off quick action (`Quick Mark Approved`) from coverage controls for faster policy closure workflows
  - Added purchase-document intake foundation in Lots:
    - upload PDF/image or use camera capture
    - optional AI extraction of invoice fields
    - extraction mode selector (`LLM Multimodal`, `AWS Textract`, or merged `Both`) for per-document OCR/AI strategy
    - immutable original storage in S3 with checksum
    - link documents to lot/product/source for acquisition traceability
  - Added one-click “Apply AI Data To Linked Lot” from purchase-document details to populate lot vendor/date/total/tax/shipping/handling from extracted invoice fields
  - Added runtime-controlled auto-apply for extracted purchase-document fields into linked lots (`purchase_doc_auto_apply_linked_lot_fields`) across both Intake Wizard and Lots upload flows
  - Added explicit audit actions for purchase-document lot normalization (`auto_apply_extracted_fields_to_lot`, `manual_apply_extracted_fields_to_lot`) for governance traceability
  - Added purchase-document lot-apply audit visibility in both Reports and Admin (date-window metrics, detailed table, CSV export)
  - Added “Create Lot + Link Document” flow from purchase-document details for unlinked invoice uploads, prefilled by AI-extracted fields
  - Added “Create Product + Link Document” flow from purchase-document details, prefilled by extracted line-item fields and supporting optional immediate lot assignment
  - Added extracted line-item selector in purchase-document product creation so operators can explicitly choose invoice rows for conversion
  - Added bulk line-item product creation grid in purchase-document details (select/edit multiple extracted rows and create products in one action)
  - Added intake-wizard existing-media attach selectors (product/listing) with quick filter, media-type filter, and select-all/clear controls for bulk reuse
  - Standardized camera UX by collapsing direct camera capture panels by default across intake wizards, tools, and lots
  - Started GS-V10-009 eBay workflow simplification:
    - Created dedicated `eBay Templates` page so template CRUD is separated from daily listing operations
    - Slimmed Listings template section to quick-load + link-out model
    - Added first `Listing Wizard` scaffold page (`pages/26_Listing_Wizard.py`) with in-flow AI draft suggestion apply and guided step structure
    - Scoped next steps: guided Listing Wizard (`Product -> Template -> Policy/Format -> Review -> Publish`), eBay Workspace role split (Auth/Health vs Daily Ops), and clearer publish-state UX
    - Scoped AI-first additions for Listing Wizard: AI title/details draft, AI policy/readiness preflight, AI risk summary, and explicit operator acceptance before publish
    - Added explicit wizard safety controls: unmet-requirements create-button disable reasons, duplicate-draft override gate, and one-click input/state reset to keep daily listing flow clean and predictable
    - Added context-change stale-state protections so switching product/template does not leak prior AI/media selections into a different listing draft
    - Added direct eBay Workspace quick links into `Listing Wizard` and `eBay Templates` so setup/template and drafting flows are reachable without sidebar scanning
    - Tightened Listing Wizard linear UX with explicit `Step X of 9` framing, recommended-flow hint, preflight progress bar, and default-collapsed advanced controls to reduce operator overload
    - Added opt-in AI troubleshooting panels in Listing Wizard (shown only post-run) to reduce mid-flow noise while preserving diagnostics access
    - Compressed AI suggestions review by default (compact previews first, full payload behind explicit toggle) to keep listing flow readable
  - Added stage-aware `Next recommended action` guidance throughout Listing Wizard to improve operator throughput and reduce decision friction
  - Added top-level `Wizard Status` strip in Listing Wizard (product/template/mode/preflight signal) for faster context recovery during edits
  - Added readiness-aware preview expansion in Listing Wizard (auto-open preview only when preflight is fully clean)
  - Closed GS-V10-009 eBay workflow UX simplification:
    - Listing Wizard AI-first assist lane is now complete (title/details generation, readiness risk summary, comps+spot price-band suggestions, and operator price apply mode)
    - Listing Wizard/eBay Workspace split is now the primary operator path for daily eBay work (template setup, draft flow, readiness checks, and publish handoff)
    - Added helper-level regression coverage for Listing Wizard parsing/sanitization (`tests/test_listing_wizard_helpers.py`) and eBay template HTML block persistence/merge flows (`tests/test_listings_helpers.py`)
  - Added Playwright e2e coverage for Listing Wizard category/quantity/preview flow and eBay Templates create/edit/custom-block actions (`tests/e2e/listing_wizard_flow.spec.ts`, `tests/e2e/ebay_templates_flow.spec.ts`)
  - Extended Listing Wizard Playwright flow with direct-post mode/preflight assertions (`Post to eBay Immediately`, post-mode selector, and `Run eBay Dependency Preflight` card visibility) to reduce regressions in direct-post readiness UX
  - Extended Listing Wizard Playwright flow with create-draft direct-post result handling assertions (direct-post skip/failure feedback + post-create `Open Listings` link visibility) to harden post-submit UX coverage.
  - Added Listings Playwright e2e coverage for `eBay Post Mode` selection + dependency preflight card flow, with environment-aware skip when seller-ops controls are intentionally disabled
  - Extended post-close GS-V10-009 eBay publish hardening:
    - Listings now includes in-form `Run eBay Dependency Preflight` and a persistent readiness summary card (pass/warn/fail + checks table) before publish/revise
    - Listings publish now supports explicit post mode (`Publish Live Listing` vs `Create Offer Draft Only`) with draft-link surfacing for Seller Hub review
    - Listings and Listing Wizard now include cache-first category suggestions backed by DB (`ebay_category_suggestions`) with explicit `Refresh from eBay` bypass action
    - Listings category-suggestion apply action now uses queued pending-field updates + rerun for reliable category ID population across fixed-price and auction workflows
    - Listings + Listing Wizard auction payload guardrail now omits `availableQuantity` for `AUCTION` offers (eBay API compatibility)
    - Listings + Listing Wizard now support explicit main eBay image selection; publish/revise/direct-post payloads send the selected image first and persist selected main-image metadata for audit review.
    - Listings + Listing Wizard EPS image handling now stays eBay-hosted only: transient Media API upload/import failures retry, URL import is used only as another EPS hosting path, and failed EPS hosting blocks before inventory publish instead of sending self-hosted image URLs.
    - Listings + Listing Wizard video handling now supports MP4 plus MOV/QuickTime conversion, eBay-required video upload headers, Inventory/Trading video verification, stale `None` selection recovery, and non-blocking image-only warnings when requested video media is missing/unsupported.
    - Added shared payload-construction helper in Listing Wizard direct-post path (`_wizard_build_ebay_offer_payload`) to reduce inline state drift and keep auction/fixed payload behavior aligned with Listings.
    - Listing Wizard and Listings dependency preflight now passes listing format and auction BIN context into eBay policy checks, blocking immediate-payment auction-policy mismatches before live publish.
    - Added explicit regression tests for wizard offer payload construction (`tests/test_listing_wizard_helpers.py`) covering:
      - `FIXED_PRICE` payloads include `availableQuantity` + best-offer terms
      - `AUCTION` payloads omit `availableQuantity` and use auction pricing fields
    - Hardened Listing Wizard category suggestion apply/fetch/refresh/profile-apply flows to use queued pending-field updates + rerun (no post-widget direct state mutation), eliminating recurring Streamlit `cannot be modified after widget is instantiated` failures for category ID.
    - Hardened Listing Wizard direct-post failure UX:
      - wizard now stays in-place on direct-post failure (no immediate redirect after local draft create)
      - failure context is persisted in listing metadata (`direct_post_last_error`, `direct_post_last_error_at`)
      - added staged diagnostics capture (`direct_post_last_error_stage`, `direct_post_last_context`) and in-flow `Last Direct Post Diagnostics` panel for immediate troubleshooting
    - Hardened Listings publish failure UX with staged diagnostics persistence:
      - failed publish attempts now persist `last_publish_error`, `last_publish_error_at`, `last_publish_error_stage`, and `last_publish_error_context` under `ebay_publish` metadata
      - Listings now surfaces `Last eBay Publish Diagnostics` directly in publish flow for fast operator triage
      - added helper regression coverage for publish-error persistence/metadata extraction (`tests/test_listings_helpers.py`)
    - Shared eBay bullion/coin default aspects now include required `Circulated/Uncirculated` starter value (`Uncirculated`) while remaining operator-editable
    - Added eBay publish resiliency fallbacks for common production failures:
      - EPS image upload failure now retries/transitions to EPS URL import when possible and otherwise blocks before inventory publish; direct self-hosted image URL fallback is disabled
      - Inventory upsert system error (`API_INVENTORY` `25001`) now retries once with a safer payload shape
    - Added shared eBay publish dependency verification methods in service layer (`verify_publish_dependencies`) and wired them into wizard + listings flows
    - Added migration `0043_ebay_category_cache` for persisted category suggestion caching and lookup reuse
    - Hardened AI detail-generation quality in Listing Wizard + Listings Copilot:
      - prefer explicit detail keys over marketplace placeholder fields
      - reject weak/too-short/bullet-only AI details
      - auto-generate enriched eBay-ready fallback details when AI detail output quality is insufficient
    - Hardened Listings eBay publish duplicate-offer handling:
      - on eBay `errorId 25002` (`Offer entity already exists`), resolve/reuse existing `offer_id` and revise/update instead of hard-failing create
    - Improved Listing Wizard item-specifics discoverability:
      - `Advanced eBay Fields + Item Specifics` section now opens by default for faster operator edits
  - Started GS-V10-010 auth adapter modernization: replaced legacy cookie manager dependency path with `extra-streamlit-components` adapter and added `cookie_manager_backend` telemetry in Auth Session Debug
  - Closed GS-V10-010 auth adapter modernization: standardized cookie backend selection to `extra-streamlit-components` (no deprecated fallback path), added adapter-selection regression tests in `tests/test_auth.py`, and revalidated Playwright auth smoke (`tests/e2e/auth_flow.spec.ts`)
    - Completed runtime-coupling guardrail for eBay UX: Listing Wizard now surfaces active AI runtime chain/fallback metadata and explicitly avoids hardcoded provider/model assumptions at execution time
    - Split eBay Workspace into explicit workflow lanes (`Auth / OAuth`, `Connection Health`, `Daily Ops`) with lane-specific metrics and quick remediation links
    - Added consolidated `Publish Readiness Summary` card in `Connection Health` with blocker/warning/format-fix metrics and direct remediation actions into Listings/Format Fix/Wizard/Admin Verify
    - Removed duplicate top-of-workspace readiness tables so `Connection Health` remains the single high-signal readiness lane
    - Removed format-fix/ops remediation controls from `Auth / OAuth` lane to enforce clearer setup-vs-operations separation
    - Moved runbook checklist and document handoff actions under `Daily Ops` lane to complete primary lane-boundary cleanup
    - Simplified `Daily Ops` by removing duplicate format-fix controls (centralized in `Connection Health`) and collapsing document handoff by default
  - Added scheduled DB backup automation in sync-runner with runtime-configurable local schedule/timezone and optional S3 upload path (`backup_policy_runner_enabled`, `backup_policy_schedule_timezone`, `backup_policy_schedule_local_time`)
  - Added daily Slack ops report automation in sync-runner with runtime-configurable local schedule/timezone/channel (`slack_daily_report_enabled`, `slack_daily_report_timezone`, `slack_daily_report_local_time`, `slack_daily_report_channel`)
  - Added Admin controls for backup/slack scheduler runtime keys so operators can set local schedule/timezone/channel directly from UI
  - Added global app timezone runtime key (`app_default_timezone`, default `America/Denver`) and aligned scheduler defaults/UI controls to that local timezone baseline
  - Added optional Slack alerts for scheduled backup success/failure with runtime toggles, channel override, and editable templates
  - Scoped next-phase notification routing: optional email delivery (Google integration) for backup/report/system alerts in addition to Slack
  - Scoped future `Goldy in Slack` agent mode: Slack-connected AI assistant that can execute constrained app workflows (reports, comps, listings, ops triage) with approval guardrails
  - Added Admin notification-routing matrix + manual business-status report dispatch (daily/weekly/inventory-risk) to tighten operator visibility and comms cadence
  - Added business-report dry-run preview controls in Admin (resolved route/event/channel + rendered payload text, send-preview-now, payload export)
  - Added daily Slack-report normalized fee-source coverage guardrail:
    - sync-runner computes weekly normalized coverage from reconciliation rows and raises alert context when threshold/consecutive-week criteria are breached
    - coverage diagnostics are persisted into daily-report integration-event details for operational traceability
  - Added Admin runtime controls for daily-report fee-coverage alerting:
    - `slack_daily_report_normalized_fee_coverage_lookback_weeks`
    - `slack_daily_report_normalized_fee_coverage_threshold_pct`
    - `slack_daily_report_normalized_fee_coverage_consecutive_weeks`
  - Added fee-coverage operator visibility surfaces:
    - `Admin -> Integrations -> Slack Notifications` live fee-coverage preview panel
    - `Admin -> System Health` `eBay Fee Coverage Health` card + weekly table
  - Started GS-V10-006 QA automation baseline: added mandatory unit-test step in Docker build workflow and dedicated `qa_tests.yaml` workflow (unit + Playwright smoke on compose app stack with artifact upload)
  - Added GS-V10-006 Playwright scaffolding (`playwright.config.ts`, `tests/e2e/smoke.spec.ts`) and npm QA scripts for local/CI execution
  - Established GS-V10-006 local coverage baseline (`coverage.py`, unit-test run): global Python line coverage is currently `~5%` (2026-03-30 baseline), with expansion plan now prioritized
  - Extended GS-V10-006 QA workflow with enforced dual coverage gates (global baseline `15%` + scoped-core `>=25%` on repository/services/auth/page_common/config) and artifact export (`.coveragerc`, `coverage.xml`, `coverage.json`, `.coverage`) to prevent regression while ratcheting upward
  - Added Playwright intake critical-path coverage with `Inventory Intake Wizard` e2e create-product flow (`tests/e2e/intake_wizard.spec.ts`)
  - Added Playwright `Coin Intake Wizard` critical-path create-product flow (`tests/e2e/coin_intake_wizard.spec.ts`) with auth-aware local skip behavior
  - Added Playwright `Products` critical-path create/edit/media flow (`tests/e2e/products_flow.spec.ts`) with auth-aware local skip behavior
  - Added Playwright auth critical-path sign-in/sign-out flow with remember-token URL assertion (`tests/e2e/auth_flow.spec.ts`) and auth-aware local skip behavior
  - Added Playwright `Listings` critical-path draft/review preflight flow (`tests/e2e/listings_flow.spec.ts`) covering create draft listing + readiness preview + review approval with auth-aware local skip behavior
  - Added Playwright `Shipping` critical-path queue surface flow (`tests/e2e/shipping_flow.spec.ts`) covering shipping queue tabs and label-purchase queue controls with auth-aware local handling
  - Added Playwright `Sync` critical-path triage surface flow (`tests/e2e/sync_flow.spec.ts`) covering sync controls/execute-now visibility and exception queue presence with auth-aware local handling
  - Added Playwright `Admin` go-live governance surface flow (`tests/e2e/admin_golive_flow.spec.ts`) covering readiness/sign-off/evidence UI entry points with auth-aware local handling
  - Stabilized local Playwright execution for shared-auth flows by defaulting to serialized workers (`workers=1`, `fullyParallel=false`) and preserving query-auth token on cross-page navigation in specs
  - Verified Playwright Chromium operator suite green locally (`9 passed`) after auth-form/session selector hardening
  - Added unit-test coverage for purchase-document extraction service (`tests/test_purchase_doc_extraction.py`) covering Textract summary/line-item mapping and LLM+Textract merge behavior
  - Expanded GS-V10-006 core test suite with new `sync_jobs`, `integration_queue`, `integration_automation`, `runtime_settings`, `ebay`, `shipping_labels`, `auth`, `slack_notify`, `security/validation`, `google_workspace`, `sync_runner`, `spot_price`, `ai_orchestration`, `listing_orchestration`, `llm_runtime`, `config_health`, `coin_reference_sources`, `env_manager`, `db_backup`, `voice_runtime`, `listing_readiness`, `media_storage`, `chat_context_builders`, `db` helper wrappers, `utils.time`, `page_common`, `main` entrypoint, and `ui_helpers` unit tests plus deeper repository edge-case tests for orders/listings/reviews/sales/media/sources/users/sync-automation queues, improving measured global coverage to ~20.17% (`314` tests passing)
  - Added deeper `sync_jobs` coverage for order-item mapping and eBay upsert/shipping-push edge paths (`_build_order_items` + `_upsert_ebay_order_into_local` + shipping API exception partial-status path), raising `app/services/sync_jobs.py` coverage to ~91.07%
  - Added deeper auth helper coverage (cookie-manager status and encrypted-cookie token flows, bootstrap/session guard rails, invalid token shape parsing, and restore/user-default paths), raising `app/auth.py` coverage to ~53.92%
  - Added deeper Slack + queue-failure coverage (Slack API validation/error/connectivity branches and integration terminal-alert/provider-disabled branches), raising `app/services/slack_notify.py` to ~95.42% and `app/services/integration_queue.py` to ~93.09%
  - Added full spot-price provider coverage (metals-api + Yahoo parsing/retry/rate-limit paths), raising `app/services/spot_price.py` to ~92.62%
  - Added deeper repository helper coverage (media/source/lot/audit/user/role-permission branches plus sync/integration CRUD and queue resolution paths), raising `app/repository.py` to ~58.63%
  - Added additional repository runtime/admin domain coverage (AI provider config CRUD/defaults, runtime settings CRUD/list/get/delete, saved-filter admin transfer/delete, eBay listing template profile CRUD/validation, and document-template include-all filtering), raising `app/repository.py` to ~73.55%
  - Added orchestration helper coverage for AI and channel adapters, bringing `app/services/ai_orchestration.py` to `100%` and `app/services/listing_orchestration.py` to ~`98.28%`
  - Added deeper `llm_runtime` coverage for runtime connectivity validation, model endpoint discovery, multimodal validation/parsing/vision-capability guardrails, and multimodal fallback sequencing, raising `app/services/llm_runtime.py` to ~`83.61%`
  - Added full coverage for `config_health` and `coin_reference_sources` service modules (both now `100%`) to harden runtime governance and paid-source adapter guardrails
  - Added `env_manager` parsing/masking/upsert/default coverage, raising `app/services/env_manager.py` to ~`96.70%`
  - Added `db_backup` coverage for pg_dump/psql guards and S3 backup wrapper paths, raising `app/services/db_backup.py` to `100%`
  - Added `voice_runtime` coverage for config resolution, endpoint candidates, STT/TTS success/guardrails, and fallback errors, raising `app/services/voice_runtime.py` to ~`97.96%`
  - Added `listing_readiness` coverage for fixed-price/auction blocker rules, status warnings, and scoring outcomes, raising `app/services/listing_readiness.py` to ~`95.79%`
  - Added `media_storage` coverage for S3 init/ensure/upload/fetch flows with URL/content-type/error branches, raising `app/services/media_storage.py` to ~`96.25%`
  - Added `chat_context_builders` coverage for domain snapshot aggregation (inventory/listings/sales/shipping/sync/orders/reports/admin) and fallback-help paths, raising `app/services/chat_context_builders.py` to ~`98.17%`
  - Added dedicated `grading_standards` service coverage (helper parsing, web snapshot indicators, grading/comp context builders), raising `app/services/grading_standards.py` to ~`97.16%`
  - Added quick-action/sidebar navigation coverage for `page_common` (unified/legacy flows, command handling, navigation guardrails), raising `app/page_common.py` to ~`91.67%`
  - Added `auth` sidebar/login branch coverage for password-auth success/invalid and non-password override flows, raising `app/auth.py` to ~`71.07%`
  - Added `ai_text` parser/normalizer coverage and `media_storage` delete-object branch coverage (`ai_text` ~`87.77%`, `media_storage` ~`96.77%`)
  - Added repository coverage for document/artifact/coin-reference/dashboard/integration-event domains (including validation/error branches), raising `app/repository.py` to ~`73.06%`
  - Latest measured QA baseline on 2026-04-02: global Python line coverage `~41.77%` (`604` tests), scoped-core `~95.01%`
  - Ratcheted QA workflow coverage gates to global `>=30%` and scoped-core `>=85%` (`repository/services/auth/page_common/config`) for stronger regression protection
  - Added focused seed/compatibility coverage tests (`app/db/seed.py`, `app/views.py`) to reduce blind spots in local dev bootstrap and legacy import paths
  - Added stubbed migration CLI tests (`app/db/migrate.py`) to validate command dispatch and strengthen migration tool reliability in CI without requiring external Alembic runtime
  - Expanded repurchase/inventory-cycle repository coverage (`record_product_repurchase` weighted-cost and lot-assignment branches), improving core data-path confidence
  - Expanded conversion/update product repository coverage (`update_product` eBay/coin-ref validation and single/bulk inventory conversion flows), raising `app/repository.py` coverage to `~82.62%`
  - Added deeper auth edge-path coverage (cookie/query token error and fallback branches, remember-token mismatch handling, RBAC load success/failure paths, and auth-session warning path), raising `app/auth.py` coverage to `~92.81%` and `app/repository.py` to `~85.32%`
  - Raised shipping/scheduler service coverage with new edge-path tests (`shipping_labels` now `~99.17%`, `sync_runner` now `~98.02%`) and improved suite baseline to `411` passing tests
  - Completed deep `integration_automation` branch coverage (approval/execution gates, payload/effect parse error paths, preview/simulate edge handling), bringing `app/services/integration_automation.py` to `100%`
  - Expanded `llm_runtime` branch/error-path tests (DB/env resolution fallback branches, endpoint/model parsing variants, multimodal fallback failures, and chat/responses no-text guards), raising `app/services/llm_runtime.py` coverage to `~91.80%`
  - Added dedicated shared-view helper coverage suite (`tests/test_views_shared.py`) covering media capture/gallery/access, handoff/export helpers, byte-loading paths, and eBay push history actions, raising `app/components/views/shared.py` to `~64.62%`
  - Added a broad view-module import-coverage suite (`tests/test_views_module_imports.py`) that safely boots and imports core workspace pages under stubbed external dependencies, lifting global baseline while preserving test determinism
  - Added behavioral view tests for Orders/Sales side-panel flows (`tests/test_views_small.py`), lifting `app/components/views/orders.py` to `~65.70%` and `app/components/views/sales.py` to `~71.36%`
  - Added helper-level parsing/runtime tests for Lots and System Health modules (`tests/test_lots_helpers.py`, `tests/test_system_health_helpers.py`) to harden utility branches and lift global baseline further
  - Added helper/utility coverage for Sync + Shipping + Operations Home (`tests/test_sync_shipping_helpers.py`, `tests/test_operations_home_helpers.py`) and a minimal Sync render-flow execution path in `tests/test_views_small.py`, lifting `app/components/views/sync.py` to ~`33.79%`
  - Added focused render-path tests for Shipping read-only permission flow, shipping queue/export helper flows, and Operations Home queue rendering plus shipping export format checks (`tests/test_views_small.py`, `tests/test_sync_shipping_helpers.py`), lifting `app/components/views/shipping.py` to ~`31.29%`, `app/components/views/operations_home.py` to ~`48.06%`, and global coverage above `30%`
  - Added dedicated `workspace_shell` helper coverage (`tests/test_workspace_shell.py`) for status semantics, feedback/task completion logging, and eBay command-rail state handling, lifting `app/components/views/workspace_shell.py` to ~`90.00%` and global coverage to ~`30.72%`
  - Added populated Sync render-path + copilot tests (`tests/test_views_small.py`, `tests/test_sync_shipping_helpers.py`) covering run detail/exception queue/coplilot summarization paths; raised `app/components/views/sync.py` to ~`63.30%` and global coverage to ~`31.19%`
  - Added System Health render guard tests (`tests/test_system_health_helpers.py`) for permission-stop and refresh-rerun paths, lifting `app/components/views/system_health.py` to ~`11.11%` while maintaining deterministic non-Streamlit-run execution
  - Added full System Health render smoke coverage with stubbed Streamlit/repo integrations (`tests/test_system_health_helpers.py`), lifting `app/components/views/system_health.py` to ~`54.75%` and global coverage to ~`32.06%`
  - Added dedicated reports-helper coverage suite (`tests/test_reports_helpers.py`) for tax presets and cycle/rebuy/review/publish-outcome builders, lifting `app/components/views/reports.py` to ~`48.43%` and global coverage to ~`33.60%`
  - Added documents-helper coverage suite (`tests/test_documents_helpers.py`) for logo resolution, item/tax derivation, and retained-artifact render/download branches, lifting `app/components/views/documents.py` to ~`13.77%` and global coverage to ~`33.87%`
  - Added tools-helper coverage suite (`tests/test_tools_helpers.py`) for coin CSV normalization, parsing/repair, comp math/confidence, domain/price extraction, and AI media persistence helpers, lifting `app/components/views/tools.py` to ~`20.00%` and global coverage to ~`35.47%`
  - Added AI Chat helper coverage suite (`tests/test_ai_chat_helpers.py`) for domain token parsing, role/domain allowlist resolution, write-intent detection, sensitive masking rules, router domain gating, and Goldy planner helpers, lifting `app/components/views/ai_chat.py` to ~`35.08%` and global coverage to ~`35.86%`
  - Added eBay Ops helper coverage suite (`tests/test_ebay_ops_helpers.py`) for offer/details parsing, defaults merge, offer-id resolution fallback logic, policy/location labeling, and listing filter logic, lifting `app/components/views/ebay_ops.py` to ~`16.86%` and global coverage to ~`36.22%`
  - Added eBay Integration render-path coverage (`tests/test_ebay_view.py`) for not-configured guard and pull/import success flow with sync execution assertions, lifting `app/components/views/ebay.py` to ~`57.89%` and global coverage to ~`36.46%`
  - Added Documents render-path coverage (`tests/test_documents_view.py`) for source-type early-return branches (`Order`/`Sale`/`Listing` when empty), lifting `app/components/views/documents.py` to ~`23.45%` and global coverage to ~`36.71%`
  - Added intake-wizard helper coverage (`tests/test_intake_wizard_helpers.py`) for coin/inventory JSON extraction, media-buffer handling, and buffered upload wrappers, lifting `coin_intake_wizard.py` to ~`16.86%`, `inventory_intake_wizard.py` to ~`10.18%`, and global coverage to ~`37.17%`
  - Expanded documents-helper coverage (`tests/test_documents_helpers.py`) for classic-default logo resolution, tax-basis edge modes, and retained-artifact missing-selection handling, lifting `app/components/views/documents.py` to ~`24.85%` and global coverage to ~`37.21%`
  - Expanded eBay Ops helper coverage (`tests/test_ebay_ops_helpers.py`) for no-offer/no-match/cache-hit resolution and fallback label parsing, lifting `app/components/views/ebay_ops.py` to ~`17.55%` and global coverage to ~`37.23%`
  - Added listing outcome mutation-path coverage in `tests/test_documents_view.py` (Documents `Listing` -> `Not Sold` end/archive actions), lifting `app/components/views/documents.py` to ~`40.84%` and global coverage to ~`37.65%`
  - Added sold-posting and runtime-policy-blocked coverage in `tests/test_documents_view.py` (Documents `Listing` -> `Sold` create-order/sale/artifact path and blocked-post guardrail), lifting `app/components/views/documents.py` to ~`52.04%` and global coverage to ~`37.95%`
  - Added Google integration failure-queue coverage in `tests/test_documents_view.py` (Gmail/Calendar/Drive submit failure -> queue retry job branches), lifting `app/components/views/documents.py` to ~`62.19%` and global coverage to ~`38.21%`
  - Added eBay Ops profile-apply render-path coverage (`tests/test_ebay_ops_helpers.py`) for saved-profile quick switch propagation (token/filter/date) and rerun behavior, raising `app/components/views/ebay_ops.py` to ~`33.26%` and global coverage to ~`38.82%` (`549` tests)
  - Added Sync render guard coverage in `tests/test_views_small.py` for execute-now disabled-job rejection and disabled-queued-run finalize confirmation guard, raising `app/components/views/sync.py` to ~`67.77%` while keeping suite stable at `549` passing tests
  - Added Operations Home governance queue render coverage in `tests/test_views_small.py` (single-queue empty-state and cadence follow-up resolve action path), raising `app/components/views/operations_home.py` to ~`51.94%` and global coverage to ~`38.82%` (`549` tests)
  - Added dedicated Admin helper coverage suite (`tests/test_admin_helpers.py`) for audit parsing, migration history/revision helpers, env/runtime defaults coverage builders, and runtime seed paths
  - Added Operations Home blocker-queue open-workflow render-path test (`tests/test_views_small.py`) and expanded queue action branch coverage
  - Added targeted sync/shipping guardrail tests (`tests/test_sync_shipping_helpers.py`) for retry eligibility edge-cases and shipping queue submit/exception-resolution branches
  - Added additional sync lineage and shipping update error-path tests (`tests/test_sync_shipping_helpers.py`) covering orphan/cycle retry ancestry and mixed-success bulk shipping updates
  - Added dedicated Reports render-path coverage (`tests/test_reports_view.py`) including full empty-state run plus Reports Copilot permission-denied and success flows, raising `reports.py` to `~71.13%`; latest suite is `585` tests with global coverage at `~41.43%`
  - Expanded Documents render-flow coverage (`tests/test_documents_view.py`) for prefill/session controls (`Clear Prefill`, handoff `Clear History`, and `Reopen Selected Handoff`), raising `documents.py` to `~67.56%`
  - Expanded `page_common` quick-action unified-nav error-path coverage (`tests/test_page_common.py`) for role-default and workflow-link failures, raising `page_common.py` to `~95.63%`
  - Expanded repository edge-path coverage (`tests/test_repository_inventory_movements.py`) for purchase-lot update/not-found/no-op, create-product eBay/coin-ref validation paths, missing-product sale no-movement path, and lot-allocation fallback branches; raised `repository.py` to `~87.65%` and fixed audit serialization for nested `Decimal` values via `_record_audit(..., default=...)`
  - Expanded repository upsert/update coverage for eBay listing template profiles and saved filter profiles (`tests/test_repository_inventory_movements.py`), lifting `repository.py` to `~89.40%` and scoped-core to `~92.90%`
  - Added additional repository API-contract coverage for purchase-document/source no-op/unknown-field paths and reconciled tests to current purchase-document creation signature (`document_kind` + storage metadata), lifting `repository.py` to `~90.19%` and scoped-core to `~93.56%`
  - Expanded `llm_runtime` branch/error-path coverage for runtime setting exception/unknown-value fallbacks, fallback all-fail path, model endpoint candidate variants, dedupe/no-rows chain behavior, multimodal empty-image branches, and direct responses `output_text` handling, raising `app/services/llm_runtime.py` to `~96.52%`
  - Expanded repository branch coverage for shipping preset/listing template/AI provider/runtime setting/shared-filter ownership/order-update/app-user password-update paths, raising `app/repository.py` to `~93.47%` and scoped-core coverage to `~94.85%`
  - Completed scoped-core QA gate milestone by expanding repository branches (purchase-lot updates, app-user no-change/update unknown-field, media update unknown-field), raising `app/repository.py` to `~93.97%` and scoped-core coverage to `~95.01%`
  - Hardened Playwright operator flows with deeper mutation assertions:
    - Listings review action now asserts approved review-state evidence.
    - Sync execute-now test now validates result/disabled states (instead of surface-only checks).
    - Shipping flow now validates bulk-update form guardrail messaging.
    - Shipping flow now includes deterministic carrier-preset create/save mutation assertion for non-empty and empty queue environments.
    - Sync flow now includes deterministic run-detail counter/status update assertion (`Update Sync Run`) independent of exception queue population.
    - Admin go-live flow now includes deterministic `Go-Live Section Sign-Off` form mutation assertion (`Record Checklist Item Sign-Off`) with success + row visibility validation.
    - Added deterministic e2e role-permission seeding (`E2E_ENSURE_ROLE_PERMISSIONS=true`) and coin-intake navigation hardening; current local e2e status: `9/9` passing.
  - Added QA CI evidence packaging in `qa_tests.yaml`: dedicated `qa-evidence` artifact (`qa_evidence.md` + `qa_evidence.json`) plus GitHub job summary output for direct go-live checklist linkage.
  - Extended QA evidence packaging with explicit coverage-gate trend artifact (`qa-evidence/coverage_gates.json`) and gate snapshot in summary output (global/scoped thresholds + pass/fail) to harden ratchet tracking evidence.
  - Added segmented suite manifest evidence export in CI (`qa-evidence/suite_fast_manifest.json`, `qa-evidence/suite_integration_manifest.json`) using enhanced `scripts/run_test_suites.py` list-only/json modes for deterministic suite split tracking and parallelization planning.
  - Added QA suite segmentation + deterministic integration test helpers:
    - New `scripts/run_test_suites.py` supports `fast` and `integration` unittest selectors for CI fan-out.
    - Added reusable test support helpers (`tests/test_support.py`) with deterministic SKU suffix generation and shared in-memory repository fixture/factory wiring.
    - Added local convenience targets: `make qa-unit-fast`, `make qa-unit-integration` (and `task` equivalents).
  - Ratcheted CI QA coverage gates in `.github/workflows/qa_tests.yaml` to global `>=30%` and scoped-core `>=85%` (from baseline `25/25`) and aligned README/QA docs/checklists
  - Revalidated Playwright critical-path suite locally after QA/auth-session hardening (`2026-04-09`): `9 passed / 0 skipped`, including deterministic admin-go-live governance flow execution.
  - Standardized local Playwright command guidance on `npm run test:e2e` wrapper (instead of direct `npx playwright test`) for consistent env handling in local/operator shells.
  - Added GS-V10-006 phased quality-gate plan:
    - Phase 1: enforce baseline reporting + minimum gates (`>=30%` global, `>=85%` scoped-core) and fail build below threshold
    - Phase 2: raise to `>=40%` global while adding core flow Playwright coverage
    - Phase 3: raise to `>=55%` global and enforce scoped-core `>=95%` target
    - Phase 4: continue ratchet toward near-100% on core modules and higher global target post-go-live
  - Started GS-V10-011 workflow-state persistence scope definition:
    - documented migration path from session-heavy business state to DB-backed drafts/events
    - scoped first rollout target on Listing Wizard and Listings publish/edit flows
    - scoped follow-on rollout to eBay Workspace and intake wizards with autosave/resume behavior
  - Implemented GS-V10-011 first delivery slice:
    - added DB-backed workflow persistence tables (`workflow_drafts`, `workflow_events`) and migration `0044_workflow_state`
    - added repository workflow draft/event APIs for load/save/resume/clear/event logging
    - Listing Wizard now supports explicit `Save Workflow Draft`, `Resume Saved Draft`, `Clear Workflow Draft`, and draft autosave on tracked state change
    - Listing Wizard resume restores saved product/template context from draft payload (reducing stale session-only flow drift)
  - Extended GS-V10-011 into Listings eBay publish/edit flow:
    - added per-listing draft scope persistence with `Save/Resume/Clear Publish Draft` controls
    - added autosave for publish/edit form state (category/pricing/aspects/policies/shipping/post-mode/manage offer fields)
    - switched category/item-specific apply actions to pending-update + rerun flow to reduce session-state drift after aspect-default actions
  - Extended GS-V10-011 into eBay Workspace auth/setup controls:
    - added scoped setup draft persistence with explicit `Save/Resume/Clear Setup Draft` controls
    - added autosave for workspace auth/setup state (token/filter/store-policy/location/create-location/package defaults)
    - added safe date-range restore coercion to reduce rerun-time widget state/type failures
  - Extended GS-V10-011 into intake wizards:
    - added initial `Save/Resume/Clear Draft` + autosave for key-backed Coin Intake and Inventory Intake wizard state
    - persisted AI/prefill/buffered-media context to reduce rerun/restart loss in intake-assisted flows
    - expanded intake draft payload coverage to core long-form keyed form state (item/inventory/cost/tax/shipping/lot/AI/draft-handoff), improving restart-safe resume parity
    - added helper-state persistence for media selector/search and capture-mode controls while explicitly excluding binary upload buffers from persisted draft payloads
    - added explicit resume warnings for non-resumable local media buffers when reopening saved drafts after restart/device change
  - Extended GS-V10-011 with workflow-state governance operations:
    - added repo-level workflow retention cleanup (`cleanup_workflow_state`) and draft listing API (`list_workflow_drafts`)
    - added Admin Governance Exports panel for workflow drafts/events preview + CSV export + one-click retention cleanup
    - added workflow-state retention coverage for cleanup behavior (expired-active drafts, stale events, active-draft preservation) in `tests/test_workflow_state.py`
    - hardened Listing Wizard workflow-state Playwright spec to skip gracefully when target environment has not applied `workflow_drafts` migration (prevents false-negative CI/local failures from schema drift)
  - Completed GS-V10-011 documentation/runbook closeout:
    - README now includes explicit workflow-state operator runbook (`Save/Resume/Clear`, restart-safe expectations), Admin governance cleanup runbook, and retention policy guidance for DB drafts/events vs session-only UI state.
  - Expanded GS-V10-011 draft-contract regression coverage:
    - strengthened `tests/test_listing_workflow_draft_parity.py` to assert parity for category/aspects/pricing plus quantity, best-offer thresholds, volume pricing JSON, package weight, shipping cost, and post-mode fields across Listing Wizard and Listings publish/edit draft payloads.
  - Hardened GS-V10-011 e2e stabilization path for environment variance:
    - updated `tests/e2e/listings_flow.spec.ts` to use visibility guards and skip semantics when review/publish controls are unavailable due to environment preconditions (auth/role/data), reducing false-negative failures while keeping successful-path assertions intact.
    - added deterministic Listings e2e fixture creation/selection (per-test unique listing titles) to reduce reliance on preexisting listing rows before review/publish assertions.
    - fixed auth helper sign-in completion heuristic (`tests/e2e/_auth.ts`) to avoid false negatives when signed-in state is present but a `Sign In` control remains visible in the UI tree.
    - non-prod seed now enforces runtime `ebay_allow_sandbox_seller_ops=true` for stable sandbox eBay preflight control availability in e2e flows.
    - Playwright global setup now allows local restricted-shell fallback (continue without seed unless `PLAYWRIGHT_REQUIRE_SEED=1`) while preserving strict CI behavior.
    - seed data now includes deterministic eBay draft fixture (`EBAY-LIST-E2E-DRAFT`) and Listings e2e spec falls back to that fixture for review/publish selection paths.
    - added strict seeded Listings e2e gate (`tests/e2e/listings_flow_strict.spec.ts`) with hard assertions and explicit env gating (`E2E_STRICT_LISTINGS=1`) for closeout evidence runs.
    - added strict Listings execution wiring in tooling/CI (`npm run test:e2e:listings:strict`, optional workflow toggle `QA_ENABLE_STRICT_LISTINGS=1`).
  - GS-V10-011 (P0) closed (April 13, 2026): workflow-state modernization closeout complete (DB-backed drafts/events across targeted flows, retention/governance coverage, parity tests, and strict seeded Listings Playwright gate wiring).
  - GS-V10-006 ratchet update (April 13, 2026):
    - coverage gates increased to global `>=35%` and scoped-core `>=86%` across CI + local QA commands (`qa_tests.yaml`, `Makefile`, `Taskfile.yml`)
    - latest measured baseline: `743` tests passing, global `~38.46%`, scoped-core `~86.87%`
    - added focused workflow-contract fallback/error-path tests (`tests/test_workflow_contracts.py`) to harden draft-contract extraction behavior and increase scoped-core headroom
    - added focused eBay health + fee-reconciliation edge-path tests (`tests/test_ebay_health.py`, `tests/test_reports_fee_reconciliation.py`) to further increase scoped-core margin
  - GS-V10-006 follow-on ratchet update (April 13, 2026):
    - coverage gates increased to global `>=36%` and scoped-core `>=87%` across CI + local QA commands
    - latest measured baseline: `749` tests passing, global `~38.53%`, scoped-core `~87.17%`
    - expanded `ebay_aspects` service coverage to `100%` (`tests/test_ebay_aspects.py`) for additional scoped-core margin
  - GS-V10-006 scheduler-coverage update (April 13, 2026):
    - expanded `sync_runner` helper/scheduler/health-check branch coverage (`tests/test_sync_runner.py`) from ~64.78% to ~80.11%
    - latest measured baseline: `754` tests passing, global `~38.66%`, scoped-core `~87.69%`
    - coverage gates ratcheted to global `>=37%` while maintaining scoped-core `>=87%`
  - GS-V10-006 sync-jobs-coverage update (April 13, 2026):
    - expanded `sync_jobs` helper/fallback branch coverage (`tests/test_sync_jobs.py`) including token persistence/refresh guards, slack-notify exception path, shipping-service/address fallbacks, and line-level fee/shipping helper branches
    - latest measured baseline: `761` tests passing, global `~38.81%`, scoped-core `~88.29%`
    - coverage gates ratcheted to global `>=38%` and scoped-core `>=88%`
  - GS-V10-013 listing workflow contract/state coverage update (April 13, 2026):
    - expanded DB-first workflow-state and listings helper contract parity tests (`tests/test_workflow_state.py`, `tests/test_listings_helpers.py`)
    - latest measured baseline: `770` tests passing, global `~38.92%`, scoped-core `~88.45%`
  - High-priority remaining (as of 2026-04-13):
    - GS-V10-009 follow-on (P0): production direct eBay draft-posting validation is complete from small-scale rollout; targeted e2e direct-post preflight coverage is now CI-fixtured (seller-ops enabled in sandbox), with ongoing telemetry trend monitoring remaining.
    - GS-V10-006 (P0): raise coverage gate beyond baseline and continue core-module + operator-critical e2e expansion.
    - GS-V10-010 (P0): monitor auth-session adapter behavior in target environments and keep deterministic admin-go-live e2e coverage green as part of release gating.
    - Go-live comp evidence (P0) closed (April 15, 2026): production proof captured for both normal sold-comp and cooldown/fallback comp behaviors, with accepted fallback policy documented in `GO_LIVE_CHECKLIST.md`.
    - GS-V10-012 (P0) closed (April 13, 2026): eBay fee-economics lifecycle implemented and operationally validated in small-scale production live testing (estimate/reconciliation/reporting/sign-off).
      - Progress: eBay pull/import now hydrates full order detail per order before local upsert, improving captured pricing/buyer/shipping fidelity feeding fee reconciliation and Slack/report outputs.
      - Progress: order-level shipping economics now include internal actual label spend (`orders.shipping_label_cost/currency`) captured during eBay sync (with sale-level fallback), and order exports now include charged-vs-actual shipping deltas for P/L traceability.
      - Progress: Reports now include `Order Actual Economics Allocation` and actual-economics margin columns (`actual fee alloc`, `actual shipping alloc`, `actual net before COGS`, `fifo/lot actual margin`) so product/channel/period profitability can use order-level actuals instead of charged-shipping proxies.
      - Progress: eBay order payload persistence now includes finance transaction context (`_finance_transactions`) and Reports now exposes normalized fee-type attribution from `orderLineItems[].marketplaceFees[]` (`Fee Detail`, `Fee Summary`, `Fee by SKU`, `Fee by Category`).
      - Progress (April 15, 2026): added normalized `order_finance_entries` persistence and sync ingestion for eBay finance transactions (marketplace-fee rows + shipping-label debit rows), and Reports now prefers this DB-first source for fee detail output.
      - Progress (April 15, 2026): eBay fee reconciliation actual-fee source now prefers normalized `order_finance_entries` marketplace-fee sums per order, then falls back to notes-derived and sale-field sources.
      - Progress (April 15, 2026): Reports now surface an `Actual Fee Source Priority` summary (normalized/notes/sale fallback) for reconciliation quality visibility.
      - Progress (April 27, 2026): Dashboard live metrics and Ask/AI sales/report snapshots for net, estimated profit/margin, fees, and label spend now use Reports-aligned sale-window actual-economics rows; linked normalized marketplace-fee and shipping-label finance rows are allocated once per order and unlinked finance rows no longer inflate profit inputs.
      - Progress (April 27, 2026): Dashboard all-time `Net Sales`, Sales table rows, and Sales Copilot context now expose/use actual fee, actual label spend, actual net, and source columns from repository actual-economics rows; Reports COGS/margin rows use actual net before COGS for primary margin/close-readiness math when available while retaining field net for audit comparison.
      - Progress (April 27, 2026): marketplace reconciliation rows now use repository actual-economics rows for sales fees, shipping charged, label spend, sales net before returns, and net after returns.
      - Progress (April 27, 2026): inventory-cycle analytics now use repository actual-economics rows for cycle fees, shipping charged, label spend, net sales, and estimated margin versus known acquisition cost.
      - Progress (April 27, 2026): Reports `Sales` export rows now expose raw field net plus actual fee, actual shipping charged, actual label spend, actual net before COGS, and actual source columns from repository actual-economics rows.
      - Progress (April 27, 2026): estimated tax detail rows now use repository actual-economics allocated buyer shipping when available, while retaining raw sale shipping and source evidence columns for review.
      - Progress (April 27, 2026): Reports `Orders` export rows now carry actual fee/label/net/source columns from normalized order finance entries while preserving raw order fee/label fields for audit comparison.
      - Progress (April 27, 2026): Orders workspace table rows and Orders Copilot context now surface normalized actual fee/label/net/source fields from repository order rollups.
      - Progress (April 27, 2026): Reports shipping-economics rows now use the same repository actual-economics allocation as dashboard/profit rows, preventing even-split label spend from disagreeing with gross-weighted multi-line order profit math.
      - Progress (April 27, 2026): Purchase Lot `Lot P/L Snapshot (Estimated)` now uses repository actual-economics rows for product sales net/profit attribution, keeping lot-level estimated profit aligned with dashboard and Reports when normalized marketplace fees/labels exist.
      - Progress (April 27, 2026): legacy in-memory Reports inventory-cycle fallback now accepts actual-economics rows for fee/shipping/label/net calculations, so degraded report paths retain the same accounting basis.
      - Progress (April 27, 2026): legacy in-memory marketplace reconciliation fallback now uses actual-economics rows when the repository reconciliation rollup is unavailable, keeping fallback reconciliation packets aligned with normalized fee/label/net math.
      - Progress (April 27, 2026): Reports `Sales Detail` dataframe/export now keeps raw field net beside actual fee/shipping/label/net/source columns and uses actual-backed `net_sales` where actual-economics evidence exists.
      - Progress (April 27, 2026): Dashboard no-rollup fallback now uses repository actual-economics rows for 7d/30d fee, shipping, label spend, net, shipping delta, and estimated profit when `dashboard_live_metrics` is unavailable.
      - Progress (April 27, 2026): standalone eBay fee reconciliation fallback now prefers normalized `order_finance_entries` marketplace-fee rows from `order.finance_entries`, keeping fee-source fallback behavior aligned with repository reconciliation.
      - Progress (April 27, 2026): daily Slack ops report 24h gross/net now uses repository actual-economics rows when available and canonical shipping economics as raw fallback.
      - Progress (April 28, 2026): Reports accounting exception rows now use normalized actual fee evidence before raw `sales.fees`, preventing false missing-fee and nonpositive-margin exceptions when linked finance entries prove the fee.
      - Progress (April 28, 2026): Dashboard live metrics now use actual-economics rows for 7-day net sales, so short-window dashboard net uses the same normalized fee/label evidence as 30-day net/profit.
      - Progress (April 28, 2026): Admin manual business status report context now uses actual-economics rows for gross, fees, buyer shipping, label spend, and net before raw field fallback.
      - Progress (April 28, 2026): Ask/AI accounting context fallback paths now roll back failed actual-economics and FIFO cost-map lookups before continuing with sale/product field fallbacks.
      - Progress (April 15, 2026): Dashboard now surfaces normalized eBay fee attribution (`ebay_fees_30d_total` + fee-type breakdown) from `order_finance_entries`.
      - Progress: Admin now includes `eBay Fee Calibration Sign-Off Tracker` (Dev/Prod) and Go-Live evidence-pack export integration (`ebay_fee_calibration_signoffs.csv`) for finance acceptance traceability.
    - Go-live ops closeout update (April 13, 2026): GS-V10-002 / GS-V10-003 / GS-V10-004 / GS-V10-005 operational validation completed via small-scale production live testing; continue normal evidence-capture cadence.
    - Go-live evidence/sign-off closeout update (April 15, 2026): checklist evidence rows and final engineering/operations/business-owner sign-offs were recorded in `GO_LIVE_CHECKLIST.md`.
  - Added next-scope execution plan for workflow speed + reliability:
    - GS-V10-013 (P0): DB-first listing workflow unification
      - unify Listing Wizard + Listings publish/edit state under one DB-backed draft contract (single source of truth)
      - persist preflight blockers/warnings as structured draft records (resume-safe, operator-auditable)
      - reduce session-state-only mutation paths to minimize rerun race/ordering failures
    - GS-V10-014 (P0): database reliability/performance hardening
      - add hot-path composite indexes (sync runs, listings lifecycle, workflow drafts/events, audit timelines)
      - add notification outbox table + retry/idempotent dispatch worker path for Slack/email events
      - add dashboard/report rollup strategy for fast reads under production load
    - GS-V10-015 (P1): AI/LLM operations management layer
      - split per-workflow AI runtime profiles (listing copy, intake extraction, comp analysis, risk checks)
      - add AI quality gates before apply (minimum detail shape, required sections, policy/risk checks)
      - add prompt/version registry with acceptance telemetry (`accepted as-is` vs `human-edited`) for continuous tuning
    - GS-V10-016 (P1): economics intelligence loop
      - unify listing-time estimate + order actuals for fees/shipping into one P&L fact model
      - add expected-net score in listing flows before publish/draft decision
      - add KPI drilldowns by SKU/channel with configurable threshold alerts
      - Progress (April 20, 2026): added repository rollup `report_economics_intelligence_fact_rows` and Reports export `Economics Intelligence Facts (Estimate vs Actual)` for first-pass per-sale estimate-vs-actual fee/shipping/net variance analysis.
      - Progress (April 20, 2026): added `Expected Net Score (Pre-Publish)` cards in Listing Wizard + Listings publish workspace (fee estimate + landed cost + local fulfillment estimate) to improve before-publish pricing decisions.
      - Progress (April 20, 2026): added Reports `Economics Intelligence Drilldowns + Alerts` with threshold-driven grouped alerting (`by SKU`, `by Marketplace`) and row-level alert export for operator triage.
      - Progress (April 20, 2026): added Admin `Economics Threshold Sign-Off Tracker` (Dev/Prod coverage, seed/quick-approve/form, CSV) and wired its status into go-live readiness scoring + Go-Live Evidence Pack (`economics_threshold_signoffs.csv`) to close the economics intelligence loop.
      - Progress (April 26, 2026): dashboard and Reports profitability formulas now use corrected shipping economics (`shipping charged` as revenue, label spend as expense) and lot-aware/product-cost-aware COGS; cost maps are time-aware FIFO across multiple product repurchases/lots and inventory value uses FIFO remaining lot cost. Regression suite validates dashboard live metrics, sale cost maps, Reports helpers, and chat/report snapshots.
      - Progress (May 13, 2026): dashboard, Reports close readiness, Accounting Close Packet guidance, scheduled Slack reports, Ask/AI accounting snapshots, Reports Copilot, AI Accountant contexts, and lot P/L now distinguish before-return margin from final estimated profit after returns. The canonical close formula is `estimated_profit_after_returns = profit_before_returns - return refunds + returned COGS reversal`.
      - Progress (May 13, 2026): dashboard live metrics now expose `sales_30d_profit_before_returns` separately from final `sales_30d_est_profit`, and the dashboard labels the main profit card as `Est Profit After Returns (30d)` with `Profit Before Returns (30d)` beside it; Reports drift checks now tie out both dashboard before-return profit and after-return estimated profit separately.
      - Progress (May 13, 2026): AI Accountant monitor evidence, workspace notes, and scheduled-review context now include dashboard before-return profit beside final after-return estimated profit, keeping accountant alerts and LLM review packets aligned with dashboard labels.
      - Progress (May 13, 2026): scheduled Slack daily reports now include 24-hour profit before returns beside estimated profit after returns, and integration-event evidence records the before-return figure for close-review tie-out.
      - Progress (May 13, 2026): QuickBooks Sales Export now emits `profit_before_returns_estimate` beside legacy `gross_margin_estimate`, and period drift checks validate the clearer field for QBO before-return profit tie-out.
      - Progress (May 13, 2026): Ask/AI Reports snapshot citations now include structured `profit_before_returns` beside `estimated_profit_after_returns`, keeping AI-visible accounting evidence aligned with dashboard, Reports, Slack, and close packets.
      - Progress (May 14, 2026): Listing Wizard and Listings now load eBay Sell Metadata category condition policies, map condition IDs to Inventory API condition enums, restrict condition selectors to category-valid values, and block/auto-correct invalid selected conditions before publish to prevent eBay `25021` failures.
      - Progress (May 14, 2026): shared eBay dependency preflight now validates the selected condition against the live category condition policy, so saved presets, cached UI state, and direct-post paths fail before inventory publish when the condition/category pair is invalid.
      - Progress (April 26, 2026): added Reports `Accounting Exception Queue` and export for missing cost basis, missing label spend, missing fee evidence, fee-source fallback, nonpositive margins, and lot over/under-allocation anomalies.
      - Progress (April 26, 2026): added Reports `Accounting Review / Close Readiness` panel with close status, inventory value, sales net before COGS, FIFO COGS, profit before returns, exception counts, reconciliation flags, fee-source fallback coverage, and shipping label-spend coverage.
      - Progress (May 13, 2026): close readiness now surfaces `Est. Profit After Returns` as the lead profit metric and labels `FIFO Margin Before Returns`, return refunds, return COGS reversal, and return profit impact separately.
      - Progress (April 26, 2026): added in-app `Accounting Field Semantics` reference covering product acquisition/product cost, lot totals, product-lot unit/allocated landed components, and FIFO remaining lot cost.
      - Progress (April 26, 2026): added deterministic Reports `Accounting Close Packet` ZIP export with manifest, close-readiness checks, exception evidence, sales/COGS/margin/reconciliation/shipping/fee/tax/lot/inventory/return/QBO CSVs.
      - Progress (April 26, 2026): hardened Ask/AI accounting context so inventory and reports snapshots prefer repository FIFO/lot cost maps before product-level fallbacks, keeping AI-visible profit explanations aligned with dashboard and Reports.
      - Progress (April 26, 2026): aligned Sales page, inventory-cycle analytics, and QuickBooks-style sales staging exports with the canonical shipping convention, and made actual-economics/QBO staging paths prefer normalized order finance entries for marketplace fees and shipping-label spend before order/sale field fallbacks.
      - Progress (April 26, 2026): aligned close-readiness return impact with reconciliation/QBO adjustment exports by subtracting full refund totals (`refund_amount + refund_fees + refund_shipping`) from post-return margin.
      - Progress (April 27, 2026): added Admin `Accounting Close Sign-Off Tracker` for accounting model acceptance and monthly close review, including seed/quick-approve helpers, CSV export, Go-Live Evidence Pack artifact (`accounting_close_signoffs.csv`), and readiness metadata for missing model-acceptance sign-offs.
      - Progress (April 27, 2026): added role-gated AI Accountant access in Ask GoldenStackers and Reports, using `ai_accountant_use`, read-only cited accounting snapshots, close-readiness/exception/lot-allocation evidence, write-intent blocking, and tax/legal human-review guardrails.
    - Progress (April 27, 2026): Reports AI Accountant reviews now record `ai_chat` audit events with `event_type=ai_accountant_review`, cited evidence row counts, read-only metadata, and model citation payload when available.
    - Progress (May 4, 2026): Reports AI Accountant audit events now explicitly include `tax` in allowed domain scope when tax profile/sign-off evidence is cited.
    - Progress (May 6, 2026): edited/rejected AI Accountant review outcomes now feed back into the dedicated AI Accountant page and scheduled monitor as actionable follow-up rows until a replacement accepted review outcome is recorded.
    - Progress (May 6, 2026): AI Accountant automation now defaults on in all environments, including sync-runner scheduling, interval monitoring several times per day, Slack alert routing, scheduled LLM monitor reviews, a reusable expert accountant identity/system prompt for chat/Slack/reviews, audit trail and nonblocking runtime failure handling, setup warnings for invalid schedule mode, interval cadence, timezone, daily local time, lookback window, minimum severity, or disabled monitor-alert routing, System Health visibility for monitor due/overdue/route status and last attempt/success evidence, explicit requested/effective severity metadata in result, audit evidence, message history, and evidence exports when fallback is needed, schedule-mode-specific success/failure attempt markers, dedicated `notification_route_ai_accountant_monitor` Slack routing, default-on external web-research context for tax/accounting questions with Admin runtime default coverage for monitor timing and web enabled/limit/timeout settings, setup warnings for disabled/invalid web research config, and chat/Slack regression coverage, plus a read-only Slack `accountant` intent for ops/admin interaction.
    - Progress (May 8, 2026): migration `0060_ai_accountant_default_on` upgrades existing runtime settings so environments seeded before the default-on change do not require manual Admin cleanup to enable monitor scheduling, Slack alerts, scheduled LLM review, accountant chat, or web research.
    - Progress (May 8, 2026): migration `0061_notification_outbox_default_on` enables the notification outbox runner and retention cleanup by default, completing the monitor-to-Slack automation path for queued AI Accountant alerts.
    - Progress (May 8, 2026): migration `0062_slack_notifications_default_on` enables the global Slack notification master switch by default so missing Slack token/channel setup is visible as delivery evidence instead of suppressing queued AI Accountant alerts.
    - Progress (May 9, 2026): AI Accountant setup readiness now distinguishes Slack alert routing from Slack delivery configuration and warns directly when `slack_bot_token` or a target channel is missing, so default-on monitor alerts cannot look fully ready while the notification outbox is retrying delivery.
    - Progress (May 9, 2026): the AI Accountant workspace now shows recent monitor Slack notification-outbox delivery rows with retry/error evidence, keeping delivery blockers visible in the accounting workflow itself.
    - Progress (May 9, 2026): the AI Accountant workspace can process due queued/retrying monitor Slack deliveries from the Recent Slack Delivery panel, so configuration fixes can be retried without switching to Admin while normal outbox backoff remains intact.
    - Progress (May 9, 2026): monitor Slack delivery evidence now includes summary metrics for total rows, due rows, retrying/failed rows, sent rows, and latest error so stuck AI Accountant alerts are easy to spot.
    - Progress (May 9, 2026): scheduled AI Accountant LLM review payloads now use compact accounting-specific context limits and skip comp-reference prompt context for `workflow=accounting`, reducing LocalAI 500s from oversized monitor review requests while retaining omitted-row evidence.
    - Progress (May 9, 2026): scheduled AI Accountant LLM reviews now retry once with a five-row compact context before falling back to the deterministic unavailable note, and audit metadata records compact retry plus omitted-row evidence.
    - Progress (May 9, 2026): Recent AI Accountant Messages now show automated review status, compact retry, answer/prompt/data-scope hashes, review error, monitor/exception/FIFO evidence row counts, and omitted-row counts so scheduled-review runtime failures and payload identity are visible from the accountant workspace.
    - Progress (May 9, 2026): scheduled AI Accountant review failures now preserve both default-context and compact-context errors in Slack/app evidence, making runtime failures easier to diagnose.
    - Progress (May 9, 2026): manual AI Accountant workspace reviews now share the scheduled monitor's accounting workflow, compact context limits, and compact retry behavior.
    - Progress (May 9, 2026): scheduled review audit/message evidence now includes final-attempt row counts and omitted-row totals beside compact retry, hash, and error details.
      - Progress (April 27, 2026): added migration `0059_ai_accountant_permission` to backfill `ai_accountant_use` for existing ops/admin role-permission rows.
      - Progress (April 28, 2026): Products table and `Product Detail/Edit` now render as full-width inline sections, reducing operator friction when reviewing product cost fields, lot assignments, conversions, media, lifecycle status, and repurchase history during accounting cleanup.
      - Current hardening focus (May 14, 2026): keep dashboard, Reports, Sales, Orders, lot P/L, Slack, exports, Reports Copilot, and AI Accountant snapshots locked to the same actual-economics, FIFO/lot COGS, and return-adjusted profit model, with extra checks for mixed-lot repurchases, partial check-ins, label-spend evidence, return COGS reversals, and accounting exception coverage. Dashboard now exposes an opt-in sale-level Profit Basis Audit for recent net, FIFO COGS, before-return profit, COGS source, bundle flag, and FIFO evidence row count; AI Accountant now turns monitor findings into answerable operator questions with Ask/Slack reply prompts tied to the originating issue, records matching operator replies as read-only `ai_accountant_answer` audit evidence, annotates those answers back onto generated questions in snapshots and the AI Accountant workspace for follow-up, exports/cites them in evidence packets and accounting snapshots, tracks whether each answer was applied or still needs more information, feeds needs-more-info/obsolete answers back into monitor/action rows with replacement-answer prompts, and suppresses the older prompt once a usable replacement answer exists for the same target.
    - GS-V10-017 (P1): lifecycle close/archive controls
      - add explicit `archive/restore/close` lifecycle actions for listings, products, lots, and media without hard-delete defaults
      - add UI filters/views for active vs archived records across operations pages
      - enforce cross-entity guardrails (for example: block archiving products tied to active listings unless explicitly confirmed)
      - add lifecycle audit trail + optional scheduled retention/cleanup policy for archived records
    - GS-V10-020 (P0): accounting verification + AI accountant
      - document canonical field semantics for product cost, product landed acquisition components, lot total landed cost, and product-lot assignment allocation fields
      - add an Accounting Review page or Reports panel that reconciles inventory value, COGS, sales net, returns/refunds, fee source coverage, shipping charged vs label spend, and lot allocation completeness
      - add an accounting exception queue for missing/ambiguous cost basis, overallocated/underallocated lots, negative/zero margins, missing label spend, fee-source fallback rows, and products sold without cost evidence
      - add deterministic export packets for accountant review: sales/fees/shipping/COGS summary, lot allocation audit, SKU margin detail, and exception evidence CSV/XLSX
      - introduce a role-gated AI Accountant in Reports/Ask GoldenStackers that can explain variances, summarize exception queues, suggest correcting mappings, and draft adjustment proposals without direct writes unless explicitly approved
      - require AI Accountant responses to cite source tables/rows and clearly label estimated vs actual values; block unsupported tax/legal conclusions and route those to human sign-off
      - add owner/date sign-off tracker for accounting model acceptance and periodic monthly close review
  - Started GS-V10-014 first delivery slice:
    - added `notification_outbox` schema/model foundation for retry-safe, idempotent notification dispatch (`0046_notification_outbox`)
    - added follow-up outbox composite index migration (`0052_outbox_claim_dedupe_idx`) for due-dispatch and dedupe lookup hot paths
    - added repository APIs for outbox enqueue/list/update operations with audit trail capture
    - added first-pass outbox processor service + sync-runner hook (`notification_outbox_runner_enabled`, `notification_outbox_runner_limit`)
    - hardened outbox processor/repository path with DB-side due filtering (`due_before`) and direct row retrieval (`get_notification_outbox`) to reduce broad list scans on process runs
    - added outbox enqueue dedupe-key idempotency reuse for open/sent rows (`queued`/`retrying`/`processing`/`sent`) to avoid duplicate dispatch rows
    - added outbox retention cleanup service + daily scheduler hook (`notification_outbox_cleanup_enabled`, `notification_outbox_cleanup_timezone`, `notification_outbox_cleanup_local_time`)
    - added System Health `Notification Outbox` visibility panel (due/retrying/failed/sent metrics + row preview)
    - added System Health `Outbox Runner Activity` visibility (latest process/cleanup run timestamps/status/details)
    - added config-health required runtime coverage for outbox runner/backoff/retention/cleanup keys
    - added Admin `Notification Outbox Controls` (runner/backoff/retention/cleanup settings + manual run-now/cleanup-now actions + quick queue preview)
    - fixed Products filter session-state warning path by removing pre-widget state mutation for category/status/inventory-class multiselects
    - hardened eBay shipping-status enrichment to avoid false delivered mapping from weak `deliveryDate` signals when order status is still shipped/in-transit
  - Started GS-V10-014 dashboard rollup optimization slice:
    - added report-window hot-path index migration (`0053_report_hotpath_idx`) for `sales.sold_at/id`, `orders.sold_at/id`, and `order_finance_entries` kind/date scans used by rollup baseline queries
    - added returns/listings date-window index migration (`0054_returns_listing_date_idx`) for `returns.returned_at/id` and `marketplace_listings` listed/created window probes used by extended rollup EXPLAIN baselines
    - added product/inventory/lot-assignment time-window index migration (`0055_report_time_window_idx`) for `products.acquired_at/id`, `product_lot_assignments.acquired_at/id`, and `inventory_movements.occurred_at/id` report-window scans
    - expanded rollup EXPLAIN baseline capture set with additional report-window probes (`report_orders_rows`, `report_order_items_rows`, `report_sales_rows`, `report_returns_rows`, `report_listings_rows`) for deeper plan evidence collection
    - expanded rollup EXPLAIN capture set further with `report_products_rows` and `report_lot_assignment_rows` probes for product/acquisition and lot-assignment windows
    - expanded rollup EXPLAIN capture set with `report_inventory_movement_rows` and `report_marketplace_reconciliation_rows` probes for movement-window and marketplace aggregate reconciliation plans
    - fixed rollup EXPLAIN listings probe enum/column compatibility (`listing_status::text`, `external_listing_id`) and added System Health rollback guard on EXPLAIN failures to prevent aborted-transaction cascades in recent-snapshot queries
    - hardened rollup EXPLAIN baseline execution to continue after individual probe failures (transaction rollback + per-probe error row capture) so operators still receive partial snapshots for triage
    - reworked rollup EXPLAIN `dashboard_live_metrics` probe into CTE-based rollups (`sales_rollup`/`orders_rollup`/`label_spend_rollup`) so baseline plans no longer rely on repeated scalar subqueries for the same windows
    - reduced duplicate scan/fan-out overhead in remaining EXPLAIN probes by removing unused joins, directly joining fee rows on `sales.order_id`, and switching marketplace reconciliation probe to per-table CTE aggregates + marketplace union
    - added focused rollup EXPLAIN probes for normalized fee-type grouping (`dashboard_ebay_fee_type_breakdown_30d`) and notification outbox runner activity (`notification_outbox_runner_activity_14d`) to extend plan evidence coverage for finance attribution and outbox timeline reads
    - added Slack Ops governance-path rollup EXPLAIN probes (`slack_ops_queue_health_rows`, `slack_ops_events_24h`) to capture plan evidence for queue snapshot hydration and 24h integration-event trend reads used by System Health/Admin
    - hardened Slack/outbox EXPLAIN probe compatibility by casting `audit_logs.changes_json` to `JSONB` in probe SQL and adding table-existence guards for `audit_logs`/`integration_queue_jobs` so partially migrated environments emit explicit `skipped` rows instead of opaque SQL probe failures
    - hardened per-probe EXPLAIN error classification so missing-table relation errors are recorded as explicit `skipped` rows (`skip_reason=table <table> not present`) instead of inflating failure-rate metrics in mixed-schema environments
    - refined EXPLAIN snapshot semantics so guarded non-applicable probes are tracked as explicit skips (`skipped` + `skip_reason`) and shown separately in System Health, keeping failure-rate metrics focused on true query failures
    - tightened nullable date-window report predicates (`products/listings/lot_assignments`) to use bounded `created_at` fallback when date is null, avoiding unbounded `IS NULL` branch scans in report windows
    - added repository DB-aggregated live metrics rollup (`dashboard_live_metrics`) for 7d/30d orders/sales/shipping/profit calculations
    - optimized `dashboard_live_metrics` aggregation path to reduce repeated sales-window scans and switched normalized finance date filters to index-friendlier split predicates (`transaction_date` or fallback `created_at` when null)
    - Dashboard now prefers rollup query path to reduce read latency/memory churn from full sales+orders hydration
    - added validation coverage for rollup correctness + dashboard rollup usage path
    - added DB-backed Reports shipping economics rollups (`report_shipping_economics_rows`, `report_shipping_economics_summary`) and wired Reports to use them with compatibility fallback
    - added DB-backed Reports tax-detail rollup (`report_tax_estimate_detail_rows`) and wired Tax Drilldown/summary inputs to use query-side calculations with compatibility fallback
    - added DB-backed Reports fee-reconciliation rollup (`report_ebay_fee_reconciliation_rows`) and wired `eBay Fee Reconciliation` detail export to use query-side assembly with compatibility fallback
    - added repository validation coverage for fee-reconciliation rollup query path (`test_report_ebay_fee_reconciliation_rows`)
    - added System Health on-demand DB rollup latency baseline capture + CSV export (`collect_rollup_latency_baseline`) for production read-performance evidence
  - GS-V10-014 (P0) closed (April 20, 2026):
    - notification outbox foundation + runner/cleanup controls, hot-path indexing, DB rollup-first dashboard/reports, and System Health rollup EXPLAIN capture/telemetry are complete.
    - EXPLAIN baseline now covers 18 probes including finance/outbox/Slack governance query paths, with explicit failure-vs-skip visibility for operator triage.
    - Final operator sign-off refresh captured on April 21, 2026: rollup EXPLAIN snapshot completed with 18 rows captured (no probe-failure banner) and `dashboard_live_metrics` execution at ~0.252ms, confirming healthy performance at current production scale.
  - Started GS-V10-015 first runtime-capacity tuning slice:
    - raised Admin AI Runtime `Max Output Tokens` cap from `4096` to `16000`
    - raised env fallback default `COMP_LLM_MAX_OUTPUT_TOKENS` to `16000` for higher-detail LLM outputs
    - added Admin one-click bulk profile upgrade helper (`Bulk Max Output Tokens Upgrade`) with dry-run preview to raise existing profiles below target
  - Started GS-V10-015 workflow-profile split slice:
    - added workflow-aware AI runtime-chain selection (`resolve_comp_llm_runtime_chain(..., workflow=...)`)
    - added runtime workflow selector keys (`ai_workflow_profile_listing`, `ai_workflow_profile_intake`, `ai_workflow_profile_comp`, `ai_workflow_profile_risk`, `ai_workflow_profile_accounting`) supporting profile id or name targeting
    - wired Listing Wizard + Listings Copilot to `workflow=listing` profile preference
    - wired Coin/Inventory Intake + purchase-invoice extraction multimodal paths to `workflow=intake` profile preference
    - added Admin AI Runtime workflow-routing controls (`Workflow AI Profile Routing`) for listing/intake/comp/risk/accounting profile mapping and one-click reset
    - wired AI Accountant monitor/manual review calls to first-class `workflow=accounting` runtime routing so accountant automation can be pinned to a known-good model profile
    - added AI Accountant runtime-chain diagnostics and a read-only smoke test so LocalAI/profile failures can be diagnosed separately from accounting monitor data
    - added scheduled-monitor runtime-chain evidence to automated-review metadata and unavailable-review messages so Slack/in-app failures show the sanitized provider/model route
    - added migration-backed blank workflow-profile defaults for listing/intake/comp/risk/accounting so every environment has config-health-complete routing keys without changing model selection
    - added System Health visibility for the sanitized AI Accountant `workflow=accounting` LLM route without requiring a live LLM call
    - wired service-check errors such as `AI Accountant LLM Route` into System Health critical-signal alert context as service signals
    - enabled System Health critical auto-alert and Slack critical-notification defaults through migration-backed runtime settings
    - made System Health critical auto-alert Slack dispatch respect `notification_route_system_health_critical`
    - added System Health service-row visibility for critical alert policy and route gating
    - added System Health critical alert Slack delivery-readiness visibility for token/channel readiness
    - added System Health critical Slack delivery evidence filtered to `system_health_critical` queue jobs
    - added a narrow System Health action to process only due queued `system_health_critical` Slack jobs
    - added sync-runner AI Accountant monitor event evidence for optional LLM review status/hash/error/compact-retry/runtime-route, separate from deterministic monitor success
    - surfaced latest AI Accountant scheduled review evidence in System Health Service Checks, with warnings when actionable monitor findings exist but the optional LLM review is unavailable
    - added test coverage for workflow selector priority + orchestration workflow propagation
  - Started GS-V10-015 apply-time AI quality-gate slice:
    - added shared AI quality gate service (`app/services/ai_quality.py`) for placeholder/generic text detection and minimum title/details quality checks
    - Listings Copilot now rejects weak/generic AI titles from overwriting listing title
    - Listing Wizard AI apply flow now rejects weak/generic titles with explicit operator feedback
    - Inventory/Coin intake AI prefill now rejects weak title/description suggestions before default-field assignment
    - added policy blocked-term checks so suggestion apply is prevented when prohibited promise/advice phrases are present
    - added runtime-configurable AI quality thresholds + forbidden-term controls in Admin (`AI Runtime -> AI Apply-Time Quality Gates`)
    - seeded `ai_quality_*` runtime defaults and added config-health runtime coverage for these keys
    - added helper validation coverage for AI quality gates (`tests/test_ai_quality.py`)
  - Started GS-V10-015 acceptance telemetry slice:
    - Listing Wizard AI apply now records accepted snapshot telemetry (title/details/price/offer fields) for downstream quality analysis
    - Listing Wizard draft creation now computes `accepted_as_is` vs `edited_fields` outcome and persists under `ai_draft.outcome`
    - Added audit telemetry events (`ai_prompt_acceptance`: `listing_wizard_apply`, `listing_wizard_outcome`) to support prompt tuning and future registry dashboards
    - Added centralized prompt registry service (`app/services/ai_prompt_registry.py`) and Admin rollback UI (`AI Prompt Version Registry + Rollback`) for `listing` + `comp` workflows
    - Added active prompt version runtime controls (`ai_prompt_active_version_listing`, `ai_prompt_active_version_comp`) and registry stores (`ai_prompt_registry_listing_json`, `ai_prompt_registry_comp_json`)
    - Added prompt registry unit-test coverage (`tests/test_ai_prompt_registry.py`)
  - Started GS-V10-015 operator quality metrics slice:
    - Added Admin `AI Quality Metrics` panel from `ai_prompt_acceptance` telemetry (lookback/workflow filters, outcome KPIs, workflow breakdown, prompt-version breakdown, recent outcomes, CSV export)
    - Added rollback-safe DB-session recovery on metrics query failures in Admin panel
    - extended AI quality metrics with richer trend/drilldown views:
      - daily acceptance trend (`accept_rate_pct`) with timeline table,
      - workflow-specific daily trend drilldown selector,
      - workflow-level top edited-field attribution table for prompt-tuning prioritization.
    - added helper-level regression coverage for AI metrics summarization/filtering (`tests/test_admin_helpers.py`).
  - GS-V10-015 (P1) closed (April 20, 2026):
    - runtime-capacity tuning, workflow profile routing, apply-time quality gates, prompt/version registry + rollback controls, and operator-facing AI quality metrics/drilldowns are complete.
  - Completed additional System Health hardening:
    - all raw DB reads in System Health now use rollback-safe query wrappers to recover session state after query exceptions
    - `Outbox Runner Activity` now sources from `audit_logs` integration events (`integration=notification_outbox`) for schema compatibility
    - added composite performance indexes for hot reads:
      - `sync_runs(job_name,status,started_at)`
      - `marketplace_listings(marketplace,listing_status,updated_at)`
      - `workflow_drafts(environment,workflow_key,status,updated_at)`
      - `audit_logs(entity_type,entity_id,created_at)`
  - Started GS-V10-013 Step 1:
    - added shared listing draft contract service (`app/services/workflow_contracts.py`) for unified draft shape (`contract`, `signature`, `context`, `state`)
    - refactored Listing Wizard + Listings publish/edit draft save/resume paths to use shared contract helpers
    - preserved backward compatibility for legacy draft payload shapes via shared extractor fallback paths
    - added targeted coverage for contract + legacy extraction (`tests/test_workflow_contracts.py`)
  - Started GS-V10-013 Step 2:
    - Listing Wizard draft payload now persists dependency preflight payload + readiness blocker/warning counters
    - Listings publish/edit draft payload now persists dependency preflight payload for resume-safe readiness context
    - added test coverage for structured preflight payload persistence through shared contract helpers
  - Started GS-V10-013 Step 3 (partial):
    - moved Listing Wizard AI business state (`suggestions/diagnostics/acceptance/evidence/has_run`) and `risk_summary` into persisted draft state (no longer session-only for resume-critical AI decisions)
    - persisted Listing Wizard category-query seed/suggestion state + AI seed/seed-context fields in draft payload for deterministic resume
    - persisted Listings publish category-query seed product id (`ebay_pub_category_query_seed_product_id`) for stable suggestion resume behavior
    - Listings publish/edit now uses centralized persisted draft key contract (`LISTINGS_EBAY_PUBLISH_DRAFT_SESSION_KEYS`) to keep business-critical fields DB-backed and reduce drift back to session-only state
  - Started GS-V10-013 Step 4:
    - added draft save/resume parity test suite (`tests/test_listing_workflow_draft_parity.py`) validating category/aspects/pricing/post-mode coverage across Listing Wizard + Listings
    - added shared contract roundtrip assertions for parity paths to prevent regression in draft payload shape compatibility
    - expanded repository workflow-state tests (`tests/test_workflow_state.py`) for DB-level validation/missing-path handling, scope-aware latest-resume behavior, and workflow-event filter/limit guardrails
    - expanded Listings helper tests (`tests/test_listings_helpers.py`) for contract payload build/apply and pending-update key-filtering to stabilize DB-first draft mutation paths
    - added end-to-end Listings publish draft roundtrip coverage over repository save/load/resume/clear flow with preflight payload persistence assertions (`test_listings_publish_contract_roundtrip_save_resume_clear`)
    - expanded strict seeded Listings Playwright gate (`tests/e2e/listings_flow_strict.spec.ts`) with publish-draft `Save -> Resume -> Clear` control assertions and eBay preflight strict checks
    - latest local strict-spec evidence run (April 16, 2026): `2 passed` under strict seeded mode (`E2E_STRICT_LISTINGS=1`, `PLAYWRIGHT_REQUIRE_SEED=1`)
  - Started GS-V10-013 Step 5:
    - aligned Listing Wizard + Listings publish/edit grading behavior by defaulting condition-description from product AI grading context
    - added guarded grading-note enrichment into listing details (`AI Grading Notes`) with no duplicate reinjection on rerun
    - added helper-level regression coverage for grading-prefill status signaling and grading-note merge behavior
  - Added follow-on eBay comp flow stabilization (April 15, 2026):
    - addressed wizard-path hard-fail regressions where eBay Finding local cooldown (`EBAY_FINDING_RATE_LIMITED`) bubbled as blocking errors
    - normalized behavior across Comp Tool, Listing Wizard, and Intake wizards to preserve operator flow under cooldown windows
  - Extended Listings + Intake UX/state hardening (April 18, 2026):
    - Listings publish pending-update preserve path now captures full publish-form state and marks one-run signature-reset skip to prevent rerun clobber after category/aspect actions
    - Inventory Intake and Coin Intake now always render eBay purchase item ID/link fields and enable them only when `Purchased On eBay` is checked
  - Extended eBay Finding cooldown reliability hardening (April 18, 2026):
    - local cooldown path now supports periodic probe calls (`ebay_finding_rate_limit_probe_interval_seconds`) instead of full lockout until expiry
    - successful probe responses now auto-clear local Finding cooldown for faster comp/intake recovery
    - added eBay service regression coverage for probe behavior and cooldown-clear-on-success paths (`tests/test_ebay_service.py`)
    - added dedicated Admin `eBay Finding Throttle Controls` surface (Sync Jobs tab) for run/10m caps and cooldown/probe tuning via runtime settings, plus one-click recommended-default reset
  - Extended GS-V10-018 Listings performance hardening (April 18, 2026):
    - replaced relationship-driven per-row listing media counting with repository aggregate query map (`listing_media_count_map`) to remove per-listing lazy relationship churn.
    - removed `listing.product` and `listing.media_assets` lazy relationship dependence from readiness/history/batch-plan flows by reusing `product_by_id` and explicit listing-media repository reads.
    - added repository regression coverage for listing media count aggregation with archived-media filtering semantics (`test_listing_media_count_map_filters_archived_by_default`).
  - Extended GS-V10-018 System Health query efficiency hardening (April 18, 2026):
    - added per-render integration-event audit query cache in System Health and routed repeated 24h/7d/14d/cooldown slices through shared helper reads.
    - removed duplicate audit-log roundtrips across rollup snapshot history, page-baseline snapshot history, outbox runner activity, error signals, and critical-alert evidence/cooldown checks.
  - Extended GS-V10-018 Intake Wizard media-attach performance hardening (April 18, 2026):
    - added repository targeted media lookup helper (`list_media_assets_by_ids`) to support sparse media resolution by selected IDs.
    - Inventory Intake Wizard and Coin Intake Wizard now resolve existing-media attachments using selected IDs only, removing full media-table scans during submit-time attach flows.
    - Intake draft-listing media attach now uses repository ID-only unlinked lookup (`list_unlinked_product_media_ids`) instead of full product-media object hydration (`list_media_assets_for_product`) during submit loops.
    - Existing-media selector now uses repository DB-level row cap (`list_media_assets(limit=...)`) to avoid full media-table reads when selector UI only needs bounded rows.
    - Intake media selector panels now support shared preloaded-row reuse in the same rerun (`preloaded_rows` pass-through), preventing duplicate selector queries when both product/listing selectors are enabled together.
    - Added repository bulk media update helper (`bulk_update_media_assets`) and migrated intake attach paths (product attach, listing attach, unlinked-to-listing attach) to single-transaction batch updates instead of per-row commit loops.
  - Extended GS-V10-018 Intake Wizard diagnostics render hardening (April 19, 2026):
    - Inventory Intake and Coin Intake diagnostics now default to compact summaries and only render full AI payload JSON/code when `Render Full AI Payloads (slowest)` is explicitly enabled.
    - keeps troubleshooting visibility while reducing default rerun/render overhead from large payload blocks.
  - Extended GS-V10-018 Reports lazy-load hardening (April 19, 2026):
    - split inventory-cycle/rebuy analytics behind dedicated opt-in toggle (`Load Inventory Cycle + Rebuy Analytics (slower)`) so these heavy rollups do not run unless requested.
    - keeps fee/margin extended analytics available independently while reducing default heavy-query contention on Reports renders.
  - Extended GS-V10-018 Listings lazy media-count hardening (April 19, 2026):
    - `listing_media_count_map` is now computed only when `Load Listing Media Counts (slower)` is enabled, removing unconditional aggregate media-count query cost from default Listings load.
    - side-panel selected-listing media count now uses targeted on-demand lookup when global media-count loading remains deferred.
    - added repository scalar helper `count_media_assets_for_listing(...)` and switched Listings side-panel deferred-count path to this count query (avoids row hydration just to compute media count).
  - Extended GS-V10-018 Listings template-query deferral (April 19, 2026):
    - `eBay Listing Templates` query/UI hydration is now explicit opt-in (`Load eBay Listing Templates (slower)`) to reduce default Listings rerun query and widget overhead.
  - Extended GS-V10-018 Dashboard fee-breakdown deferral (April 19, 2026):
    - Dashboard now defers grouped eBay fee-type analytics behind explicit toggle (`Load eBay Fee Attribution Breakdown (slower)`).
    - repository live-metrics path now supports optional fee-breakdown suppression (`include_fee_type_breakdown=False`) so default dashboard load avoids unnecessary fee-type group aggregation.
  - Extended GS-V10-018 Listings readiness-queue deferral (April 19, 2026):
    - readiness scoring/blocker analytics now run only when explicit toggle is enabled (`Load eBay Readiness Queue (slower)`), removing default per-listing readiness evaluation on initial load.
    - orchestration queue now clearly indicates dependency on readiness load state when readiness computation is deferred.
  - Extended GS-V10-018 Listings deep-history master gate (April 19, 2026):
    - added `Load Deep Queue Analytics (slower)` master toggle to gate heavy readiness-adjacent history scans.
    - follow-up audit history (`Load Recent Blocker Follow-up Tasks`) and bulk publish metadata history (`Load Bulk Publish History`) now require the master deep-analytics gate to be enabled.
  - Extended GS-V10-018 Listings side-panel/media-manager deferral (April 19, 2026):
    - side-panel related-context hydration now uses master gate (`Load Side Panel Context (slower)`) so related sales/orders and review-history rendering are disabled by default.
    - listing media manager now uses explicit load gate (`Load Listing Media Manager Data (slower)`), deferring listing media table/gallery hydration and media-linked publish option preparation unless requested.
  - Extended GS-V10-018 Listings publish-workspace deferral (April 19, 2026):
    - lower eBay publish/revise/preflight/manage workspace is now explicit opt-in (`Load eBay Publish Workspace (slower)`), preventing heavy publish-workflow state hydration from running on default Listings renders.
  - Extended GS-V10-018 Listings readiness-defaults lazy resolution (April 19, 2026):
    - eBay publish preset/default-runtime hydration used by readiness and bulk-publish defaults is now lazily resolved only when those workflows are loaded/executed, removing an unconditional preset query from default Listings load.
  - Extended GS-V10-018 Listings channel-adapter lazy resolution (April 19, 2026):
    - channel adapter construction is now lazy and only initializes when orchestration/capability workflows need it, removing unconditional adapter build work from default Listings load.
  - Extended GS-V10-018 Listings create-flow readiness deferral (April 19, 2026):
    - create-flow eBay readiness preview is now explicit opt-in (`Load Create-Flow eBay Readiness Preview (slower)`), and create-flow default preset lookup is lazily resolved only when that preview is enabled.
  - Extended GS-V10-018 Listings workflow-telemetry deferral (April 19, 2026):
    - workspace feedback and workflow-completion telemetry are now explicit opt-in (`Load Listings Workflow Telemetry (slower)`), reducing default audit-query load on Listings first render.
  - Extended GS-V10-018 Listings bulk-draft creator deferral (April 19, 2026):
    - bulk draft creator candidate hydration is now explicit opt-in (`Load Bulk Draft Creator Data (slower)`), deferring product/listing pair scans and selector build work on default reruns.
  - Extended GS-V10-018 Listings store-profile context deferral (April 19, 2026):
    - eBay store profile context/load-apply UI is now explicit opt-in (`Load eBay Store Profile Context (slower)`), with lazy profile payload hydration to reduce default rerun overhead.
  - Extended GS-V10-018 Listings external-ID map lazy build (April 19, 2026):
    - external listing owner map for eBay collision checks now builds lazily only when publish/relist paths require it, removing unconditional map construction from default Listings reruns.
  - Extended GS-V10-018 Listings create-media capture deferral (April 19, 2026):
    - create-flow camera/file capture controls are now explicit opt-in (`Load Create Listing Media Capture (slower)`), reducing default widget/render overhead on Listings load.
  - Extended GS-V10-018 Listings publish-meta parse caching (April 19, 2026):
    - parsed `ebay_publish` metadata is now cached per listing per rerun and reused across table/readiness/publish sections to reduce repeated JSON decode overhead.
  - Extended GS-V10-018 Listings format-diagnostics filter safety (April 19, 2026):
    - selecting `Format Issue Only` now auto-enables format diagnostics for that run (with operator notice), preventing empty/incorrect filtered results when diagnostics were deferred.
  - Extended GS-V10-018 Listings filtered-row diagnostics optimization (April 19, 2026):
    - listing table rows now hydrate format diagnostics after base filters are applied, so eBay publish metadata parsing runs only for rows in the current filtered set instead of the full listing dataset.
  - Extended GS-V10-018 Listings readiness map reuse (April 19, 2026):
    - readiness/history paths now reuse the existing per-rerun listing-id map instead of rebuilding a second listing-id index in the same render cycle.
  - Extended GS-V10-018 Listings diagnostics guardrail tests (April 19, 2026):
    - added helper-level regression coverage for base listings filter behavior, deferred/required diagnostics hydration semantics, and `Format Issue Only` row filtering (`tests/test_listings_helpers.py`).
  - Extended GS-V10-018 Listings preview-aware diagnostics hydration (April 19, 2026):
    - when full-table rendering is off, non-queue format diagnostics now hydrate preview rows only; `Format Issue Only` still hydrates all filtered rows to preserve correctness.
  - Extended GS-V10-018 Listings index-map reuse (April 19, 2026):
    - side-panel and media-manager listing selectors now reuse the page-level listing-id map instead of rebuilding duplicate listing index dictionaries in the same rerun.
  - Extended GS-V10-018 Listings audit-row fallback correctness (April 19, 2026):
    - `_photo_comp_created_listing_ids` now honors explicitly supplied empty audit-row lists (no unintended fallback audit query), with helper test coverage for the empty-list path.
  - Extended GS-V10-018 Listings deferred-origin tagging optimization (April 19, 2026):
    - origin-tag row mutation now runs only when `Load Photo-Comp Origin Tags (slower)` is enabled; base filtering now treats missing origin as `other` for consistent deferred behavior.
  - Extended GS-V10-018 Listings object-first base filtering (April 19, 2026):
    - Listings now applies base filters directly on listing objects (query/marketplace/status/origin/archive) and materializes row dictionaries only for the filtered subset, reducing per-rerun row allocation on large datasets.
  - Extended GS-V10-018 Listings row-actions deferral (April 19, 2026):
    - table-level listing row actions now load only when explicitly enabled (`Load Listing Row Actions (slower)`), reducing default control hydration cost for large filtered result sets.
  - Extended GS-V10-018 Listings export-bytes deferral (April 19, 2026):
    - heavy Listings table toolbars now defer CSV/XLSX byte generation behind explicit opt-in (`Load Table Exports (slower)`), reducing default rerun overhead from eager export payload creation.
  - Extended GS-V10-018 shared toolbar export deferral guardrails (April 19, 2026):
    - added shared-view tests covering deferred export behavior (`skip by default`, `load when enabled`) and aligned media-selector test doubles with repository `limit` argument contract.
  - Extended GS-V10-018 Listings detail-panel deferral (April 19, 2026):
    - listing detail selector/editor panel is now explicit opt-in (`Load Listing Detail Panel (slower)`), deferring heavy selector-map/widget hydration on default table load.
  - Extended GS-V10-018 Listings media-manager strict deferral (April 19, 2026):
    - when media manager loading is deferred, Listings now skips media upload widgets, listing-media queries, and gallery/file-action hydration entirely (not just table rendering).
  - Extended GS-V10-018 Listings preview-first DataFrame materialization (April 19, 2026):
    - main Listings table now materializes preview-only DataFrame rows in default preview mode and lazily builds full filtered DataFrame only when export generation is explicitly requested.
  - Extended GS-V10-018 Listings readiness/history selector deferral (April 19, 2026):
    - readiness bulk-review selector map and failed-history retry selector map are now explicit opt-in (`Load Bulk Review Actions (slower)`, `Load Retry Failed Actions (slower)`) to avoid large option-map hydration on default queue/history views.
  - Extended GS-V10-018 Listings media-count id-list lazy build (April 19, 2026):
    - listing-id vector for media-count aggregate query is now built only when `Load Listing Media Counts (slower)` is enabled, removing eager ID list materialization on default reruns.
  - Extended GS-V10-018 Listings orchestration-queue deferral (April 19, 2026):
    - orchestration-status derivation and queue table hydration are now explicit opt-in (`Load Listing Orchestration Queue (slower)`), preventing default adapter/status computation when not needed.
  - Extended GS-V10-018 Listings bulk-publish planner deferral (April 19, 2026):
    - readiness bulk publish planner controls are now explicit opt-in (`Load Bulk Publish Batch Planner (slower)`), deferring candidate-map hydration and dry-run/publish planner state work on default queue load.
  - Extended GS-V10-018 Listings reviewer-dashboard deferral (April 19, 2026):
    - reviewer KPI/grouping panel is now explicit opt-in (`Load Reviewer Dashboard (slower)`), deferring pending-age/approval-window metrics and reviewer-summary groupby work on default readiness load.
  - Extended GS-V10-018 Listings blocker-followup workbench deferral (April 19, 2026):
    - blocker follow-up task workbench is now explicit opt-in (`Load Blocker Follow-up Workbench (slower)`), deferring follow-up creation controls and follow-up history/preset hydration by default.
  - Extended GS-V10-018 Listings readiness-breakdown table deferral (April 19, 2026):
    - readiness blocker/warning breakdown tables are now explicit opt-in (`Load Readiness Breakdown Tables (slower)`), deferring DataFrame render cost while retaining counts for quick filters and reason filters.
  - Extended GS-V10-018 Listings top-blocker actions deferral (April 19, 2026):
    - top-blocker quick-filter actions are now explicit opt-in (`Load Top Blocker Actions (slower)`), deferring quick-action button rendering and follow-up-action branch hydration by default.
  - Extended GS-V10-018 Listings readiness filter-controls deferral (April 19, 2026):
    - readiness triage shortcuts and filter widget cluster are now explicit opt-in (`Load Readiness Filters + Shortcuts (slower)`), so default readiness queue rendering skips shortcut/action and multi-filter widget hydration.
  - Extended GS-V10-018 Listings readiness queue-table deferral (April 19, 2026):
    - readiness queue table and export-toolbar rendering are now explicit opt-in (`Load Readiness Queue Table (slower)`), deferring table hydration and toolbar work on default readiness loads.
  - Extended GS-V10-018 Listings readiness evaluation-loop deferral (April 19, 2026):
    - per-listing readiness scoring/row-build loop is now explicit opt-in (`Load Readiness Evaluation (slower)`), deferring heavy evaluation work and avoiding false `no listings found` messaging when evaluation is intentionally deferred.
  - Extended GS-V10-018 Listings orchestration dependency short-circuit (April 19, 2026):
    - orchestration queue now explicitly depends on evaluated readiness rows and short-circuits with a dependency caption when readiness evaluation remains deferred.
  - Extended GS-V10-018 Listings orchestration dependency helper guardrails (April 19, 2026):
    - centralized orchestration dependency caption logic in a pure helper and added helper-level regression tests for queue/readiness/evaluation deferred-state combinations.
  - Extended GS-V10-018 Listings readiness-default lazy resolution (April 19, 2026):
    - readiness preset/runtime default resolution now executes only when readiness evaluation is enabled, avoiding unnecessary settings/preset reads when readiness queue is loaded but scoring remains deferred.
  - Extended GS-V10-018 Listings bulk-history retry-analysis deferral (April 19, 2026):
    - Bulk Publish Execution History now defers failed-row extraction and retry-control hydration behind explicit opt-in (`Load Bulk Publish Retry Analysis (slower)`), reducing default history-load DataFrame/filter overhead.
  - Extended GS-V10-018 Listings bulk-history preview-first rendering (April 19, 2026):
    - Bulk Publish Execution History now uses preview-first row rendering and lazy full filtered export DataFrame generation (`row_count` + `export_df_factory`), while retry analysis evaluates failed rows from the full filtered history set.
  - Extended GS-V10-018 Listings channel-capability preview-first rendering (April 19, 2026):
    - Channel Capability Matrix now uses preview-first rendering with row-count-aware toolbar and lazy full export DataFrame generation to reduce default in-page render cost while retaining full export behavior.
  - Extended GS-V10-018 Listings side-panel selector-map deferral (April 19, 2026):
    - Listings side panel now defaults to lightweight ID-based selection and defers heavy title/marketplace selector-label map construction behind explicit opt-in (`Load Detailed Listing Selector Labels (slower)`).
  - Extended GS-V10-018 Listings document-source label deferral (April 19, 2026):
    - Listings Document Draft source selection now defers rich sale/order label formatting behind explicit opt-in (`Load Detailed Document Source Labels (slower)`), defaulting to lightweight ID labels for large related sets.
  - Extended GS-V10-018 Listings document-source option-list previewing (April 19, 2026):
    - Listings Document Draft source picker now uses preview-limited option lists with explicit opt-in full-list loading (`Load Full Document Source List (slower)`), including session-state guardrails for truncated option transitions.
  - Extended GS-V10-018 Listings side-panel review-history preview-first rendering (April 19, 2026):
    - side-panel review history now uses preview-first rendering with row-count-aware toolbar and lazy full export DataFrame generation, reducing side-panel table/render overhead while retaining full history exports.
  - Extended GS-V10-018 Listings side-panel review-controls deferral (April 19, 2026):
    - side-panel review actions and review-history controls now sit behind explicit opt-in (`Load Review Controls (slower)`), reducing default side-panel widget/render overhead when review operations are not needed.
  - Extended GS-V10-018 Listings media-manager table preview-first rendering (April 19, 2026):
    - listing media table now uses preview-first rendering with row-count-aware toolbar and lazy full export DataFrame generation (`listings_media_manager_*`), reducing default media-table payload while preserving full exports.
  - Extended GS-V10-018 Listings publish-workspace draft/preset deferral (April 19, 2026):
    - eBay publish workspace now defers workflow-draft lookup and publish-preset hydration behind explicit opt-in (`Load Publish Draft + Presets (slower)`), reducing default publish-workspace DB reads for manual publish flows.
  - Extended GS-V10-018 Listings category-assist deferral (April 19, 2026):
    - eBay publish workspace now defers category-assist suggestion-map/control hydration and taxonomy lookup/cache query paths behind explicit opt-in (`Load eBay Category Assist (slower)`), reducing default publish-workspace overhead.
  - Extended GS-V10-018 Listings default-aspects preview deferral (April 19, 2026):
    - item-specifics default-aspect merge/preview/apply path now sits behind explicit opt-in (`Load Suggested Default Aspects (slower)`), while manual item-specifics editing remains available by default.
  - Extended GS-V10-018 Listings sanitized-preview lazy compute (April 19, 2026):
    - publish workspace sanitized-HTML preview now performs sanitization only when preview is explicitly enabled, removing duplicate always-on sanitization work during normal reruns while preserving publish-path sanitization.
  - Extended GS-V10-018 Listings preflight reuse optimization (April 19, 2026):
    - eBay publish/revise flows now reuse cached dependency preflight results when the preflight input signature is unchanged (token fingerprint + marketplace/category/location/policies), avoiding duplicate dependency-check API calls within repeated publish/revise runs.
  - Extended GS-V10-018 Listings aspects-merge lazy resolution (April 19, 2026):
    - merged eBay item-specific defaults are now resolved lazily only when revise/publish payload construction needs aspects, removing always-on merge overhead from non-submit reruns.
  - Extended GS-V10-018 Listings HTML-validation lazy resolution (April 19, 2026):
    - listing HTML validation now resolves lazily only when revise/publish paths require validation, removing always-on `_validate_listing_html` work from default publish-workspace reruns.
  - Extended GS-V10-018 Listings publish-description sanitization lazy resolution (April 19, 2026):
    - publish description sanitization now resolves lazily only in revise/publish action paths, removing always-on `_sanitize_listing_html` work from default publish-workspace reruns.
  - Extended GS-V10-018 Listings publish-meta cache reuse (April 19, 2026):
    - publish workspace now reuses existing `publish_meta` cache for discovered-offer and last-publish diagnostics paths, eliminating duplicate marketplace-details JSON parse passes in the same rerun.
  - Extended GS-V10-018 Listings publish-draft autosave deferral (April 19, 2026):
    - publish-workspace workflow-draft autosave writes now run only when draft/preset controls are explicitly enabled (`Load Publish Draft + Presets (slower)`), reducing default rerun draft-write activity.
  - Extended GS-V10-018 Listings fee-assist deferral (April 19, 2026):
    - eBay fee estimate metrics now sit behind explicit opt-in (`Load eBay Fee Estimate Assist (slower)`), with lazy estimate computation reused for publish metadata to avoid always-on fee-estimate work in default reruns.
  - Extended GS-V10-018 Listings volume-pricing lazy resolution (April 19, 2026):
    - volume-pricing JSON parse now resolves lazily (`_get_volume_pricing_tiers_and_errors`) with explicit opt-in preview (`Load Volume Pricing Preview (slower)`) and on-demand parse in revise/publish paths, removing always-on parse work and eliminating early-reference hazards.
  - Extended GS-V10-018 Listings diagnostics-card deferral (April 19, 2026):
    - publish-workspace preflight card and last-publish diagnostics rendering now sit behind explicit opt-in (`Load eBay Diagnostics Cards (slower)`), reducing default diagnostics UI hydration overhead.
  - Extended GS-V10-018 Listings category-assist init deferral (April 19, 2026):
    - category-assist seed/session initialization now runs only when category assist is enabled (`Load eBay Category Assist (slower)`), removing default rerun state-setup work for deferred category assist.
  - Extended GS-V10-018 Listings volume-pricing builder deferral (April 19, 2026):
    - volume-pricing builder controls now sit behind explicit opt-in (`Load Volume Pricing Builder (slower)`), reducing default publish-workspace widget hydration while leaving submit-path pricing behavior intact.
  - Extended GS-V10-018 Listings manage-controls deferral (April 19, 2026):
    - “Manage Existing eBay Listing” form/offer-inspector controls now sit behind explicit opt-in (`Load Manage Existing eBay Listing Controls (slower)`), reducing default publish-workspace widget hydration overhead.
  - Extended GS-V10-018 Listings aspects-json lazy normalization (April 19, 2026):
    - publish workspace now uses shared lazy item-specific payload normalization (`_get_aspects_payload`) across editor + payload construction paths, eliminating always-on item-specific JSON parse work when specifics are unchanged.
  - Extended GS-V10-018 Listings publish runtime-default bulk hydrate (April 19, 2026):
    - publish workspace now resolves eBay runtime defaults from one bulk runtime-settings snapshot (`get_runtime_values`) instead of many per-key runtime-setting reads, reducing publish-workspace rerun DB lookup overhead.
  - Extended GS-V10-018 Listings manage-offer details render gate (April 19, 2026):
    - publish workspace offer-details expander now renders only when manage-existing controls are enabled, avoiding deferred-mode diagnostics-card hydration for stale offer payloads.
  - Extended GS-V10-018 Listings template-apply runtime-default reuse (April 19, 2026):
    - fixed/auction template-apply actions now reuse the publish workspace bulk runtime-default snapshot (`get_runtime_values`) for best-offer/auction/category/policy defaults, reducing per-click runtime-setting DB reads.
  - Extended GS-V10-018 Listings publish category-default runtime lookup removal (April 19, 2026):
    - publish workspace default/signature-reset category initialization now reuses bulk runtime defaults, eliminating the last per-key `ebay_category_id` runtime lookup in publish-state setup.
  - Extended GS-V10-018 Listings manage-offer lookup caching (April 19, 2026):
    - manage-offer resolution now caches SKU offers and resolved offer IDs within a rerun, reducing duplicate `get_offers` API calls across inspect/manage actions for the same listing.
  - Extended GS-V10-018 Listings merchant-location resolver caching (April 19, 2026):
    - preflight/revise/publish flows now share rerun-local cached merchant-location resolution (`token` + key), reducing duplicate `resolve_merchant_location_key` API calls in a single publish workflow run.
  - Extended GS-V10-018 Listings publish autosave debounce (April 19, 2026):
    - publish-workspace autosave now debounces changed-signature draft writes (5s window), reducing rapid repeated `save_workflow_draft` activity during interactive editing while keeping periodic autosave persistence.
  - Extended GS-V10-018 Listings readiness-runtime bulk defaults (April 19, 2026):
    - readiness evaluation and bulk-publish default resolution now use a rerun-local bulk runtime-settings snapshot (`get_runtime_values`) for listing format/duration/token/policy defaults, reducing repeated per-key runtime-setting DB reads in readiness planner flows.
  - Extended GS-V10-018 Listings readiness bulk-default memoization (April 19, 2026):
    - `_resolve_bulk_publish_defaults` now memoizes per rerun and returns copies, so repeated readiness planner/history branches reuse one resolved defaults payload instead of recomputing it.
  - Extended GS-V10-018 Listings preflight-signature reuse (April 19, 2026):
    - `_run_or_reuse_preflight` now returns the computed signature and callers reuse it for session-state persistence, eliminating duplicate signature recomputation in preflight submit/revise/publish branches.
  - Extended GS-V10-018 Listings selected-media resolver reuse (April 19, 2026):
    - publish workspace now resolves selected image/video label mappings once per rerun and reuses them across revise/publish paths, reducing duplicate media-selection mapping work.
  - Extended GS-V10-018 Listings sandbox-ops runtime lookup reuse (April 19, 2026):
    - bulk publish and retry-failed branches now share a rerun-local `ebay_allow_sandbox_seller_ops` resolver, removing duplicate runtime-setting reads in readiness/history flows.
  - Extended GS-V10-018 Listings bulk-history source filtering (April 19, 2026):
    - bulk publish execution history now scans only active eBay listings for `publish_batch_execution` payloads, reducing metadata parse/row-build overhead on large mixed-marketplace listing sets.
  - Extended GS-V10-018 Listings bulk-history raw-detail short-circuit (April 19, 2026):
    - history scan now checks raw `marketplace_details` text for `publish_batch_execution` before JSON decode, reducing unnecessary detail parsing for listings without batch execution metadata.
  - Extended GS-V10-018 Listings retry-analysis map builder simplification (April 19, 2026):
    - failed-history retry selector map now builds directly from failed-row dicts instead of DataFrame `iterrows`, reducing pandas overhead in retry analysis.
  - Extended GS-V10-018 Listings bulk-history row-build constant hoisting (April 19, 2026):
    - bulk history row builder now resolves listing id/sku/title/url once per listing before iterating batch events, reducing repeated lookup work per event row.
  - Extended GS-V10-018 Listings bulk-history filter pass consolidation (April 19, 2026):
    - batch/executor history filters now apply in one combined pass over rows instead of two sequential traversals, reducing list-iteration overhead in history filter paths.
  - Extended GS-V10-018 Listings bulk-history filtered-empty short-circuit (April 19, 2026):
    - history section now skips table/export/retry-analysis hydration when filters yield zero rows, avoiding unnecessary DataFrame and retry-control setup work for empty filtered states.
  - Extended GS-V10-018 Listings bulk-history per-row normalization reuse (April 19, 2026):
    - bulk-history filter evaluator now computes normalized batch/executor strings once per row during the single pass, removing repeated normalization calls from filter matching.
  - Extended GS-V10-018 Listings bulk-history blank-filter fast path (April 19, 2026):
    - bulk-history filter evaluator now bypasses per-row normalization when both batch and executor filters are blank, reusing sorted rows directly for default-history views.
  - Extended GS-V10-018 Listings bulk-history tiny-set sort fast path (April 19, 2026):
    - bulk-history ordering now skips `sorted(...)` when history rows are size 0–1, reusing the existing row list directly.
  - Extended GS-V10-018 Listings review-history tiny-set sort fast path (April 19, 2026):
    - side-panel review-history ordering now skips `sorted(...)` when review rows are size 0–1, reusing the existing row list directly.
  - Extended GS-V10-018 Listings full-table export DataFrame reuse (April 19, 2026):
    - review-history, channel-capability, and media-manager export factories now reuse the already-built render DataFrame in full-table mode, avoiding duplicate full DataFrame construction.
  - Extended GS-V10-018 Listings filtered-table export DataFrame reuse (April 19, 2026):
    - main Listings filtered export now reuses the already-built filtered table DataFrame in full-table mode, avoiding duplicate full DataFrame construction.
  - Extended GS-V10-018 Listings bulk-history/follow-up DataFrame reuse (April 19, 2026):
    - bulk-history export now reuses the already-built history DataFrame in full-table mode, and blocker follow-up table + CSV export now share one DataFrame instance to avoid duplicate construction.
  - Extended GS-V10-018 Listings readiness records-map reuse (April 19, 2026):
    - readiness bulk-review and bulk-publish planner selectors now share one cached `to_dict('records')` snapshot per rerun instead of running separate `iterrows()` passes over the readiness DataFrame.
  - Extended GS-V10-018 Listings readiness option-sort reuse (April 19, 2026):
    - blocker/warning option lists now reuse one pre-sorted key snapshot per rerun across follow-up and readiness-filter controls, avoiding duplicate sort passes.
  - Extended GS-V10-018 Listings readiness filter-series reuse (April 19, 2026):
    - readiness filtering now reuses precomputed format/blocker/warning string Series during filter branches, reducing repeated per-branch `astype`/`str` conversion setup.
  - Extended GS-V10-018 Listings readiness single-mask filtering (April 19, 2026):
    - readiness filter branches now compose one boolean mask and apply a single final DataFrame slice, reducing intermediate filtered-frame allocations during triage reruns.
  - Extended GS-V10-018 Listings follow-up option-set single-pass extraction (April 19, 2026):
    - blocker follow-up status/owner/priority/SLA option sets now derive from one row pass instead of separate per-field scans.
  - Extended GS-V10-018 Listings follow-up filter-loop metrics/map reuse (April 19, 2026):
    - follow-up filtered-row loop now also computes open/due-soon/overdue counters and open-task label map, removing extra list/dict passes after filtering.
  - Extended GS-V10-018 Listings follow-up tiny-set sort fast path (April 19, 2026):
    - follow-up history now skips sort work for 0–1 row sets in both event-ordering and final task-ordering phases.
  - Extended GS-V10-018 Listings follow-up action-normalization reuse (April 19, 2026):
    - follow-up history event loop now normalizes event action once per event and reuses it for status/last-action updates.
  - Extended GS-V10-018 Listings follow-up preset default-resolution single-pass (April 19, 2026):
    - follow-up preset map build now also resolves owner/shared default labels in the same pass, removing a second pass over preset labels.
  - Extended GS-V10-018 Listings follow-up preset selection tuple reuse (April 19, 2026):
    - preset action controls now reuse one selected preset tuple (`row`, `payload`) across apply/default/clear/delete actions, avoiding repeated map lookups.
  - Extended GS-V10-018 Listings follow-up preset membership-set reuse (April 19, 2026):
    - quick-preset actions now use precomputed option sets (`status/owner/priority/sla`) for membership checks, avoiding repeated scans over sorted option lists.
  - Extended GS-V10-018 Listings follow-up preset-row normalization reuse (April 19, 2026):
    - follow-up preset map loop now normalizes row flags (`username`, `is_shared`, `is_default`) once per row and reuses them for label/default selection decisions.
  - Extended GS-V10-018 Listings follow-up preset tiny-set label-sort + default-tuple reuse (April 19, 2026):
    - preset label options now skip `sorted(...)` for 0–1 label sets, and default-preset application now reuses the fetched default tuple payload without an extra map lookup.
  - Extended GS-V10-018 Listings follow-up option-list tiny-set sorting (April 19, 2026):
    - follow-up status/owner/priority/sla option-list derivation now skips `sorted(...)` for 0–1 value sets.
  - Extended GS-V10-018 Listings filter/selector option-list tiny-set sorting (April 19, 2026):
    - listings marketplace/status/category/listing-ID option-list derivation now skips `sorted(...)` for 0–1 value sets in filter, bulk-create, and media-manager selector paths.
  - Extended GS-V10-018 Listings cache/profile key tiny-set sorting (April 19, 2026):
    - audit-log cache limit reuse and create-flow store-profile option keys now skip `sorted(...)` for 0–1 key sets.
  - Extended GS-V10-018 Listings controls tiny-set sorting (April 19, 2026):
    - active-filter marketplaces/statuses, blocker/warning reason options, top-blocker quick-filter ranking, and bulk-draft candidate-product sorting now skip `sorted(...)` for 0–1 item sets.
  - Extended GS-V10-018 Listings item-specifics preview sort reuse (April 19, 2026):
    - item-specific default/aspect preview rendering now prebuilds key/item lists once and uses tiny-set sort fast paths, reducing repeated sort calls in the same rerun.
  - Extended GS-V10-018 Listings item-specifics remove-selector key reuse (April 19, 2026):
    - remove-aspect selector options now prebuild aspect keys once and use tiny-set sort fast path for key ordering.
  - Extended GS-V10-018 Listings document-source related-row tiny-set sorting (April 19, 2026):
    - related sale/order sorting in the listing document-source side panel now fast-paths 0–1 row sets and skips unnecessary sorting.
  - Extended GS-V10-018 Listings audit-cache reusable-limit scan optimization (April 19, 2026):
    - `_get_audit_logs` now finds smallest reusable cached limit via single-pass candidate scan instead of sorting cache keys each lookup.
  - Extended GS-V10-018 Listings publish-preset map reuse (April 19, 2026):
    - eBay publish-preset save flow now prebuilds lowercase preset-name map and reuses it for existing-preset detection, removing repeated linear preset scans.
  - Extended GS-V10-018 Listings create-preset default resolution single-pass (April 19, 2026):
    - create-flow default preset selection now resolves fallback/default in one pass over cached preset rows (first-row fallback + break on explicit default).
  - Extended GS-V10-018 Listings publish-preset dual-map single-pass build (April 19, 2026):
    - publish-preset UI now constructs label and lowercase-name preset maps in one pass over preset rows, removing duplicate preset-row iteration.
  - Extended GS-V10-018 Listings readiness default-preset resolver single-pass (April 19, 2026):
    - readiness default preset resolution now applies first-row fallback with early-break on explicit default in one pass over cached preset rows.
  - Extended GS-V10-018 Listings volume-tier tiny-set sort fast path (April 19, 2026):
    - volume pricing tier normalization now skips sorting for 0–1 tiers before min-qty duplicate validation.
  - Extended GS-V10-018 Listings default-aspect key-sort reuse (April 19, 2026):
    - item-specific default-aspect apply flow now reuses sorted default-aspect item keys for flash messaging instead of running a second key sort.
  - Extended GS-V10-018 Listings aspect-preview/remove option sort reuse (April 19, 2026):
    - item-specific editor now reuses sorted aspect preview items for remove-selector options, avoiding duplicate key sorting in the same rerun.
  - Extended GS-V10-018 Listings audit-entity filter cache reuse (April 19, 2026):
    - `_audit_logs_for_entity` now caches filtered entity rows by `(entity_type, limit)` per rerun, avoiding repeated entity-filter scans over identical audit slices.
  - Extended GS-V10-018 Listings saved-filter normalization single-pass loops (April 19, 2026):
    - marketplace/status set derivation now uses single-pass normalization loops and avoids repeated string-strip conversions per value.
  - Extended GS-V10-018 Listings audit-cache exact-limit memoization (April 19, 2026):
    - reusable-limit audit slices are now memoized back into exact-limit cache entries to avoid repeated slicing for identical limits in the same rerun.
  - Extended GS-V10-018 Listings entity-audit reusable-limit memoization (April 19, 2026):
    - entity-filtered audit cache now reuses nearest higher cached limit for the same entity type and memoizes exact-limit slices for repeated nearby limit requests.
  - Extended GS-V10-018 Listings aspect-remove branch elimination (April 19, 2026):
    - item-specific remove-selector options now derive directly from prebuilt `aspects_preview_items`, eliminating fallback normalization/sort branching.
  - Extended GS-V10-018 Listings audit-cache typed-key loop cleanup (April 19, 2026):
    - reusable-limit scans in raw/entity audit caches now use typed keys directly, removing redundant `int(...)`/`str(...)` conversions.
  - Extended GS-V10-018 Listings audit-cache redundant-copy elimination (April 19, 2026):
    - raw/entity audit cache memoization now avoids extra `list(...)` wrapping when storing reusable-limit slices and filtered rows.
  - Extended GS-V10-018 Listings orchestration-queue pre-DataFrame filtering (April 19, 2026):
    - orchestration status filter now applies on row dicts before DataFrame materialization, reducing DataFrame filter/copy overhead in queue rendering.
  - Extended GS-V10-018 Listings orchestration inline filter application (April 19, 2026):
    - orchestration queue now applies selected status filtering inline during row construction, reducing intermediate row-set size and downstream DataFrame payload for filtered views.
  - Started GS-V10-017 lifecycle close/archive controls:
    - added reversible listing archive/restore lifecycle support (`marketplace_details.lifecycle`) and listing hard-delete path for bad/duplicate records with linked-record detachment (sales/order-items/media references set null, history preserved)
    - added Listings `Danger Zone` controls (archive/restore/delete) and default-hidden archived-listing filtering (`Include Archived` toggle)
    - added product archive/restore repository + UI controls with active-listing guardrail (block archive unless `force=True`)
    - added lot archive/restore repository + UI controls with lifecycle metadata in lot notes and default-hidden archived-lot filtering
    - added media archive/restore model (`media_assets.is_archived`) with repository archive/restore APIs and default-hidden archived media queries
    - added Media Library and Search/Edit > Media archive/restore actions with `Include Archived` filtering
    - added Search/Edit lifecycle parity for Listings, Lots, and Media (archive/restore actions + archived visibility filters)
    - added lot/media archive preflight guardrails and force semantics:
      - repository blocker APIs (`get_purchase_lot_archive_blockers`, `get_media_asset_archive_blockers`)
      - lot/media archive operations now block linked active dependencies unless `force=True`
      - Lots, Media, and Search/Edit lifecycle panels now show blocker summaries + force toggles before archive
    - added repository regression coverage for listing delete detach, listing archive/restore metadata, product archive guardrails, lot archive/restore metadata, and media archive/restore filtering paths
    - expanded scheduled lifecycle retention cleanup to archived media/listings/lots/products (`lifecycle_archive_cleanup_enabled/timezone/local_time`, per-entity retain-day settings) with sync-runner integration-event telemetry (`integration=lifecycle_retention`, `action=cleanup`)
    - added conservative dependency guardrails during retention purge (linked listings/lots/products are skipped rather than deleted, with skipped counts emitted in cleanup details)
    - added Admin lifecycle retention controls (`lifecycle_archive_cleanup_*` scheduler keys + per-entity retain-day settings) with one-click manual lifecycle cleanup execution and integration-event audit logging (`integration=lifecycle_retention`, `action=manual_cleanup`)
    - added first lifecycle Playwright coverage scaffold (`tests/e2e/lifecycle_archive_flow.spec.ts`) for Listings danger-zone archive/restore roundtrip, with seed-safe skip behavior when no selectable listing row is present
    - added strict seeded lifecycle gate (`tests/e2e/lifecycle_archive_flow_strict.spec.ts`) for Listings archive/restore danger-zone transition assertions
    - added strict seeded lifecycle entities gate (`tests/e2e/lifecycle_entities_strict.spec.ts`) covering Products/Lots/Media lifecycle controls under strict seeded auth
    - added Admin lifecycle cleanup evidence persistence in Integrations tab (`Last Lifecycle Cleanup Run` JSON/timestamp) for manual-run sign-off artifacts
    - added Admin lifecycle retention e2e flow (`tests/e2e/admin_lifecycle_retention_flow.spec.ts`) for `Run Lifecycle Cleanup Now` + evidence-block validation
    - enabled strict lifecycle gates by default in QA CI for PR/push and retained workflow-dispatch strict lifecycle toggle (`qa_enable_strict_lifecycle`) for manual-run override
    - wired Admin lifecycle retention evidence e2e (`tests/e2e/admin_lifecycle_retention_flow.spec.ts`) into QA CI strict lifecycle gate execution
    - added Admin `Lifecycle Retention Policy Sign-Off Tracker` (Dev/Prod owner/date/status/evidence + cleanup policy snapshot) with CSV export and Go-Live evidence-pack artifact (`lifecycle_retention_policy_signoffs.csv`)
    - expanded Admin go-live governance e2e assertions to include lifecycle retention policy tracker visibility + lifecycle sign-off CSV control (`tests/e2e/admin_golive_flow.spec.ts`)
    - added admin helper regression assertion to keep lifecycle sign-off evidence-pack filename wiring locked (`tests/test_admin_helpers.py`)
    - latest strict lifecycle evidence runs (April 16, 2026):
      - Listings strict gate: `1 passed` (`tests/e2e/lifecycle_archive_flow_strict.spec.ts`)
      - Entities strict gate: `3 passed` (`tests/e2e/lifecycle_entities_strict.spec.ts`, deterministic Products/Lots/Media archive->restore roundtrip)

## GS-V10-018 App Performance Optimization (Completed)
- Goal: improve app responsiveness on high-density pages (Listings, Reports, Admin, Search/Edit, Intake wizards) without removing workflow capability.
- Scope:
  - establish measurable page performance baselines (render time, query count/duration, payload/table size) and set per-page budgets.
  - reduce DB load on slow pages via targeted rollups, tighter default filters/pagination, and index tuning driven by observed heavy queries.
  - reduce Streamlit rerun/render overhead by lazy-loading expensive sections, deferring non-critical panels, and minimizing full-page hydration on first load.
  - optimize table-heavy views with bounded default windows, on-demand expansion, and export-first patterns for large result sets.
  - optimize media-heavy views with thumbnail-first rendering and deferred full-resolution preview loading.
  - add System Health/Admin performance insights for ongoing hotspot visibility (slow-page counters, slow-query indicators, latest baseline snapshot).
  - add performance regression guardrails (baseline artifacts and targeted tests for known heavy repository/report paths).
- First-wave target pages:
  - Listings
  - Reports
  - Search/Edit
  - Admin (Governance/System Health)
  - Inventory/Coin Intake Wizards

## GS-V10-019 Slack AI Ops Bot (Planned)
- Goal: enable operator workflows directly from Slack (images + commands) while preserving strict in-app controls, approvals, and auditability.
- Initial Step 1 delivery (April 20, 2026):
  - added `app/services/slack_ops_bot.py` with canonical command envelope + deterministic idempotency keying.
  - defined command contract/allowlist intents (`intake`, `comp`, `status`, `operations`) and per-role routing guards.
  - added audited routing outcomes (`accepted`/`denied`/`rejected`) and integration telemetry (`integration=slack_ops`, `action=command_routed`).
  - added focused routing/guardrail regression coverage (`tests/test_slack_ops_bot.py`).
- Initial Step 2 delivery (April 20, 2026):
  - added `ingest_slack_command_request` queueing flow in `app/services/slack_ops_bot.py`.
  - queues accepted Slack commands into `integration_queue_jobs` (`integration=slack_ops`, `action=command_ingest`) with canonical request context (requester/channel/thread/message metadata).
  - added replay protection by idempotency-key dedupe against existing Slack-ops queue jobs and emits `deduped` audit/telemetry outcomes.
  - expanded Slack ops unit coverage for queued, duplicate, and denied-no-queue ingestion scenarios (`tests/test_slack_ops_bot.py`).
- Initial Step 3 delivery (April 20, 2026):
  - added queue execution support for `slack_ops/command_ingest` in `app/services/integration_queue.py`.
  - Slack file attachments are downloaded with bot-token auth (`url_private`/`url_private_download`), uploaded through existing S3 media storage, and persisted through existing repository pipelines:
    - image/video -> media assets
    - application/text documents -> purchase documents
  - ingestion supports optional target context hints (`product_id`, `listing_id`, `lot_id`, `source_id`, `document_kind`) carried in command payload.
  - added regression coverage for Slack ops media/document/no-file ingestion branches in `tests/test_integration_queue.py`.
- Initial Step 4 delivery (April 20, 2026):
  - added write-intent approval gating in `app/services/slack_ops_bot.py` controlled by runtime key `slack_ops_write_actions_require_approval` (default true).
  - accepted write commands are persisted with approval metadata and placed into queue `status=blocked` until approved.
  - added explicit approval helper `approve_slack_ops_queue_job(...)` with role enforcement (`admin`/`ops`) that stamps approval metadata and promotes job to `queued`.
  - added regression coverage for pending-approval and approval transitions in `tests/test_slack_ops_bot.py`.
- Initial Step 5 delivery (April 20, 2026):
  - integrated AI runtime into `slack_ops/command_ingest` queue execution for `comp` and `intake` intents.
  - comp requests route through `execute_comp_summary(..., workflow=\"comp\")`; intake requests route through `execute_multimodal_task(..., workflow=\"intake\")` using first image attachment when present.
  - concise operator-safe summary payload is persisted as `ai_response` on the queue job (summary/error/links/timestamp) with contextual links to related in-app records.
  - optional Slack channel auto-reply support added (runtime-gated, default off) to post AI summary back into originating channel.
  - added regression coverage for Slack ops AI summary persistence (`tests/test_integration_queue.py`).
- Initial Step 6 delivery (April 20, 2026):
  - added Slack ops guardrail runtime controls to ingestion path:
    - global enable/kill switch (`slack_ops_enabled`)
    - per-intent toggles (`slack_ops_intent_<intent>_enabled`)
    - channel allowlist (`slack_ops_allowed_channels`)
    - user allowlist (`slack_ops_allowed_users`)
    - request rate limit window + cap (`slack_ops_rate_limit_window_minutes`, `slack_ops_rate_limit_max_requests`)
  - guardrail rejections now emit explicit audit + integration telemetry outcomes (`command_ingest_rejected`) with rejection reason context.
  - added regression coverage for key guardrail rejection branches (`tests/test_slack_ops_bot.py`).
- Initial Step 7 delivery (April 20, 2026, partial):
  - added Admin Integration `Slack Ops Queue (Bot)` observability + governance panel with:
    - queue depth/status metrics and pending-approval SLA metrics,
    - detailed queue table including parsed intent/approval metadata,
    - triage actions (run due jobs, retry failed, approve pending write-intent jobs).
  - backed by normalized helper snapshot logic and regression coverage (`tests/test_admin_helpers.py`).
  - completed Step 7 follow-up: System Health now mirrors Slack Ops KPIs/trends (queue depth, blocked/pending approvals, approval SLA max, and 24h success/queued/failed/rejected counters) with helper test coverage.
- Initial Step 8 delivery (April 20, 2026):
  - finalized Slack Ops operational runbook: `SLACK_OPS_RUNBOOK.md` (runtime controls, approval escalation, incident handling, rollback/disable, and evidence expectations).
  - added lifecycle regression coverage for approval-gated execution (`ingest -> blocked -> approve -> process -> success`) in `tests/test_slack_ops_bot.py`.
  - fixed Slack Ops idempotency dedupe status matching to include successful queue jobs (`success`), with regression coverage.
- Initial Step 9 delivery (April 20, 2026):
  - added inbound Slack Socket Mode worker `app/services/slack_ops_runner.py` to accept `app_mention` and slash-command requests directly from Slack and ingest via `ingest_slack_command_request(...)`.
  - added in-thread Slack acknowledgements for accepted/duplicate/blocked/denied command outcomes, plus inline help when no intent is supplied.
  - added automatic due-queue execution loop for `integration=slack_ops` jobs in runner (`slack_ops_process_queue_*` controls) so bot requests can complete without manual Admin run-due actions.
  - added threaded AI auto-reply propagation in queue execution (`thread_ts` support in `send_slack_message`) for `comp`/`intake` summaries when enabled.
  - added compose service `slack_ops_worker` and helper tests `tests/test_slack_ops_runner.py`.
  - added backward-compatible operations aliases for existing bot commands:
    - `operations run_sync` -> `operations run_due slack_ops`
    - `operations queue_status` -> `status`
  - extended `operations` intent execution with:
    - `run_due <integration> [limit]`
    - `approve <queue_job_id>`
    - `create_ebay_draft <product_id> [price] [qty]`
  - extended `intake` intent execution to auto-create local product drafts when attachments are present and `product_id` is not provided:
    - AI-assisted field extraction (title/category/description/metal/weight hints)
    - media auto-link to created product
    - threaded summary including missing confirmations for operator follow-up
  - deployed Slack Ops worker as part of existing env stacks (not separate apps):
    - `k8s/templates/dev/deployment-slack-ops-worker.yaml` included in dev `kustomization.yaml`
    - `k8s/templates/prod/deployment-slack-ops-worker.yaml` included in prod `kustomization.yaml`
  - hardened Slack `comp` execution quality:
    - fixed ingest/audit integer-entity-id failure for hash idempotency keys in route audit writes.
    - comp execution now uses eBay sold-results HTML as primary eBay evidence source before AI summary generation (Finding path deprecated/non-primary).
    - added Slack comp web fallback hints path (DuckDuckGo HTML + visible-price parsing) when eBay rows are sparse/unavailable.
    - comp response metadata now records query/fetch strategy and evidence counts (`eBay rows`, `Web rows`, query, fetch mode) for operator triage.
    - comp suggested list band now uses runtime-configurable low/high percentages (`slack_ops_comp_band_low_pct`, `slack_ops_comp_band_high_pct`) with decimal-safe parsing and test coverage.
    - added direct eBay sold-results HTML fallback for Slack `comp` during Finding API throttles/unavailability, ahead of generic web fallback, with runtime controls and regression coverage.
    - expanded deprecation mitigation across app comp flows: shared `EbayClient` fallback path (`find_completed_items_with_fallback`) now supports Finding -> broadened completed -> eBay sold HTML fallback, and intake/wizard/tools paths are aligned to avoid hard dependency on `findCompletedItems`.
    - promoted the deprecation mitigation to primary runtime behavior: app comp surfaces now source eBay comps from sold-results HTML first and no longer call `findCompletedItems` in active workflows.
    - bounded Slack web-fallback detail-page fetches with runtime knobs (`slack_ops_comp_web_detail_fetch_limit`, `slack_ops_comp_web_detail_fetch_timeout_seconds`) to keep queue execution predictable while preserving structured page-price enrichment.
    - improved Slack web-fallback price robustness by filtering shipping-threshold promo prices (`orders $X`) and using median-selected row price from extracted hints to reduce outlier anchoring.
    - hardened Slack web-fallback price acceptance: non-product category/search URLs no longer contribute snippet-only prices unless structured page-price extraction succeeds.
    - added domain-aware product-page preference in Slack web fallback (APMEX product URLs prioritized over category/search URLs when both are present) to reduce false high anchors from category/promotional pages.
    - expanded product-page preference to additional bullion dealer domains (JM Bullion/SD Bullion/Monument/Provident/Silver.com and similar), reducing category-page noise in web-only comp fallback.
    - improved structured fallback extraction by parsing JSON-LD product/offer pricing and `og:price:amount`, increasing recovery of true product prices on dealer pages with minimal snippet pricing.
    - tightened structured fallback extraction to ignore generic raw `"price"` token scraping and JSON-LD range keys (`lowPrice`/`highPrice`), reducing category-page promo threshold contamination in comp pricing.
    - added confidence-weighted top-comp ranking so summary ordering favors higher-trust evidence (sold rows, structured page prices, product-like URLs) instead of raw highest-price snippet rows.
    - added explicit Slack comp confidence UX: top-row confidence tags plus aggregate evidence-confidence signal for faster operator trust assessment.
    - added runtime minimum-confidence gate so low-evidence comp runs suppress suggested list bands and emit explicit caution guidance in Slack summaries.
    - added runtime minimum-qualified-rows gate so thin comp samples (too few priced rows) cannot produce suggested list bands without explicit caution.
    - added runtime trusted-source-only mode for web fallback so comp rows can be constrained to approved bullion domains during stricter pricing operations.
    - added per-request `trusted_only=true|false` override for Slack comp commands, enabling strict trusted-source filtering on demand without toggling global runtime settings.
    - added per-request comp gate overrides (`confidence_gate`, `rows_gate`, `min_confidence`, `min_rows`) so operators can adjust strictness per command with explicit summary-link traceability.
    - added per-request `trusted_domains=` override for Slack comp commands to temporarily restrict trusted-source filtering to specific domains for a single request.
    - tightened `trusted_domains=` semantics so domain override auto-enables trusted-source filtering unless explicitly negated with `trusted_only=false`.
    - normalized evidence-gated AI field output in Slack comp summaries (including inline single-line markdown chains) so `Confidence`, `Suggested Range`, and `Recommendation` stay consistent with gate outcomes.
    - replaced generic gate metadata suffix with reason-specific fetch-mode tags (`evidence_gate_rows`, `evidence_gate_confidence`, `evidence_gate_confidence_rows`) for clearer operator/debug traceability.
    - dampened overall comp confidence for thin evidence sets (single-row or 2-row single-domain evidence) to avoid surfacing `high` confidence labels from one-source comps.
    - expanded comp summary observability with explicit `Qualified comps` and `Distinct sources` header metrics and `Min qualified comps` link metadata so operators can immediately compare observed evidence vs active gate threshold.
- Scope:
  - Slack bot command surface for:
    - image/document intake requests (inventory/purchase docs),
    - comp requests (manual query or image-seeded),
    - read operations (status, queue, KPI snapshots),
    - action requests (create draft, run sync job, trigger approved automations).
  - approval-gated write path:
    - all write-impacting requests become pending actions requiring explicit approval by authorized app users/roles.
    - no direct bypass of existing app permissions, readiness checks, or workflow guardrails.
  - secure artifact handling:
    - ingest Slack images/files into existing media/document pipelines without exposing private storage.
    - map Slack request context to workflow drafts for resume/review in app pages.
  - AI orchestration integration:
    - route Slack requests through existing AI runtime profiles/fallback chain with workflow-scoped prompt policy.
    - support image-assisted extraction/comp summaries with operator-visible diagnostics.
  - observability + governance:
    - full request/decision/execution audit trail (requestor, approver, action, payload hash, outcome).
    - operational metrics in Admin/System Health (queue depth, success/failure, median action time, approval SLA).
  - safety controls:
    - runtime kill switch, allowlisted Slack channels/users, per-command rate limits, replay/idempotency keys.

## GS-V10-020 Accounting Verification + AI Accountant (Active)
- Goal: make financial outputs trustworthy enough for routine close review by combining deterministic accounting checks with a role-gated AI accountant assistant.
- Canonical accounting model:
  - `products.acquisition_cost` is per-unit product acquisition basis before optional tax/shipping/handling landed components.
  - `products.product_cost` is fallback per-unit internal/product basis when acquisition cost is unavailable or the product was derived from conversion/intake workflows.
  - `purchase_lots.total_cost + total_tax_paid + total_shipping_paid + total_handling_paid` is whole-lot landed cost.
  - `product_lot_assignments.unit_*` / `allocated_*` are product-lot allocation overrides and take precedence over lot-level spreading.
  - Blank-cost lot assignments receive only the remaining unallocated lot landed cost after explicit assignment costs are honored.
  - Profit before returns uses `gross + shipping charged - fees - label spend - COGS`; final estimated profit after returns uses `profit before returns - return refunds + returned COGS reversal`. COGS uses FIFO for sale-level margin and lot-weighted costs for inventory/product rollups.
- Planned accounting double-check scope:
  - build monthly close dashboard with inventory value, sales net, COGS, profit before returns, estimated profit after returns, returns/refunds, fee-source quality, and shipping economics
    - Progress (April 26, 2026): first close-readiness panel is live in Reports with exportable readiness checks.
  - detect missing cost basis and lot allocation anomalies before reports are treated as close-ready
    - Progress (April 26, 2026): repository-backed Reports exception queue now covers missing cost/fee/shipping evidence, fee-source fallback rows, nonpositive margins, and lot allocation anomalies.
  - compare dashboard KPIs against Reports exports and normalized finance-entry rollups to prevent drift
  - add evidence exports and sign-off records for accounting acceptance
    - Progress (April 27, 2026): close packet ZIP export is live and Admin now tracks accounting model acceptance/monthly close sign-offs with owner/date/evidence fields and Go-Live Evidence Pack export.
- Planned AI Accountant scope:
  - add `accountant_agent` / `finance_agent` prompt mode with read-only default access to Reports, Orders, Sales, Products, Lots, Returns, and finance entries
    - Progress (April 27, 2026): `accountant_agent` is available in Ask GoldenStackers with `accounting` domain scoping and `ai_accountant_use` enforcement; Reports has a separate AI Accountant close-readiness review action.
  - provide accountant-facing summaries: "what changed this month", "why did margin move", "which lots/products need cost cleanup", and "which fee/shipping rows need reconciliation"
    - Progress (April 27, 2026): accounting snapshots include Reports economics, accounting exceptions, lot allocation source counts, fee/shipping evidence, and close-readiness context.
  - allow proposal drafting for corrections, mappings, and close notes; require explicit admin/finance approval before any write action
  - audit every AI accountant response with prompt hash, data scope, cited rows, model/profile, and whether output was accepted/edited/rejected
  - period-level drift check output now compares dashboard, Reports, QBO staging, Slack, and AI accounting totals before sign-off, including estimated profit after returns; approved close sign-off evidence is now reviewed against current readiness/blocker/drift counts and packet references; Reports can now record the close sign-off evidence directly from the close packet workflow; close arithmetic, sales component tie-out, return tie-out, inventory valuation, fee evidence, shipping evidence, marketplace reconciliation tie-out, COGS source, and lot allocation checks now block stale/incorrect totals before AI/accountant sign-off; next close-hardening step is production-sample packet review and completed owner/date sign-off evidence.

## GS-V10-021 Tax Reporting + Guidance (Active)
- Goal: make tax-related reporting review-ready while keeping all tax guidance advisory, evidence-based, and explicitly subject to accountant/tax-advisor validation.
- Planned tax reporting scope:
  - consolidate Reports tax outputs into a tax review workspace covering Tax Summary, Tax by Marketplace, Tax Detail, Tax Drilldown, marketplace-facilitator exclusions, exempt-category treatment, shipping-taxability assumptions, and Documents handoff evidence.
    - Progress (April 28, 2026): Reports now renders/exports `Tax Exceptions / Advisor Review` rows from Tax Detail evidence for jurisdiction/rate/category gaps, facilitator-channel inclusion, exempt-category review, and taxable shipping on exempt items.
  - add monthly/quarterly tax packet exports with manifest, sale-level taxable/exempt evidence, marketplace facilitator channel treatment, jurisdiction/rate assumptions, shipping taxable/exempt basis, exemption basis, and advisor-review notes.
    - Progress (April 28, 2026): first tax review packet ZIP export is live with manifest, stable SHA-256 evidence hash, Tax Summary, Tax by Marketplace, Tax Detail, and Tax Exceptions / Advisor Review CSVs.
    - Progress (May 18, 2026): Reports Tax Drilldown now generates a Colorado SUTS upload workbook for any selected `YYYY-MM` filing month from the official `Upload Data` template using account `080390`, and the Reports control is now labeled/defaulted as `Load Shipping + Tax/SUTS Analytics (slower)` so the SUTS panel is visible by default; the export keeps A1 blank, writes account numbers as numeric cells, writes gross sales as text, appends both Golden `11-0042`/`110042` rows when selected (`STATE` with SUTS account `970074130001` and `LOCAL`/self-collected with a blank account allowed for SUTS-accepted zero filings unless a local account is assigned), supports multiple gross-sales rows so direct Golden sales can populate both the state-administered and self-collected Golden rows for the same month, zero-files both Golden rows for eBay/facilitator-only scoped months, warns if a nonzero Golden `LOCAL` gross-sales row has no account number because only blank-account zero filing has been observed as accepted, shows in-app guidance beside the Tax Marketplace Filter and SUTS panel that eBay/facilitator channels should stay unselected for normal SUTS remittance unless advisor-confirmed, adds a SUTS Scope Check table that separates reportable direct/local gross from excluded marketplace-facilitator gross for the selected filing month, prunes unselected template jurisdictions from the generated upload file to avoid noisy SUTS excluded-row warnings, validates selected rows for missing/ambiguous account evidence before download, defaults facilitator-only periods such as eBay-only months to no local tax-liability marketplace scope instead of silently including eBay, leaves deduction/exemption columns blank for advisor/local-code review, and bundles the SUTS XLSX into the Tax Review Packet with SHA-256 manifest/evidence-hash coverage.
  - add tax exception checks for missing jurisdiction, missing/ambiguous tax mode, missing exemption basis, facilitator-channel mismatch, taxable shipping conflicts, marketplace-collected tax mismatch, and bullion/coin exemption review needs.
  - add saved tax profiles for repeatable jurisdiction/channel rules, including explicit effective-date metadata and "needs human validation" status for local/state rule changes.
    - Progress (April 28, 2026): Admin `Tax Profile + Sign-Off Tracker` persists profile evidence through audit events and CSV/evidence-pack exports.
    - Progress (April 28, 2026): Reports Tax Reporting Scope can apply saved profile assumptions for jurisdiction, rate, shipping taxability, facilitator channels, and exempt categories; Tax Review Packet manifest includes selected profile evidence.
  - add tax reporting sign-off records with period, jurisdiction, owner, advisor/evidence reference, status, and packet hash/export path.
    - Progress (April 28, 2026): Admin tax reporting sign-offs capture period, jurisdiction, profile key, owner/date/status, advisor evidence, exception count, and packet reference; Go-Live Evidence Pack includes `tax_reporting_signoffs.csv`.
    - Progress (April 28, 2026): Reports exports `tax_reporting_signoffs.csv` in Tax Review and Accounting Close packets.
    - Progress (May 3, 2026): Reports now records Tax Reporting Sign-Off audit evidence directly from the Tax Review Packet workflow, including packet hash, advisor evidence, owner/date/status, profile key, jurisdiction, and tax exception count.
    - Progress (May 3, 2026): Reports now adds Tax Reporting Sign-Off Review beside the Tax Review Packet workflow, flagging approved tax sign-offs when recalculated jurisdiction/profile, exception count, owner/date, advisor evidence, packet reference, or packet hash no longer match.
    - Progress (May 4, 2026): Tax Reporting Sign-Off Review now exports in both Tax Review and Accounting Close packets and is included in AI Accountant context/citations for advisor-evidence review.
    - Progress (May 4, 2026): Reports Copilot context now includes tax review assumptions, selected tax profile evidence, tax reporting sign-off rows, and Tax Reporting Sign-Off Review rows.
    - Progress (May 4, 2026): Reports Copilot now asks for an explicit `tax_review_findings` output category so tax profile/sign-off evidence is surfaced beside margin, reconciliation, export, and next-action findings.
    - Progress (May 4, 2026): Reports Copilot now renders structured JSON sections as readable bullets, including Tax Review Findings, while retaining the raw JSON for audit/debug review.
    - Progress (May 4, 2026): Reports AI Accountant result rendering now turns structured close/profit/lot/fee/action/tax-guardrail JSON sections into readable bullets while retaining raw JSON for audit/debug review.
    - Progress (May 4, 2026): Reports Copilot and AI Accountant section rendering now tolerates fenced or prefaced JSON responses, keeping tax/accounting findings visible even when the model wraps the requested JSON.
    - Progress (May 4, 2026): Reports AI Accountant audit metadata now includes deterministic prompt and data-scope hashes plus packet evidence hashes and compact cited row counts for tax/accounting review traceability.
    - Progress (May 4, 2026): Reports Copilot reviews now record read-only `reports_copilot_review` audit events with deterministic prompt/data-scope hashes, packet evidence hashes, tax/accounting citation row counts, and model citation metadata when available.
    - Progress (May 4, 2026): Reports Copilot and AI Accountant results now expose accepted/edited/rejected feedback actions that write outcome audit events tied to the response hash and original review metadata.
    - Progress (May 4, 2026): Reports now surfaces AI Review Outcome Evidence from audit logs, includes it in Accounting Close Packet exports, and passes it into AI Accountant context/citations for close-review feedback traceability.
    - Progress (May 5, 2026): Accounting Close Readiness now adds an `AI Review Outcome Follow-Up` check and blocks close-ready status when the latest Copilot/AI Accountant outcome per review type is `edited` or `rejected`.
    - Progress (May 5, 2026): Accounting Close Sign-Off evidence now captures `ai_review_followup_count`, and Sign-Off Review warns when approved sign-offs no longer match recalculated AI review follow-up blockers.
    - Progress (May 5, 2026): Reports `Record Accounting Close Sign-Off` workflow now displays the readiness/blocker/drift/AI-follow-up snapshot that will be captured with the sign-off evidence.
- Planned tax guidance scope:
  - add a role-gated AI Tax/Accounting guidance mode that can summarize tax report evidence, explain assumptions, identify records needing review, and draft questions for the tax advisor.
    - Progress (April 28, 2026): Reports AI Accountant consumes tax profile/sign-off evidence and keeps tax outputs advisory via unsupported-tax/legal guardrails.
  - keep tax guidance read-only and non-authoritative: no filing instructions, no unsupported legal conclusions, and no taxability decisions without citing configured profiles, source rows, and human-review status.
  - require responses to label estimates versus actual marketplace-collected/remitted amounts and include a local/state validation reminder, especially for bullion/coin exemptions and shipping taxability.
  - audit every tax guidance response with prompt hash, data scope, cited rows/profiles, model/profile, and accepted/edited/rejected outcome.

## Cross-Phase Technical Standards
- Migrations required for all schema changes
- Feature flags for risky integrations
- Idempotent sync jobs and retry-safe write logic
- Test coverage for repository/business logic on new domains
- Documentation updated per milestone (README + runbooks)

## Suggested Delivery Sequence
1. Build `inventory_movements` before expanding order logic.
2. Ship `orders/order_items` and migrate sales workflows to consume them.
3. Add returns/refunds once orders are canonical.
4. Implement eBay pull sync before push actions.
5. Add cost basis + accounting exports after sync data is stable.
