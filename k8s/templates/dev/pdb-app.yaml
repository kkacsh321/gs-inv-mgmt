apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gs-inv-app-pdb
  labels:
    app.kubernetes.io/name: gs-inv-mgmt
    env: dev
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: gs-inv-app
