apiVersion: batch/v1
kind: Job
metadata:
  name: gs-inv-migrate
spec:
  backoffLimit: 6
  template:
    spec:
      containers:
        - name: migrate
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
